# DSD W1 Finite-Parent Weak-L3 Core-Blob Gate

Date: 2026-08-26

Status: **GLOBAL WEAK-L3 LOOP-GAIN THRESHOLD LOCALIZED TO A FIXED FINITE PARENT REGION / LARGE CRITICAL NORM CANNOT LIVE ONLY IN THE REMOTE `1/r` TAIL / RECURRENT HIGH-AMPLITUDE CORE OCCUPANCY FORCED / GLOBAL REGULARITY UNPROVED.**

## 1. Inputs

The invariant endpoint obeys

\[
\langle F_P\rangle
=
\nu\langle D_3\rangle
+
\mathfrak J_c,
\qquad
\mathfrak J_c=\frac{\mathscr R_3}{6}>0.
\]

The global Lorentz estimate gives

\[
|F_P|
\le
C_{WL3}
\|U\|_{L^{3,\infty}}D_3.
\]

We now localize the payer.

---

## 2. Remote pressure cannot pay the fixed core work

Fix a core radius `R0` containing the recurrent nonlinear core.

The existing finite-parent pressure locality estimate gives, for sources outside `RP >> R0`,

\[
\boxed{
\|\nabla P_{>R_P}\|_{L^\infty(B_{R_0})}
\le
C_P Z_+R_P^{-2}.
}
\]

Hence

\[
\begin{aligned}
\left|
\int_{B_{R_0}}
|U|U\cdot\nabla P_{>R_P}
\right|
&\le
\|\nabla P_{>R_P}\|_\infty
\int_{B_{R_0}}|U|^2\\
&\le
C_{core}R_P^{-2}.
\end{aligned}
\]

Thus the pressure generated arbitrarily far away cannot carry a fixed positive `p=3` work in the core.

---

## 3. Far-tail D3 and pressure work are also negligible

The existing endpoint shell estimates give summable far tails for `D3` and for the gauge-repaired pressure work.

Therefore for every `epsilon>0` one may choose `R0` and then `RP` sufficiently large so that, in invariant mean,

\[
\boxed{
\text{all contributions outside the finite parent buffer}
<\varepsilon.
}
\]

Choose `epsilon` smaller than a fixed fraction of `J_c`.

The endpoint balance then forces a positive finite-parent pressure-amplitude work.

---

## 4. Local Lorentz gain estimate

Decompose the pressure source into a near part generated by a cutoff of `U tensor U` inside the parent buffer and a remote harmonic part.

For the near part, the same Lorentz interpolation argument as in the global weak-L3 gate gives

\[
\|P_{near}\|_{L^{3,1}}
\le
C_{loc}
M_{loc}^{1/2}
D_{3,parent}^{1/2},
\]

where

\[
\boxed{
M_{loc}
:=
\|U\|_{L^{3,\infty}(B_{R_P})}.
}
\]

Likewise

\[
\|e\|_{L^{3/2,\infty}(B_{R_0})}
\le
C_{loc}
M_{loc}^{1/2}
D_{3,parent}^{1/2}.
\]

Therefore

\[
\boxed{
|F_{P,core}|
\le
C_{loc}
M_{loc}D_{3,parent}
+
\varepsilon.
}
\]

Since `D3_parent <= D3`, this is a true finite-parent version of the loop-gain inequality.

---

## 5. A local weak-L3 threshold is mandatory

Let

\[
D_{3,max}
:=
\sup_{U\in M}D_3(U)<\infty
\]

on the compact W1 class.

Choose the tail/localization errors below `J_c/4`.

If every state satisfied

\[
M_{loc}
\le
\frac{\nu}{C_{loc}}
+
\frac{\mathfrak J_c}
{4C_{loc}D_{3,max}},
\]

then the invariant finite-parent pressure work would be unable to pay

\[
\nu\langle D_3\rangle+\mathfrak J_c.
\]

Therefore the compact minimal class contains states satisfying

\[
\boxed{
M_{loc}
\ge
M_{loc,*}
>
\frac{\nu}{C_{loc}}.
}
\]

Because the relevant local norms/functionals are continuous on the compact smooth W1 class after fixing the finite parent ball, the strict superlevel set is open.

Minimality therefore makes such large-local-weak-L3 events recurrent with bounded Leray-time gaps.

---

## 6. Weak-L3 lower bound forces an actual amplitude blob

Let

\[
V_P=|B_{R_P}|
\]

and let

\[
A_P
=
\sup_{U\in M}\|U\|_{L^\infty(B_{R_P})}<\infty.
\]

By the definition of the weak `L3` norm, for every state with

\[
M_{loc}\ge M_{loc,*}
\]

there exists an amplitude level `lambda` such that

\[
\lambda
\left|
\{|U|>\lambda\}\cap B_{R_P}
\right|^{1/3}
\ge
\frac12M_{loc,*}.
\]

Since the set has volume at most `V_P`, the witnessing level obeys

\[
\boxed{
\lambda
\ge
\frac{M_{loc,*}}{2V_P^{1/3}}
=:a_*>0.
}
\]

Also `lambda <= A_P`, so

\[
\boxed{
\left|
\{|U|>\lambda\}\cap B_{R_P}
\right|
\ge
\left(
\frac{M_{loc,*}}{2A_P}
\right)^3
=:v_*>0.
}
\]

Thus every large-local-weak-L3 event contains a genuine normalized high-amplitude blob:

\[
\boxed{
|U|\ge a_*
\quad\text{on a set of volume at least }v_*.
}
\]

---

## 7. Physical interpretation

At physical scale

\[
r(t)=\sqrt{T_*-t},
\]

the recurrent blob becomes a region of volume

\[
\gtrsim v_*r(t)^3
\]

with velocity amplitude

\[
\gtrsim a_*r(t)^{-1}.
\]

Its kinetic energy is only order

\[
r(t),
\]

so this is still compatible with the finite-energy parent.

Its `L3` mass, however, is order one and scale invariant.

Thus the gate is a structural concentration theorem, not yet an energy contradiction.

---

## 8. Updated DSD endpoint

The large weak-critical survivor cannot hide its nonlinearity only in a remote endpoint tail.

It must recurrently contain

\[
\boxed{
\text{a finite-parent high-amplitude core blob}
+
\text{pressure-amplitude loop gain above one}
+
\text{critical scale memory at infinity}.
}
\]

The remaining theorem must show that this finite normalized blob cannot recurrently maintain the required pressure/strain/vorticity loop while the physical scale contracts to zero, or must otherwise break the large weak-critical endpoint.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
