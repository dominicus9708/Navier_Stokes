# Exact bridge from Gaussian residual source to directional stretch / axis conversion

Date: 2026-08-14

Status: **EXACT SOURCE DECOMPOSITION + SOURCE-GEOMETRY DICHOTOMY / DRIFT-DOMINANT BRANCH REMAINS**.

This note sharpens the channel-coupled residual analysis.  The first part of the Gaussian-averaged residual vorticity source is not merely bounded by the residual gradient: because the antisymmetric residual gradient is generated by the same vorticity fluctuation, it annihilates that fluctuation.  Therefore the source couples exactly to the mean-free strain.

No novelty claim is made without a separate literature-priority audit.

---

## 1. Residual gradient decomposition

On the self-consistent Gaussian affine window write

\[
r=u-a'-Ly,
\qquad
G:=\nabla r=\nabla u-L.
\]

Let

\[
\delta S=S-\bar S_\gamma,
\qquad
\delta\Omega=\Omega-\bar\Omega_\gamma.
\]

Since `L` is the Gaussian mean of `grad u`, the symmetric and antisymmetric parts of `G` are

\[
\operatorname{sym}G=\delta S,
\qquad
\operatorname{skew}G=A(\delta\Omega),
\]

where `A(omega)` is the antisymmetric matrix representing cross product with `omega` up to the conventional factor/sign.

For every vector `omega`,

\[
A(\omega)\omega=0.
\]

Hence

\[
\boxed{
G\,\delta\Omega
=\delta S\,\delta\Omega.
}
\]

Equivalently,

\[
\boxed{
(\delta\Omega\cdot\nabla)r
=\delta S\,\delta\Omega.
}
\]

This is exact and removes the residual rotational part from the stretching source.

---

## 2. Exact Gaussian-mean source split

The residual vorticity source is

\[
f_r=(\Omega\cdot\nabla)r-(r\cdot\nabla)\Omega.
\]

Using

\[
\int\gamma\nabla r=0,
\qquad
\int\gamma r=0,
\qquad
\nabla\cdot r=0,
\]

and subtracting the Gaussian-mean vorticity gives

\[
\boxed{
\int\gamma f_r
=J_{\rm str}+J_{\rm drift},
}
\]

where

\[
\boxed{
J_{\rm str}
:=\int\gamma\,\delta S\,\delta\Omega,
}
\]

and

\[
\boxed{
J_{\rm drift}
:=\int\gamma\,\delta\Omega\,
(r\cdot\nabla\log\gamma).
}
\]

Thus every residual endpoint source is produced by either

1. mean-free strain acting on vorticity fluctuation; or
2. Gaussian-weight drift/transport of vorticity fluctuation.

---

## 3. Directional decomposition of the stretching source

At points where `delta Omega != 0`, set

\[
n=\frac{\delta\Omega}{|\delta\Omega|}.
\]

For symmetric `delta S`, define

\[
q_n=n^T\delta S\,n
\]

and the residual-axis conversion magnitude

\[
\chi_n
=|(I-n\otimes n)\delta S\,n|.
\]

Orthogonality of the parallel and perpendicular components gives

\[
\boxed{
|\delta S\,n|^2
=q_n^2+\chi_n^2.
}
\]

Therefore

\[
\boxed{
|\delta S\,\delta\Omega|^2
=|\delta\Omega|^2
\left(q_n^2+\chi_n^2\right).
}
\]

In an eigenbasis of `delta S`, with eigenvalues `lambda_i` and `b_i=(n dot e_i)^2`, the conversion term is

\[
\boxed{
\chi_n^2
=\sum_{i<j}b_i b_j(\lambda_i-\lambda_j)^2.
}
\]

Thus the same exact axis-conversion identity used in the geometric channel now appears inside the residual source itself.

---

## 4. Efficient stretching source forces stretch or conversion energy

Because `gamma` is a probability measure,

\[
|J_{\rm str}|^2
\le
\int\gamma|\delta S\,\delta\Omega|^2.
\]

Suppose at some time

\[
|J_{\rm str}|
\ge
\eta\sqrt{V_\omega B},
\qquad
0<\eta\le1,
\]

where

\[
V_\omega=\int\gamma|\delta\Omega|^2,
\qquad
B=V_S+\frac12V_\omega.
\]

Then

\[
\boxed{
\int\gamma|\delta\Omega|^2
\left(q_n^2+\chi_n^2\right)
\ge
\eta^2V_\omega B.
}
\]

Hence at least one of the following must hold:

\[
\boxed{
\int\gamma|\delta\Omega|^2q_n^2
\ge
\frac{\eta^2}{2}V_\omega B
}
\]

or

\[
\boxed{
\int\gamma|\delta\Omega|^2\chi_n^2
\ge
\frac{\eta^2}{2}V_\omega B.
}
\]

The first is a **directional stretch/compression channel** along the fluctuating vorticity direction.  The second is an **axis-conversion channel**.

This is an exact algebraic-geometric dichotomy once the stretching part of the source is efficient.

---

## 5. Relation to the source-efficiency parameter

Define

\[
\theta=V_\omega/B,
\qquad
0\le\theta\le2.
\]

The full residual source obeys

\[
|J_{\rm str}+J_{\rm drift}|
\lesssim_K\sqrt\theta\,B.
\]

If a surviving residual cascade produces a fixed endpoint contribution, the preceding dissipation argument already forces

\[
\Lambda\theta^{5/6}\to\infty
\]

in the notation

\[
m=W^{-1/3}\Lambda.
\]

The present result says more: whenever a non-negligible fraction of that source is carried by `J_str`, the source is not an untyped scalar budget.  It must appear as weighted directional stretching or weighted axis conversion.

---

## 6. Source split over a time interval

Let `I` be a responsible residual interval and suppose

\[
\left|
\int_I \mathcal K(s)
[J_{\rm str}(s)+J_{\rm drift}(s)]ds
\right|
\ge\rho,
\]

where the bounded-affine Duhamel propagator factor `K(s)` has uniformly bounded operator norm.

By the triangle inequality, at least one channel must carry a fixed fraction of the absolute source budget after discarding cancellations in the usual way:

### Stretch-source branch

A non-negligible integrated amount is carried by `J_str`.  Then the residual source is routed to

\[
\boxed{
\text{vorticity-direction stretch}
\quad\text{or}\quad
\text{axis conversion}.
}
\]

### Drift-source branch

A non-negligible integrated amount is carried by

\[
J_{\rm drift}
=
\int\gamma\delta\Omega
(r\cdot\nabla\log\gamma).
\]

This branch requires separate Gaussian creation/annihilation or Hermite analysis.

The current unresolved residual route is therefore no longer a single non-affine channel.

---

## 7. Important limitation: fluctuation strain versus total strain

The quantity in the exact bridge is

\[
\delta S=S-\bar S_\gamma,
\]

whereas material Cauchy stretching uses the total strain `S` along trajectories.

Therefore one may not identify `q_n=n^T delta S n` directly with cumulative material stretching without controlling the Gaussian affine mean `bar S` and the relation between the fluctuating vorticity direction and material vorticity direction.

On the mesoscopic regime `R >> W^(1/10)`, the coherent affine mean is already small in the endpoint scaling, but a precise pathwise conversion still needs to be written before claiming a Cauchy contradiction.

---

## 8. Current frontier produced by this bridge

Combining the exact source split with the previous critical-ridge constraints yields four typed exits for a surviving bounded-affine pulse:

\[
\boxed{
\begin{array}{ll}
\text{A.} & \text{directional stretch of the vorticity fluctuation},\\
\text{B.} & \text{axis conversion / eigenframe participation},\\
\text{C.} & \text{Gaussian drift/transport source},\\
\text{D.} & \text{departure from bounded-affine geometry.}
\end{array}
}
\]

The immediate next target is the drift branch, because A and B now connect to already existing Cauchy/projective-axis ledgers.

Status: **RESIDUAL STRETCH SOURCE EXACTLY TYPED INTO DIRECTIONAL STRETCH OR AXIS CONVERSION / GAUSSIAN DRIFT SOURCE IS THE NARROWEST NEW UNTYPED BRANCH**.
