# Frontier: parabolic critical renormalization loop

Date: 2026-08-16

Overall status: **THE RECENT-SOURCE ENDGAME HAS BEEN SHARPENED TO A PARABOLIC-CRITICAL RENORMALIZATION LOOP. SCALAR ENERGY/TIME ESTIMATES ARE NOW EXPLICITLY SATURATED. EVERY MINIMAL LATE EPISODE MUST RETURN TO A SUPERCRITICAL RESIDUAL CROSSING, A THIN DERIVATIVE/PRESSURE PULSE, OR LOGARITHMIC SYMMETRIC STRAIN. THE SPATIALLY TIGHT TURNOVER-BAND NEAR-SLOW FAST-ROTATION INPUT HAS ALSO BEEN DEPLETED BY A LOCAL UNCERTAINTY GAIN. GLOBAL REGULARITY NOT PROVED.**

---

## 1. Previous frontier

The previous frontier established:

1. a clean low-enstrophy precursor cannot contribute order-one vorticity by homogeneous advection-diffusion;
2. sufficiently old stretching source is pointwise negligible;
3. the coherent crossing must be freshly generated by a recent source layer;
4. the terminal coherent core already carries normalized enstrophy-time occupancy `>= c R^3` on a fixed normalized terminal block;
5. attempts to make the source thinner route into V2/higher-derivative concentration.

The open question was whether these recent episodes can repeat infinitely while their physical costs remain summable.

---

## 2. Dissipation-tail adaptive source horizon

Let

\[
d_j=\int_{t_j-W_j^{-1/2}}^{T^*}\|\omega(t)\|_2^2dt\to0.
\]

Splitting the adjoint-kernel source at that physical time shows that, for any arbitrarily slowly diverging `g_j`, source older than

\[
\boxed{
L_j
=g_j(\sqrt{W_j}d_j)^{2/3}
}
\]

normalized time is negligible.

Terminal coherent occupancy gives

\[
d_j\gtrsim R_j^3/\sqrt{W_j}.
\]

Define

\[
\boxed{
\Xi_j
=\frac{d_j\sqrt{W_j}}{R_j^3}\gtrsim1.
}
\]

Then

\[
\boxed{
L_j
=g_j\Xi_j^{2/3}R_j^2.
}
\]

Thus the minimal-dissipation branch has a recent source horizon equal, up to slow factors, to one coherent-core parabolic time.

This is an exact scalar saturation: the old-source kernel tail cannot localize the source below `R^2`, because the terminal coherent core itself already supplies enough dissipation to stop that improvement.

---

## 3. Excess-dissipation versus critical-saturation branch

The late sequence splits into

\[
\boxed{\Xi_j\to\infty}
\]

or, after a subsequence,

\[
\boxed{\Xi_j=O(1).}
\]

The first is an explicit excess-dissipation branch.

The second is the sharp minimal survivor:

\[
\boxed{
\text{fresh source on one core-parabolic time}
+\text{minimal coherent occupancy}.
}
\]

No scalar time/energy improvement remains on this branch.

---

## 4. Exact residual-seed / affine-amplification factorization

For the terminal-conditioned adjoint kernel,

\[
\bar\Omega_K'
=\bar S_K\bar\Omega_K+J_K,
\qquad
|J_K|\lesssim B_K.
\]

On the recent interval define

\[
A_R=\int\|\bar S_K\|ds,
\qquad
\mathcal B_R=\int B_Kds.
\]

Older/homogeneous contribution is negligible, while the crossing mean is order one. Hence

\[
\boxed{
1\lesssim e^{A_R}(o(1)+\mathcal B_R).
}
\]

Therefore for every fixed `gamma>0`,

\[
\boxed{
\mathcal B_R\ge R^{-\gamma}
\quad\text{or}\quad
A_R\ge\gamma\log R-O(1).
}
\]

Residual covariance supplies seed; symmetric affine strain supplies multiplicative amplification. Skew coherent rotation cannot replace either because it is energy preserving.

---

## 5. Merge with the dynamic Reynolds crossing

Fix `epsilon in (0,2)` and choose `gamma=2-epsilon`.

Then

\[
\boxed{
\mathcal B_R\ge R^{-2+\epsilon}
\quad\text{or}\quad
A_R\ge(2-\epsilon)\log R-O(1).
}
\]

If a fixed fraction of the residual seed action lies in the parabolic bulk `tau asymp R^2`, then at some bulk time

\[
B_K\gtrsim R^{-4+\epsilon}.
\]

Hence

\[
\boxed{
\mathcal R_G
=R^2\sqrt{B_K}
\gtrsim R^{\epsilon/2}\to\infty.
}
\]

The residual is again supercritical. Because the endpoint kernel collapses to `R_G=0`, continuity forces another Reynolds-one crossing.

Thus

\[
\boxed{
\text{parabolic residual seed}
\Rightarrow
\text{supercritical residual pulse}
\Rightarrow
\text{new critical crossing}.
}
\]

The recent-source problem therefore recursively returns to the earlier critical-crossing tree.

---

## 6. Thin residual seed

If the residual action avoids the parabolic bulk, it must concentrate in a thinner age layer.

Gaussian Poincare and the exact residual-variance dynamics route such concentration to

\[
\boxed{
\text{V2/high-curvature},
\quad
\text{pressure-Hessian},
\quad
\text{or affine-deformation concentration}.
}
\]

Thus thin residual seeding is not an independent escape.

---

## 7. Logarithmic strain branch

If the residual seed stays below `R^(-2+epsilon)`, then

\[
\boxed{
A_R\ge(2-\epsilon)\log R-O(1).
}
\]

This returns to the material-area contraction, strain deformation, local Betchov mismatch, and positive-middle-strain channels.

Fast rotation does not erase this symmetric-strain action.

External rapidly rotating Navier--Stokes regularity results do not directly close this local branch: their hypotheses concern globally rotating systems with global Coriolis structure and specific periodic/stress-free settings, whereas the present rotation is a local adaptive affine background. They are useful only as structural comparison, not as an imported theorem.

---

## 8. Local near-slow uncertainty depletion

The earlier helical reduction left near-slow input concentration as one exact-resonance remainder.

On the spatially tight turnover branch, write

\[
\nabla_y r=R^{-1}G(z),
\qquad y=Rz,
\]

with `G` bounded in `L2` on a fixed turnover-scale cutoff and with turnover-band frequencies `|xi| asymp 1`.

For the near-slow slab

\[
|\xi_\parallel|\le\delta,
\]

its dyadic frequency volume is `O(delta)`. Spatial localization gives `||G||_1 <= C||G||_2`, hence

\[
\boxed{
\|P_{\rm slow,\delta}G\|_2
\lesssim
\delta^{1/2}\|G\|_2.
}
\]

To remain slow over `O(R)` fast-rotation turnover blocks requires

\[
\delta\lesssim R^{-1}.
\]

Therefore

\[
\|P_{\rm slow}G\|_2\lesssim R^{-1/2}.
\]

Since the residual mean source contains the physical gradient scaling `R^-2`, a local bilinear source with one near-slow factor obeys

\[
|J_{\rm slow}|\lesssim R^{-5/2}.
\]

Integrated through the full `R^2` mean-creation time,

\[
\boxed{
\int|J_{\rm slow}|dt
\lesssim R^{-1/2}\to0.
}
\]

Hence the **spatially tight, turnover-band near-slow input cannot provide an order-one secular source**.

A surviving near-slow branch must lose at least one hypothesis:

\[
\boxed{
\text{spatial nontightness}
\quad\lor\quad
\text{low/high-frequency escape}
\quad\lor\quad
\text{localization/shell commutator}.
}
\]

These are already existing branches.

This does **not** yet close general uniformly-fast near-resonant interactions, because the Gaussian localized output is not literally a single global slow Fourier mode.

---

## 9. Closed renormalization graph

The endgame is now the loop

\[
\boxed{
\text{coherent critical crossing}
\to
\text{fresh parabolic source}
\to
\begin{cases}
\text{supercritical residual pulse}\
\text{thin V2/pressure pulse}\
\log R\text{ symmetric strain}
\end{cases}
}
\]

with

\[
\boxed{
\text{supercritical residual pulse}
\to
\text{another Reynolds-one crossing}.
}
\]

Inside the fast-rotation realization of this loop, a spatially tight near-slow turnover packet is now perturbative and cannot be the secular source.

Thus no qualitatively new source branch remains at the present level of decomposition. The unresolved issue is whether this loop can be repeated indefinitely with scale separation.

---

## 10. Sharpness audit

The adversarial power family

\[
R=W^{\delta/4},
\qquad
0<\delta<1/5,
\]

still satisfies

\[
\sum_j R_j^3/\sqrt{W_j}<\infty,
\qquad
\sum_j R_j^2/W_j<\infty
\]

for geometric `W_j`.

Thus both the terminal coherent occupancy cost and the core-parabolic durations can remain summable.

A logarithmic strain cost

\[
A_{R_j}\gtrsim\log R_j
\]

diverges across episodes, but this is not a contradiction: divergent critical/BKM strain action is allowed, and indeed expected, at a hypothetical singularity.

Therefore scalar exponent bookkeeping is exhausted.

---

## 11. Current single mathematical wall

A proof now requires a genuinely structural nonrepeatability theorem of one of the following forms:

1. **cross-scale productive-channel packing:** recurrent residual-seed or log-strain episodes cannot occupy mutually shrinking regions while remaining compatible with finite kinetic-energy dissipation;
2. **uniformly-fast resonant rigidity:** after the local near-slow packet has been depleted, the remaining near-resonant uniformly-fast interactions plus Gaussian localization commutators cannot sustain the order-one secular source;
3. **derivative nonrepeatability:** repeated thin V2/pressure-Hessian pulses force a non-summable factorial/projective derivative cost;
4. **critical residual rigidity:** the crossing-scale residual equation with bounded affine/Coriolis background admits no nontrivial ancient/recursive source-saturating limit compatible with all established ledgers.

The correct next target is not another scalar lower bound. It is a theorem that prevents the **renormalization loop itself** from recurring indefinitely.

Overall status: **RECENT SOURCE SHARPENED TO PARABOLIC CRITICAL SATURATION / EXACT SEED--AMPLIFICATION TRADEOFF / RESIDUAL SEED RECURSIVELY RETURNS TO DYNAMIC REYNOLDS CROSSING / THIN SEED RETURNS TO DERIVATIVE-PRESSURE / SMALL SEED RETURNS TO LOG STRAIN / TIGHT NEAR-SLOW FAST-ROTATION INPUT DEPLETED / GLOBAL REGULARITY NOT PROVED.**