# Compact unit cell: three-defect reduction to a nonlocal strain interaction network

Date: 2026-08-18

Status: **SYNTHESIS OF EXACT RESIDUAL-VARIANCE, ANGULAR-DAMPING, AND LOCAL BETCHOV RESULTS. AFTER PROJECTIVE AND SIGNED-LINE DEFECTS ARE CHARGED, THE DISTINCT SAME-SCALE RESIDUAL IS NON-AFFINE STRAIN VARIANCE, WHICH ON A BUFFERED SIGNED-COHERENT CELL MUST BE SUPPLIED NONLOCALLY. GLOBAL REGULARITY NOT PROVED.**

## 1. Exact three-channel residual state

On a Gaussian or comparable kernel window, the existing exact decomposition is

\[
\boxed{
B
=V_S
+\frac12D_{\rm proj}
+\frac12D_{\rm line},
}
\]

where

- `V_S` is strain variance;
- `D_proj` is energy outside the best unoriented vorticity axis;
- `D_line` is the remaining signed-line/polarity-magnitude defect.

Therefore an order-one compact residual cannot live outside these three channels.

## 2. Projective-defect channel

If

\[
D_{\rm proj}\gtrsim1
\]

on a thick natural cell, projective Poincare forces order-one angular palinstrophy.  The exact magnitude equation contains

\[
-\nu|\Omega|^2|\nabla\xi|^2
\]

and the exact terminal-adjoint identity charges the full angular loss back to required stretching.

Thus the multi-axis branch is not a free geometric source.  It is

\[
\boxed{
\text{angular viscous loss}
+\text{required nonlinear regeneration}.
}
\]

## 3. Signed-line channel

If

\[
D_{\rm proj}\ll1,
\qquad
D_{\rm line}\gtrsim1,
\]

then vorticity is near one projective axis but not near one constant signed vector.  The defect consists of polarity cancellation and/or magnitude variation along the preferred line.

On a buffered natural cell this routes through the existing Poincare/flux machinery to

\[
\boxed{
\text{magnitude gradient / palinstrophy}
\quad\lor\quad
\text{mixed polarity / flux reset}
\quad\lor\quad
\text{buffer transport}.
}
\]

It is therefore already a derivative/material-reorganization channel.

## 4. Small projective + small signed-line defect

If

\[
D_{\rm proj}+D_{\rm line}\ll1,
\]

then the exact variance identity gives

\[
\operatorname{Var}(\Omega)\ll1.
\]

Hence on the observation window

\[
\boxed{
\Omega\approx m
}
\]

in weighted `L2`, for one constant signed vector `m`.

If in addition

\[
V_S\ll1,
\]

then

\[
S\approx\bar S
\]

and the unit cell is close to a signed-coherent affine state.

A positive source in such a state is already classified pointwise:

\[
\boxed{
\lambda_2^+>0
\quad\lor\quad
\text{positive local Betchov mismatch}.
}
\]

The first is the critical middle-strain branch; the second requires buffer/Betchov compensation.

Thus a low-total-defect affine unit cell is not a new same-scale escape.

## 5. The distinct compact residual is `V_S`

After the previous channels are charged, the only distinct same-scale residual is

\[
\boxed{
V_S\gtrsim1
}
\]

while local vorticity remains approximately a constant signed vector.

Because strain is the nonlocal order-zero Biot--Savart/Riesz transform of vorticity, a buffered cell with nearly constant local vorticity cannot generate an arbitrary order-one strain fluctuation purely from its constant local core.  After a near/far decomposition, failure of the local near-field residual estimate is itself a buffer/high-frequency defect; otherwise the order-one `V_S` must be supplied by vorticity outside the coherent packet.

Therefore the distinct compact mechanism becomes

\[
\boxed{
\text{nonlocal strain import from neighboring same-scale structures}.
}
\]

## 6. Relation to the common-strain cone lemma

If that imported strain varies only on a lower/mesoscopic frequency and is common to many packets, the ensemble covariance lemma extracts a fixed signed projective-cone subpopulation.  That subpopulation returns to coherent-flux/Betchov geometry.

To avoid common-axis extraction, the imported strain must vary from packet to packet.  The responsible strain frequency is therefore forced toward the packet frequency by the existing sampling bound.

Hence the irreducible compact residual sharpens to

\[
\boxed{
\textbf{same-scale high--high nonlocal strain--vorticity correlation network}.
}
\]

## 7. Why this is a genuine critical wall

After rescaling a natural packet to unit size,

\[
|\Omega|\sim1,
\qquad
|S_{\rm imported}|\sim1,
\qquad
\nu\Delta\Omega\sim1,
\]

and the responsible frequencies are all order one.  No large or small scale parameter remains.

Therefore further closure cannot come from another power of `K` or `R`.  It must use an algebraic/geometric property of the same-scale interaction network itself, or a new global invariant/packing relation.

Status: **PROJECTIVE, POLARITY, AND AFFINE LOW-DEFECT UNIT CELLS TYPED / DISTINCT COMPACT WALL = SAME-SCALE NONLOCAL STRAIN NETWORK AMONG SIGNED-COHERENT PACKETS.**