# DSD M5-479 — Nontrivial backward blow-down forces a terminal critical tail or terminal suitable defect

Date: 2026-09-01

Status: **TERMINAL-RIGIDITY DICHOTOMY / THE NONTRIVIAL M5-478 TYPE-I ANCIENT TAIL CELL CANNOT HAVE A REGULAR ZERO TERMINAL TRACE ON EVERY PUNCTURED ANNULUS: IF VELOCITY, PRESSURE, AND LOCAL-ENERGY COMPACTNESS ALL VANISH THERE, THE STANDARD NAVIER--STOKES BACKWARD-UNIQUENESS/UNIQUE-CONTINUATION PACKAGE FORCES THE CELL TO BE ZERO, CONTRADICTING ITS OLD-CARRIER MARK / THEREFORE THE RECORD BLOW-DOWN MUST RETAIN EITHER A SCALE-CRITICAL TERMINAL VELOCITY/PRESSURE TAIL OR A TERMINAL SUITABLE-COMPACTNESS DEFECT / THE EXTERNAL BACKWARD-UNIQUENESS THEOREM IS USED ONLY AFTER ITS LOCAL TERMINAL REGULARITY HYPOTHESES ARE EXPLICITLY MET / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. M5-478 tail-cell sequence

Let

\[
V^{(m)}(y,s)=R_mV(R_my,R_m^2s),
\qquad
P^{(m)}(y,s)=R_m^2P(R_my,R_m^2s),
\]

with

\[
R_m\to\infty.
\]

On every compact subset of

\[
\mathbb R^3\times(-\infty,0)
\]

a subsequence converges to a smooth Type-I ancient solution

\[
(\mathcal V,\mathcal P),
\]

with

\[
\boxed{\mathcal\Omega\not\equiv0.}
\]

The nontriviality is marked at `s=-1` by the backward first-hitting Taylor carrier.

---

## 2. Punctured terminal annuli

Fix

\[
0<a<b<\infty.
\]

Let

\[
A_{a,b}:=\{a<|y|<b\}.
\]

At `s=0`, the rescaled fields are

\[
V^{(m)}(y,0)=R_mV(R_my,0),
\]

\[
P^{(m)}(y,0)=R_m^2P(R_my,0).
\]

Thus every fixed punctured annulus corresponds to a physical annulus of radius comparable to `R_m` in the terminal slice of the M5-474 ancient element.

---

## 3. Suitable zero-tail hypothesis

Call the selected record sequence **terminally suitable-zero** on `A_{a,b}` if, after pressure normalization by an annular average,

\[
\boxed{
\|V^{(m)}(0)\|_{L^3(A_{a,b})}
\to0,
}
\]

\[
\boxed{
\|P^{(m)}(0)-[P^{(m)}(0)]_{A_{a,b}}\|_{L^{3/2}(A_{a,b})}
\to0,
}
\]

and the sequence has the uniform local suitable bounds needed to pass to `s=0` on a slightly larger annular cylinder:

\[
\sup_m
\left[
\|V^{(m)}\|_{L^3(A'\times(-\delta,0))}
+
\|P^{(m)}-[P^{(m)}]_{A'}\|_{L^{3/2}(A'\times(-\delta,0))}
\right]
<\infty
\]

for

\[
A_{a,b}\Subset A'.
\]

This formulation deliberately includes the local pressure and space-time compactness requirements. They are not inferred from a single terminal velocity slice.

---

## 4. Terminal suitable compactness

Under the preceding bounds, standard suitable-solution compactness yields convergence through the terminal time on a smaller annulus:

\[
V^{(m)}\to\mathcal V
\quad\text{strongly in }L^3_{loc}(A_{a,b}\times[-\delta,0]),
\]

with weak pressure convergence in `L^(3/2)`.

The terminal smallness then gives

\[
\boxed{
\mathcal V(y,0)=0
\qquad	ext{for }y\in A_{a,b}.
}
\]

Consequently

\[
\boxed{
\mathcal\Omega(y,0)=0
\qquad	ext{on }A_{a,b}.
}
\]

If this suitable-zero property holds for every compact annulus in `R3 minus {0}`, then

\[
\boxed{
\mathcal\Omega(\cdot,0)=0
\quad\text{on }\mathbb R^3\setminus\{0\}.
}
\]

---

## 5. Backward uniqueness on the regular exterior

On any region

\[
|y|>\varepsilon,
\]

the terminal suitable-zero hypothesis gives a regular terminal neighborhood after the standard epsilon-regularity step.

The vorticity satisfies the linear parabolic system with lower-order coefficients generated by the smooth Navier--Stokes velocity:

\[
\partial_s\mathcal\Omega-\Delta\mathcal\Omega
=-\mathcal V\cdot\nabla\mathcal\Omega
+\mathcal\Omega\cdot\nabla\mathcal V.
\]

The classical Navier--Stokes backward-uniqueness argument of Escauriaza--Seregin--Sverak, as used in later Type-I/Liouville work, applies on the regular exterior and yields

\[
\boxed{
\mathcal\Omega=0
\quad\text{on }
(\mathbb R^3\setminus B_\varepsilon)\times(-\delta,0].
}
\]

Letting `epsilon` vary and then using spatial unique continuation/analyticity at a negative time gives

\[
\boxed{
\mathcal\Omega\equiv0
\quad\text{on }\mathbb R^3\times(-\infty,0).
}
\]

This contradicts the M5-478 old-carrier mark.

---

## 6. Exact consequence

Therefore the nontrivial tail cell cannot be terminally suitable-zero on all punctured annuli.

Hence along a subsequence there exist fixed `0<a<b<infinity` and `epsilon_*>0` such that at least one of the following survives:

### A. Critical velocity tail

\[
\boxed{
\int_{aR_m<|x|<bR_m}|V(x,0)|^3dx
\ge\epsilon_*.
}
\]

### B. Critical pressure tail

\[
\boxed{
\int_{aR_m<|x|<bR_m}
|P(x,0)-[P]_{A_m}|^{3/2}dx
\ge\epsilon_*.
}
\]

### C. Terminal suitable-compactness defect

The velocity/pressure space-time bounds required to pass regularly to `s=0` fail on the corresponding annular cylinders.

Symbolically,

\[
\boxed{
E_{ratchet}^{ancient}
\Longrightarrow
T_{L3}^{crit}
\lor
T_{p}^{crit}
\lor
H_{terminal\ suitable}^{crit}.
}
\]

---

## 7. Why this is a genuine reduction

The local marked ratchet itself was already localized to a finite-energy rotational cluster in M5-476.

M5-479 shows that a nontrivial second-generation ancient history cannot coexist with a completely negligible terminal exterior.

Thus the global obstruction must be carried by an actual **scale-critical terminal tail/defect at spatial infinity**.

This reconnects the bounded-ratchet problem with the historical W1/critical-tail side of the repository.

---

## 8. Firewall

This note does not claim that terminal `L3` tail nonvanishing alone follows without alternatives.

Pressure nonlocality and failure of terminal suitable compactness are retained explicitly as separate branches.

It also does not replace the full hypotheses of the external backward-uniqueness theorem by a weaker informal statement. The theorem is invoked only after regular zero terminal data have been obtained on the exterior.

---

## 9. Highest-value next target

Route

\[
T_p^{crit}
\quad\text{and}\quad
H_{terminal\ suitable}^{crit}
\]

back to velocity critical occupancy or strong frequency/pressure throughput.

If this succeeds, one obtains the clean terminal consequence

\[
\boxed{
\limsup_m
\int_{aR_m<|x|<bR_m}|V(x,0)|^3dx
>0,
}
\]

which can then be packed over geometrically separated record radii to produce a logarithmically occupied critical tail.

---

## 10. Status

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
