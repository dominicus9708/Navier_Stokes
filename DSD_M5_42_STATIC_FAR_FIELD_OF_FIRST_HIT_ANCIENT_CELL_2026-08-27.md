# DSD M5-42 — Static Far Field of the First-Hit Ancient Cell

Date: 2026-08-27

Status: **EXACT CANCELLATION BETWEEN W1 LOG-RADIUS TRANSPORT AND INVERSE-LERAY TIME SCALING / EVERY SELECTED ANCIENT TAIL-HULL PROFILE IS STATIC AT LEADING `1/r` ORDER / NO STATIONARY-NS EQUATION FOR THE LEADING TRACE IS CLAIMED / GLOBAL REGULARITY UNPROVED.**

## 1. W1 tail-hull transport

For a complete W1 trajectory `U^#`, define the log-radius critical amplitude

\[
F_U(\theta,\rho,\eta)
:=e^\rho U^\#(e^\rho\theta,\eta).
\]

The W1 far-field hull satisfies the exact limiting transport law

\[
\partial_\eta F_U
+\frac12\partial_\rho F_U=0.
\]

Hence every selected tail-hull element has the form

\[
\boxed{
F_U(\theta,\rho,\eta)
=\Phi(\theta,\rho-\eta/2).
}
\]

No periodicity is assumed. Periodic/DSS and aperiodic hulls differ only in the translation dynamics of `Phi` as a function of its log-radius argument.

---

## 2. Ancient-cell representation

M5-41 gives

\[
V_*(z,\sigma)
=(-\sigma)^{-1/2}
U^\#\!\left(
\frac z{\sqrt{-\sigma}},
-\log(-\sigma)
\right),
\qquad \sigma<0.
\]

Let

\[
r=|z|,
\qquad
\theta=z/r.
\]

The corresponding Leray log radius and time are

\[
\rho_Y
=\log r-\frac12\log(-\sigma),
\qquad
\eta=-\log(-\sigma).
\]

Moreover

\[
rV_*(r\theta,\sigma)
=e^{\rho_Y}U^\#(e^{\rho_Y}\theta,\eta)
=F_U(\theta,\rho_Y,\eta).
\]

---

## 3. Exact cancellation of time dependence

Insert the W1 tail-hull form:

\[
rV_*(r\theta,\sigma)
=\Phi\!\left(
\theta,
\rho_Y-\eta/2
\right).
\]

But

\[
\begin{aligned}
\rho_Y-\eta/2
&=\log r
-\frac12\log(-\sigma)
-\frac12[-\log(-\sigma)]\\
&=\log r.
\end{aligned}
\]

Therefore every selected tail-hull profile of the ancient cell satisfies

\[
\boxed{
rV_*(r\theta,\sigma)
=\Phi(\theta,\log r)
}
\]

at leading `1/r` order.

Equivalently,

\[
\boxed{
V_{tail}(z)
=\frac1{|z|}
\Phi\!\left(
\frac z{|z|},\log|z|
\right)
}
\]

is independent of ancient time `sigma`.

This conclusion does **not** require the tail to be periodic in `log r`.

---

## 4. Periodic and aperiodic cases unify

If `Phi` is periodic in its second argument, the static ancient tail is log-periodic and corresponds to the familiar DSS physical trace.

If `Phi` is aperiodic, the same formula still gives a static `1/r` spatial tail; only its log-radius pattern is aperiodic.

Thus

\[
\boxed{
\text{periodic tail}\lor\text{aperiodic tail}
\Longrightarrow
\text{static ancient leading far field}.
}
\]

The periodic/aperiodic split is therefore not a separate obstruction at the ancient-cell level.

---

## 5. Divergence-free constraint on the tail profile

Write

\[
\Phi=\Phi_r\theta+\Phi_T.
\]

The divergence-free condition for

\[
V_{tail}(z)=r^{-1}\Phi(\theta,\log r)
\]

becomes

\[
\boxed{
(\partial_\rho+1)\Phi_r
+\operatorname{div}_{S^2}\Phi_T=0.
}
\]

Thus the static tail remains subject to the same sphere Hodge constraint found in M5-8.

---

## 6. Important audit: static leading tail is not a stationary NS solution

The conclusion

\[
\partial_\sigma V_{tail}=0
\]

at leading `1/r` order does **not** imply that `V_tail` solves the stationary Navier--Stokes equation by itself.

The stationary residual generated by a `1/r` field is order `r^{-3}`. A subleading time-dependent correction of size schematically

\[
\sigma r^{-3}
\]

can have a time derivative of the same `r^{-3}` order and cancel that residual.

This is the same audit already encountered in the periodic terminal-trace branch.

Hence one must not infer a stationary-Leray or stationary-NS contradiction from the static leading trace alone.

---

## 7. DSD interpretation

The ancient cell now has a sharp interior/boundary split:

\[
\boxed{
\text{far boundary: static critical `1/r` memory}
}
\]

while

\[
\boxed{
\text{interior: time-dependent first-hit formation dynamics}.
}
\]

Thus the terminal defect is not created by a time-varying far-field amplitude pattern. The far field acts as a static critical ancestry/reservoir at leading order, while the actual first-hit increase must be carried by the finite/intermediate-scale threshold-Hodge formation mechanism derived in M5-23--40.

---

## 8. Consequence for Liouville routes

Because

\[
|V_{tail}(z)|\sim |z|^{-1},
\]

the ancient cell may remain in weak `L^3` while failing global strong `L^3` logarithmically at every negative time.

Therefore the Albritton--Barker Liouville theorem requiring bounded strong `L^3` along a backward sequence is not automatically activated.

To use that theorem one would need a genuine additional bridge, for example:

- a tail subtraction producing a strong-`L^3` ancient residual with controllable forcing;
- a strong-`L^3` backward sequence despite the static critical tail;
- or a direct Liouville theorem for this static-tail / first-hit ancient class.

None is proved here.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
