# Transverse Remote-Strain Covariance Gate — 2026-08-24

Status: **EXACT LOCALIZED COVARIANCE IDENTITY + CONDITIONAL TRANSVERSE-ACTION DICHOTOMY / GLOBAL REGULARITY NOT PROVED.**

This note addresses the transverse trace-free part of an active remote affine strain. It does **not** claim that the full `H_remote` or `T` branch is closed. Its purpose is to show that a remote symmetric trace-free action invisible to the scalar projective quantity `xi^T S xi` cannot remain simultaneously coherent, shape-invisible, and residual-free.

---

## 1. Normalized vorticity and enstrophy density

On a normalized first-hitting stage,

\[
\Omega_s+U\cdot\nabla\Omega+\frac b2y\cdot\nabla\Omega+b\Omega
=\Sigma\Omega+\nu\Delta\Omega.
\]

Set

\[
V=U+\frac b2y,
\qquad
e=\frac12|\Omega|^2.
\]

Since `div V = 3b/2`, the exact enstrophy-density equation is

\[
\boxed{
e_s+\nabla\cdot(Ve)
=\Omega^T\Sigma\Omega
-\nu|\nabla\Omega|^2
-\frac b2e
+\nu\Delta e.
}
\]

Let `chi` be a smooth compactly supported core cutoff and define

\[
\rho=\chi e.
\]

Then

\[
\boxed{
\rho_s+\nabla\cdot(V\rho)-\nu\Delta\rho=H_\chi,
}
\]

where

\[
\boxed{
H_\chi
=
\chi\left(
\Omega^T\Sigma\Omega
-\nu|\nabla\Omega|^2
-\frac b2e
\right)
+eV\cdot\nabla\chi
-\nu e\Delta\chi
-2\nu\nabla\chi\cdot\nabla e.
}
\]

Thus all cutoff/material leakage, stretching source, enstrophy dissipation, and cutoff-viscous terms are explicit in one localized source.

---

## 2. Exact centered covariance identity

Assume the localized enstrophy mass is positive:

\[
m=\int\rho\,dy>0.
\]

Define the localized centroid

\[
c=m^{-1}\int y\rho\,dy,
\qquad
z=y-c,
\]

and the normalized spatial covariance tensor

\[
\boxed{
Q=m^{-1}\int z\otimes z\,\rho\,dy.
}
\]

Also define the localized mean transport velocity

\[
\bar V=m^{-1}\int V\rho\,dy.
\]

The mass and centroid satisfy

\[
m'=\int H_\chi\,dy,
\]

\[
\boxed{
c'
=\bar V
+m^{-1}\int zH_\chi\,dy.
}
\]

The centered first moment vanishes, so all terms generated by differentiating `c(s)` cancel from the covariance numerator. Two integrations by parts give the exact tensor identity

\[
\boxed{
\begin{aligned}
Q'
&=m^{-1}\int
\Big[(V-\bar V)\otimes z
+z\otimes(V-\bar V)\Big]\rho\,dy\\
&\quad+2\nu I
+m^{-1}\int(z\otimes z-Q)H_\chi\,dy.
\end{aligned}
}
\]

The bulk viscous contribution is exactly isotropic:

\[
\boxed{Q'_{\rm visc,bulk}=2\nu I.}
\]

This fact will be decisive after transverse trace-free projection.

---

## 3. Exact affine-plus-residual split

Let `A_f(s)` be the affine velocity-gradient matrix assigned to the remote field on the tracked core. Write

\[
V-\bar V=A_fz+w,
\]

where `w` contains all local velocity, non-affine remote remainder, and mismatch between the exact localized mean and the chosen affine representation.

Then

\[
\boxed{
Q'=A_fQ+QA_f^T+2\nu I+\mathcal R_Q,
}
\]

with the exact residual

\[
\boxed{
\begin{aligned}
\mathcal R_Q
&=m^{-1}\int(w\otimes z+z\otimes w)\rho\,dy\\
&\quad+m^{-1}\int(z\otimes z-Q)H_\chi\,dy.
\end{aligned}
}
\]

No approximation has been made in this identity once `A_f` is chosen; everything not represented by the affine remote field is retained in `R_Q`.

---

## 4. Transverse decomposition relative to the core vorticity direction

Let

\[
\xi=\Omega/|\Omega|
\]

be the tracked core vorticity direction and

\[
P=I-\xi\otimes\xi.
\]

For the symmetric remote affine strain `Sigma_f = sym A_f`, use the exact decomposition

\[
\boxed{
\Sigma_f
=a\,\xi\otimes\xi
+\xi\otimes b+b\otimes\xi
-\frac a2P
+D,
}
\]

where

\[
a=\xi^T\Sigma_f\xi,
\qquad
b=P\Sigma_f\xi,
\]

and `D` is symmetric, transverse, and trace-free:

\[
D=PDP,
\qquad
\operatorname{tr}D=0.
\]

The Frobenius norm splits as

\[
\boxed{
|\Sigma_f|_F^2
=\frac32a^2+2|b|^2+|D|_F^2.
}
\]

Thus a large remote affine strain must enter at least one of the longitudinal stretch, tilt, or transverse trace-free channels.

---

## 5. Transverse shape tensor

Define

\[
Q_\perp=PQP,
\qquad
q_\perp=\frac12\operatorname{tr}(PQP),
\]

and the transverse trace-free covariance

\[
\boxed{
E_\perp
=PQP-q_\perp P.
}
\]

`E_perp=0` means that the localized enstrophy packet is second-moment isotropic inside the plane orthogonal to `xi`.

Let

\[
h=PQ\xi
\]

be the axial-transverse covariance vector.

The time dependence of `P`, the antisymmetric part of `A_f`, all local/non-affine velocity, and all localized source terms will be retained in a transverse residual `R_perp`.

Projecting the exact covariance identity onto the transverse trace-free sector gives

\[
\boxed{
E_\perp'
=2q_\perp D+\mathcal R_\perp.
}
\]

More explicitly, before collecting terms into `R_perp`, the symmetric affine contribution contains

\[
\boxed{
\operatorname{dev}_\perp
\Big(
2q_\perp D
+DE_\perp+E_\perp D
-aE_\perp
+b\otimes h+h\otimes b
\Big).
}
\]

The remaining pieces of `R_perp` are:

1. derivatives of the moving projector `P(s)`;
2. rotation/commutator terms from `skew A_f`;
3. the local and non-affine velocity moment in `R_Q`;
4. the localized source moment generated by `H_chi`;
5. the displayed lower-order couplings involving `E_perp`, `a`, `b`, and `h` when the pure transverse lane is being isolated.

The crucial cancellation is exact:

\[
\boxed{
\operatorname{dev}_\perp(2\nu I)=0.
}
\]

Therefore isotropic bulk viscosity cannot directly cancel the transverse trace-free forcing `2 q_perp D`.

---

## 6. Coherent transverse-action inequality

Assume on an interval `I=[s0,s1]` that

\[
q_\perp(s)\ge q_->0.
\]

Let

\[
A_D(I)=\int_I|D(s)|_F\,ds,
\qquad
A_{\perp,res}(I)=\int_I|\mathcal R_\perp(s)|_F\,ds.
\]

Suppose the normalized transverse-strain direction is coherent: there exists a fixed unit transverse trace-free tensor `D_hat_*` and `gamma>0` such that

\[
\boxed{
\langle D(s),\widehat D_*\rangle_F
\ge\gamma|D(s)|_F
\quad\text{for }s\in I.
}
\]

Pairing the exact shape equation with `D_hat_*` and integrating gives

\[
\boxed{
2q_-\gamma A_D(I)
\le
|E_\perp(s_1)|_F+|E_\perp(s_0)|_F
+A_{\perp,res}(I).
}
\]

Hence a fixed coherent transverse action cannot be simultaneously hidden by a shape-persistent and residual-quiet core.

If

\[
|E_\perp(s_k)|_F\le\varepsilon_\perp q_+
\qquad(k=0,1),
\]

then

\[
\boxed{
A_{\perp,res}(I)
\ge
2q_-\gamma A_D(I)-2\varepsilon_\perp q_+.
}
\]

Consequently, once

\[
q_-\gamma A_D(I)>\varepsilon_\perp q_+,
\]

at least one of the following must occur:

- order-one transverse covariance reshaping;
- order-one residual action;
- loss of transverse-direction coherence.

---

## 7. Direction turnover alternative

The vector space of symmetric trace-free `2 x 2` tensors is two-dimensional. Whenever `D != 0`, define

\[
\widehat D=D/|D|_F.
\]

If

\[
\operatorname{Var}_I(\widehat D)<\alpha<\frac\pi2,
\]

one may choose a fixed reference tensor so that

\[
\gamma\ge\cos\alpha.
\]

For example,

\[
\alpha=\frac\pi3
\quad\Longrightarrow\quad
\gamma\ge\frac12.
\]

Thus every active transverse remote-strain interval satisfies the typed alternative

\[
\boxed{
\begin{aligned}
A_D\text{ fixed positive}
\Longrightarrow\;&
T_{\rm shape}
\;\lor\;
T_{D\text{-dir}}
\;\lor\;
H/T/\text{residual action},
\end{aligned}
}
\]

where

- `T_shape` is order-one transverse covariance reshaping at essentially fixed trace;
- `T_D-dir` is order-one projective rotation of the transverse affine-strain direction;
- the residual term contains cutoff/material leakage, non-affine remote action, local compensation, axis tilt, stretching-source imbalance, or derivative/cutoff-viscous action.

This is a **classification gate**, not yet a complete finite-stage exclusion theorem for `T_shape` or `T_D-dir`.

---

## 8. Relation to the existing projective and moving-variance lanes

The existing scalar moving-ball variance controls the trace of a velocity variance and supplies a legitimate finite-stage ceiling on the pure low-turnover corridor. However, a transverse trace-free deformation can change anisotropy while preserving the scalar trace to first order.

Therefore the tensor `E_perp` is not redundant with the scalar variance ledger.

Likewise, `xi^T Sigma xi` and `P Sigma xi` do not see the example

\[
\Sigma_f=
\begin{pmatrix}
0&0&0\\
0&\lambda&0\\
0&0&-\lambda
\end{pmatrix},
\qquad
\xi=e_1.
\]

Here

\[
a=0,
\qquad
b=0,
\qquad
D=\operatorname{diag}(\lambda,-\lambda),
\]

so the transverse covariance ledger is genuinely needed to prevent this channel from disappearing from the proof tree.

---

## 9. What has and has not been closed

The exact result established here is

\[
\boxed{
E_\perp'=2q_\perp D+\mathcal R_\perp,
}
\]

with exact annihilation of isotropic bulk viscosity in the transverse trace-free sector.

Therefore a coherent active `D` branch cannot persist with all of the following simultaneously:

1. positive transverse packet size `q_perp >= q_-`;
2. small transverse shape change;
3. small residual action;
4. small projective variation of `D/|D|`.

The current proof-tree refinement is

\[
\boxed{
H_{\rm remote}^{active}
\Longrightarrow
P_{\rm stretch}
\lor P_{\rm tilt}
\lor T_{\rm shape}
\lor T_{D\text{-dir}}
\lor H/T/\text{residual}.
}
\]

The remaining task is to give `T_shape` and `T_D-dir` a finite-stage quantitative budget compatible with the already proved moving-ball variance/projective-speed bounds, or else show that their repeated occurrence forces the existing `T/H` exits.

Status: **THE PREVIOUSLY INVISIBLE TRANSVERSE TRACE-FREE REMOTE AFFINE STRAIN NOW HAS AN EXACT LOCALIZED SHAPE LEDGER. BULK VISCOSITY CANNOT HIDE IT. A FIXED COHERENT TRANSVERSE ACTION MUST PAY IN TENSOR SHAPE TURNOVER, PROJECTIVE DIRECTION TURNOVER, OR EXPLICIT RESIDUAL ACTION. GLOBAL REGULARITY REMAINS UNPROVED.**