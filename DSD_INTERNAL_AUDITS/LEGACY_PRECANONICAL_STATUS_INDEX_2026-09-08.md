# Legacy / Precanonical Status Index

Date: 2026-09-08  
Status: **PROVENANCE PRESERVATION / IMPORT FIREWALL**

This index exists because the repository root contains a large historical research trace predating the current canonical M05--M17 organization.

No historical file is deleted or moved by this cleanup. Existing links, commit history, and audit provenance are intentionally preserved.

## 1. Default status of root-level historical calculation files

Root-level historical files such as `ANCIENT_*`, `AMPLITUDE_*`, `ANTI_PROOF_*`, early genealogy/endpoint/correction notes, and similar dated documents are **historical evidence and working provenance** unless explicitly re-imported into the current canonical chain.

Their presence in `main` does not mean every historical conclusion is an active theorem in the current proof attempt.

## 2. Precanonical M5 root debt

The legacy M5-001~480 chain predates the current canonical inventory and has not been fully recertified at the same audit level as the current M05--M17 range.

Therefore its proof status is

\[
\boxed{
\text{AUDIT-QUARANTINED / ROOT-CERT MATHEMATICALLY OPEN}
}
\]

rather than silently `PASS`.

The active whole-chain dependency is

\[
\text{hypothetical singularity}
\to
\underbrace{\text{precanonical root reduction}}_{ROOT\text{-}CERT\ OPEN}
\to
\text{canonical starting survivor}.
\]

## 3. Re-import contract

A historical/precanonical result may enter the active canonical proof line only when all of the following are recorded:

1. the exact historical file/module/commit;
2. the precise lemma or estimate being imported;
3. the original hypotheses;
4. the current branch hypotheses;
5. an explicit hypothesis map;
6. current scale/generation variable identification;
7. correction/supersession check;
8. a current status label: `VALID`, `CONDITIONAL`, `OPEN`, or `SUPERSEDED`.

No broad statement such as “the old M5 chain already proved this” is sufficient.

## 4. Canonical priority

For present calculations, begin with:

- `DSD_CANONICAL_MODULES/CURRENT_FRONTIER.md`;
- `DSD_CANONICAL_MODULES/M17/CURRENT_FRONTIER.md` for the late CE-H line;
- current internal audit files in `DSD_INTERNAL_AUDITS/`.

Historical root documents are consulted only when a specific dependency needs to be reconstructed or recertified.

## 5. Known canonical branch gap

Even after entering the audited canonical range, M5-598 leaves

\[
CP\!-\!E\lor CP\!-\!S\lor CE\!-\!T\lor Migration\lor CE\!-\!H.
\]

The long M12--M17 chain follows CE-H. There is no current certificate that removes the other four branches or returns them all to CE-H.

Thus historical calculations must not be used to silently erase this branch gap.

## 6. Corrections remain historical evidence

A superseded module is not deleted. It remains useful for:

- audit provenance;
- identifying failed shortcuts;
- regression testing;
- documenting why the current statement is narrower or different.

Examples include the M17-304 generation-identification error and the M17-144--146 longitudinal `D_xi kappa` interpretation corrected by M17-313.

## 7. Repository cleanup policy

For now:

- do **not** mass-move legacy files;
- do **not** rewrite historical conclusions to look as though the correction was known at the time;
- add current indexes and explicit warnings instead;
- move files only later if a dedicated archival migration can preserve references and update all inbound links.

## 8. Proof status

This index is organizational and audit-facing. It does not close the ROOT-CERT debt or any mathematical branch.

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
