# DSD M5-149 — Flat Vorticity Reduction and Pressure Elimination

Date: 2026-08-27

Status: **P1_B REDUCTION / CURLING THE SAME-TAIL DIFFERENCE EQUATION REMOVES PRESSURE COMPLETELY; THE NATURALLY SCALED VORTICITY DIFFERENCE HAS THE SAME ONE-WAY INVERSE-FUCHSIAN PRINCIPAL NORMAL OPERATOR `4nu partial_xixi-partial_xi`; THE ONLY NONLOCAL RECOVERY IS BIOT--SAVART FROM VORTICITY TO VELOCITY, WHICH GAINS ONE SPATIAL DERIVATIVE / P1_B IS REDUCED TO FLAT VORTICITY UNIQUE CONTINUATION / GLOBAL REGULARITY UNPROVED.**

---

## 1. Leray vorticity equation

Let

\[
\Omega:=\nabla\times U.
\]

Curling the Leray equation gives

\[
\boxed{
\partial_s\Omega
-\nu\Delta\Omega
+(U\cdot\nabla)\Omega
-(\Omega\cdot\nabla)U
+\Omega
+\frac12Y\cdot\nabla\Omega
=0.
}
\]

The pressure disappears identically.

---

## 2. Same-tail vorticity difference

For two W1 states/trajectories `U,V`, define

\[
Z:=U-V,
\qquad
\Xi_{physL}:=\Omega_U-\Omega_V.
\]

Subtract the two vorticity equations.  One convenient form is

\[
\boxed{
\begin{aligned}
0={}&\partial_s\Xi_{physL}
-\nu\Delta\Xi_{physL}
+\Xi_{physL}
+\frac12Y\cdot\nabla\Xi_{physL}\\
&+(U\cdot\nabla)\Xi_{physL}
-(\Xi_{physL}\cdot\nabla)U\\
&+(Z\cdot\nabla)\Omega_V
-(\Omega_V\cdot\nabla)Z.
\end{aligned}
}
\]

Every coefficient is formed from the actual pair; no pressure variable remains.

---

## 3. Natural critical vorticity scaling

A velocity tail of order

\[
U\sim r^{-1}
\]

has vorticity scale

\[
\Omega\sim r^{-2}.
\]

Define the Fuchsian vorticity field

\[
\boxed{
\Xi_{physL}(Y,s)
=r^{-2}\Xi(z,\eta,\theta),
\qquad
z=r^{-2},
\qquad
\eta=\log r-\frac s2.
}
\]

For a general field `K` in this scaling, the passive vorticity operator satisfies exactly

\[
\boxed{
\left(
\partial_s+1+\frac12Y\cdot\nabla
\right)(r^{-2}K)
=-r^{-2}zK_z.
}
\]

Thus the same normal cancellation that produced M5-136 occurs for vorticity.

---

## 4. Flatness passes to vorticity

M5-145 gives for every finite `N`

\[
Z_H:=H^U-H^V=O(z^N)
\]

with finite derivatives on compact genealogical/angular windows.

Taking one spatial curl costs one power of `r^-1` outside the scaled coefficient but does not destroy Fuchsian flatness of the scaled field.

Hence

\[
\boxed{
\Xi(z,\eta,\theta)=O(z^N)
\qquad\forall N.
}
\]

Equivalently in inverse-Fuchsian variables `xi=z^-1`,

\[
\boxed{
\Xi(\xi,\eta,\theta)=O(\xi^{-N})
\qquad\forall N.
}
\]

---

## 5. Inverse-Fuchsian principal normal operator

Set

\[
\xi=z^{-1}=r^2.
\]

Repeating the exact M5-148 operator transformation at vorticity scaling yields the same leading normal pair

\[
\boxed{
4\nu\Xi_{\xi\xi}-\Xi_\xi.
}
\]

Genealogical/angular diffusion is multiplied by `O(xi^-1)` or `O(xi^-2)` in the normal-at-infinity formulation.

The transport/stretching terms generated by the shared critical background contain at most one `D` derivative of the flat difference.  After division by the normal `xi^2` factor, their derivative contribution is `O(xi^-1)` relative to `Xi_xi`.

Thus the vorticity flat gate has the same one-way normal principal spectrum as the velocity gate but no pressure channel.

---

## 6. Velocity recovery from vorticity

The remaining equation still contains `Z` in

\[
(Z\cdot\nabla)\Omega_V
-(\Omega_V\cdot\nabla)Z.
\]

However `Z` is divergence-free and rapidly decaying, so on each Leray-time slice it is recovered from its curl by Biot--Savart:

\[
\boxed{
Z=-\nabla\times\Delta^{-1}\Xi_{physL}
}
\]

with the usual harmless convention fixing the decaying harmonic part.

This operator has order `-1`.

Therefore, compared with the vorticity principal derivative, the nonlocal recovery is smoothing rather than derivative-losing.

All same-tail harmonic/algebraic ambiguity has already been removed by M5-145, so there is no additional curl-free decaying fiber component: a divergence-free, curl-free `L2` field on `R3` is zero.

---

## 7. Equivalence of flat-fiber closure

If one proves

\[
\boxed{
\Xi\equiv0
}
\]

for every superalgebraically decaying complete W1 relative-vorticity solution, then

\[
\nabla\times Z=0,
\qquad
\nabla\cdot Z=0,
\qquad
Z\in L^2,
\]

and therefore

\[
\boxed{Z\equiv0.}
\]

Hence P1_B can be closed entirely at the vorticity level.

---

## 8. DSD four-chain audit

### Formation — GREEN

Vorticity is formed from the actual same-tail velocity difference.  No independent vorticity solution is introduced.

### Axis — GREEN

Velocity scale `r^-1` and vorticity scale `r^-2` are distinguished; both use the same `z,eta` axes.

### Static aggregation — GREEN

Pressure is eliminated, not hidden in another scalar payer.

### Dynamics — GREEN

The stretching and transport channels remain explicit in the relative-vorticity equation.

### Cross-audit — GREEN

If vorticity vanishes, the already known `L2`/decay properties remove the residual harmonic velocity channel.

---

## 9. Updated analytic gate

The previous pressure-sensitive P1_B gate is replaced by:

\[
\boxed{
\text{Can a complete compact W1 pair support a nonzero vorticity difference}
\text{ that decays superalgebraically as }\xi=r^2\to\infty,
\text{ under a one-way viscous normal operator plus }O(\xi^{-1})\text{ critical transport/stretching?}
}
\]

This is a local-coefficient flat unique-continuation problem up to a smoothing Biot--Savart recovery.

It is strictly cleaner than the pressure formulation, but no full Carleman estimate is proved here.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]