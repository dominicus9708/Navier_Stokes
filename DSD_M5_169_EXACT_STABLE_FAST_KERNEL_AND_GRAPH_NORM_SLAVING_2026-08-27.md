# DSD M5-169 — Exact Stable Fast Kernel and Graph-Norm Slaving

Date: 2026-08-27

Status: **P1_B^S FAST COMPATIBILITY / THE M5-168 FAST EQUATION HAS AN EXACT OPERATOR INTEGRATING FACTOR; THE FLAT-SELECTED SOLUTION IS A VOLTERRA AVERAGE OVER `0<zeta<z` WITH UNITARY GENEALOGICAL TRANSPORT AND TOTAL KERNEL NORM AT MOST `z` / CONSEQUENTLY `R=z F_z` IS GRAPH-NORM SLAVED TO `L F + z N F` WITHOUT ANY SAME-NORM ANALYTIC DERIVATIVE SHORTCUT / GLOBAL REGULARITY UNPROVED.**

---

## 1. Fast equation

M5-168 gives

\[
4\nu zR_z
+\left(\frac1z+6\nu+8\nu G\right)R
=
S(z),
\]

where

\[
\boxed{S(z):=LF(z)+z\mathcal N[F(z)]}
\]

and `G` is the skew-adjoint invariant pair-flow generator.

Divide by `4 nu z`:

\[
R_z
+\left(
\frac1{4\nu z^2}
+\frac3{2z}
+\frac{2G}{z}
\right)R
=
\frac{S(z)}{4\nu z}.
\]

---

## 2. Exact operator integrating factor

Because `G` is independent of `z` and commutes with scalar functions of `z`, define

\[
\boxed{
\mathcal M(z)
:=
e^{-1/(4\nu z)}
 z^{3/2}
 z^{2G},
}
\]

where

\[
z^{2G}:=e^{2(\log z)G}.
\]

Since `G` is skew-adjoint,

\[
\boxed{\|z^{2G}f\|=\|f\|.}
\]

A direct differentiation gives

\[
\frac d{dz}\bigl(\mathcal M(z)R(z)\bigr)
=
\mathcal M(z)\frac{S(z)}{4\nu z}.
\]

---

## 3. Flat-selected stable branch

The rejected homogeneous fast mode grows like

\[
e^{1/(4\nu z)}.
\]

The flat branch satisfies

\[
\mathcal M(z)R(z)\to0
\qquad(z\downarrow0).
\]

Therefore

\[
\boxed{
R(z)
=
\int_0^z
\mathcal K(z,\zeta)S(\zeta)d\zeta,
}
\]

with

\[
\boxed{
\mathcal K(z,\zeta)
=
\frac1{4\nu\zeta}
\exp\left[
\frac1{4\nu z}-\frac1{4\nu\zeta}
\right]
\left(\frac\zeta z\right)^{3/2}
\left(\frac\zeta z\right)^{2G}.
}
\]

The genealogical factor is unitary.

---

## 4. Exact kernel-mass estimate

Hence

\[
\|\mathcal K(z,\zeta)\|
=
\frac1{4\nu\zeta}
\exp\left[
\frac1{4\nu z}-\frac1{4\nu\zeta}
\right]
\left(\frac\zeta z\right)^{3/2}.
\]

Set

\[
t:=\frac1\zeta-\frac1z
\quad(t\ge0).
\]

Then

\[
\zeta=\frac z{1+zt}
\]

and a direct substitution gives

\[
\boxed{
\int_0^z\|\mathcal K(z,\zeta)\|d\zeta
=
\frac z{4\nu}
\int_0^\infty
 e^{-t/(4\nu)}(1+zt)^{-5/2}dt.
}
\]

Therefore

\[
\boxed{
\int_0^z\|\mathcal K(z,\zeta)\|d\zeta
\le z.
}
\]

Moreover the ratio of the left side to `z` tends to one as `z->0`.

---

## 5. Basic slaving estimate

The stable branch consequently satisfies

\[
\boxed{
\|R(z)\|
\le
z\sup_{0<\zeta\le z}\|S(\zeta)\|.
}
\]

Since

\[
R=zF_z,
\]

we also have

\[
\boxed{
\|F_z(z)\|
\le
\sup_{0<\zeta\le z}
\|LF(\zeta)+\zeta\mathcal N F(\zeta)\|.
}
\]

This is a future-free `z`-Volterra form of the M5-160 stable slaving.

---

## 6. Graph-norm version

Let

\[
A:=I-4G^2-\Delta_{S^2}.
\]

The kernel is built only from scalar functions of `z` and the unitary group generated by `G`, hence it commutes with every Borel function of `G` and with the spherical spectral operator.

Therefore for every finite graph order `m` for which `S` is defined,

\[
\boxed{
\|A^{m/2}R(z)\|
\le
z\sup_{0<\zeta\le z}
\|A^{m/2}S(\zeta)\|.
}
\]

Thus the fast-normal selection does **not** create an extra cross-frequency derivative loss.

Any derivative loss comes only from the actual first-order variable coefficient operator `N`, already audited in M5-163/M5-166.

---

## 7. Relation to the slow equation

Formally the normalized kernel approaches a delta mass at `zeta=z` as `z->0`, so

\[
R\sim zS
\]

and therefore

\[
F_z=R/z\sim LF+z\mathcal N F.
\]

M5-169 does not replace this by an unjustified pointwise equality.  It records the exact Volterra average and its graph-norm bound.

This is sufficient to eliminate the possibility that the fast normal branch itself hides a higher-order inter-band transfer channel.

---

## 8. DSD audit

### Formation — GREEN

`R` is the already-defined exact fast defect `zF_z`; the kernel is obtained by exact integration.

### Axis — GREEN

Normal slaving and genealogical unitary transport are separated in the kernel.

### Static aggregation — GREEN

The kernel mass `<=z` is not converted into a same-point amplitude comparison without the explicit supremum over `0<zeta<=z`.

### Dynamics — GREEN

The explosive homogeneous branch is removed only by the previously audited flat selection.

### Cross-audit — GREEN

This strengthens M5-160 without violating M5-152: graph norms are propagated through a commuting kernel rather than estimated by amplitude in the same analytic norm.

---

## 9. Remaining M5-166 compatibility edge

The fast kernel itself is now harmless for cross-frequency transfer.

The remaining task is to convert the Volterra graph estimate into a Dirichlet-quotient inequality for `F`, using:

- monotone principal frequency damping from M5-167;
- first-order commutator control from M5-163/M5-166;
- flatness at `z=0`;
- invariant pair averaging.

If this yields bounded cross frequency, M5-154 eliminates the statistical flat branch.

---

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
