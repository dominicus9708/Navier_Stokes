# Navier–Stokes proof-challenge frontier — 2026-08-13

Status: **ACTIVE DSD-ASSISTED AMPLIFICATION / RENORMALIZATION / COMPACTNESS-RIGIDITY ROUTE — GLOBAL REGULARITY NOT PROVED**.

This file records the newest narrow frontier.  Earlier material-turnover, pruning, polarity, flux, pressure-localization, covariance, and derivative-hierarchy notes remain valid in their stated scopes, but the main route no longer needs to classify every intermediate core by material genealogy.

DSD remains a representation/aggregation/audit layer.  The incompressible Navier--Stokes equation itself is unchanged.

---

## 1. Amplification checkpoints

Choose

\[
W_j=\|\omega(t_j)\|_\infty,
\qquad
W_{j+1}=qW_j,
\qquad q>1.
\]

Let

\[
C_{j+1}\subset\{|\omega(t_{j+1})|\ge bW_{j+1}\}
\]

be the actual final dangerous core and pull it back to `t_j`.  Every pulled-back label satisfies

\[
|\omega(a,t_j)|\le W_j
\]

regardless of whether it belonged to the previous dangerous threshold set.  Thus overlap/pruning/turnover are secondary diagnostics rather than prerequisites for charging amplification.

---

## 2. Exact Cauchy I/V two-lane amplification

Restart at `t_j` and let

\[
F=D_aX,
\qquad
\zeta=F^{-1}\omega(X,t).
\]

Exactly,

\[
\partial_t\zeta=\nu F^{-1}\Delta\omega(X,t)
\]

and

\[
\boxed{
\omega(X(a,t_{j+1}),t_{j+1})
=I(a)+V(a),
}
\]

where

\[
I=F(t_{j+1})\omega(t_j)
\]

is the inviscidly transported earlier material vorticity and

\[
V=
\nu F(t_{j+1})
\int_{t_j}^{t_{j+1}}
F^{-1}\Delta\omega(X,t)dt
\]

is viscous rewriting of the Cauchy invariant.

At least half of the final core belongs to one of two lanes.

### I-lane

If

\[
|I|\ge bqW_j/2,
\]

then for

\[
z=F\omega(t_j),
\qquad e_z=z/|z|,
\]

\[
\boxed{
\int_{t_j}^{t_{j+1}}
e_z^TSe_zdt
\ge\log\frac{bq}{2}
}
\]

when `bq>2`.

### V-lane

On a thick final natural core,

\[
\boxed{
\int_{I_j}\int|\Delta\omega|^2
\gtrsim
\frac{q^{1/2}}
{\nu^2K_+^2K_-^2\sigma_j}
W_j^{3/2},
}
\]

with

\[
\sigma_j=W_j(t_{j+1}-t_j).
\]

---

## 3. Natural-window renormalization

Set

\[
\boxed{r_j=W_j^{-1/2}.}
\]

Use

\[
y=(x-x_j)/r_j,
\qquad
s=(t-t_j)/r_j^2,
\]

\[
U_j=r_ju,
\qquad
\Omega_j=r_j^2\omega.
\]

At a maximum-vorticity center,

\[
|\Omega_j(0,0)|=1.
\]

The following become scale-invariant unit-window channels:

\[
|C_j|/r_j^3,
\quad
J_j,\Pi_j,
\quad
r_j^3\int|\nabla\omega|^2,
\quad
r_j^3\iint|\Delta\omega|^2,
\quad
\int e^TSe\,dt,
\quad
F,F^{-1}.
\]

The problem is therefore converted from diverging physical units to persistence of a dimensionless channel state on fixed normalized windows.

---

## 4. Two time-noncollapse mechanisms

### Amplification time

If the normalized I-lane strain cost, V2 cost, and deformation condition number remain bounded, then

\[
\boxed{
\sigma_j\ge\sigma_*>0.
}
\]

Thus a bounded-channel amplification sequence has a nondegenerate natural-time interval.

### Local enstrophy persistence

For a fixed normalized cutoff `chi`,

\[
\frac12E_\chi'
+\nu D_\chi
=Q_\chi+T_\chi+B_\chi
=:G_\chi.
\]

If a fixed terminal local-enstrophy rise `Delta e>0` occurs and

\[
G_\chi\in L^p,
\qquad p>1,
\]

then its time width satisfies

\[
\boxed{
m
\ge
\left(
\frac{\Delta e}
{2\|G_\chi\|_{L^p}}
\right)^{p/(p-1)}.
}
\]

The natural critical bookkeeping gives `p=4/3` on the bounded source/shell branch.  Hence temporal concentration is either impossible or appears as a typed unbounded source/transport/far-strain channel.

---

## 5. Conditional compactness block

On a fixed normalized interior cylinder, ask for uniform local bounds

\[
\|U_j\|_{L_t^\infty L_x^2}
+
\|\nabla U_j\|_{L^2_{t,x}}
+
\|P_j-(P_j)_B\|_{L^{3/2}_{t,x}}
\le M,
\]

and, for vorticity compactness,

\[
\|\Omega_j\|_{L_t^\infty L_x^2}
+
\|\nabla\Omega_j\|_{L^2_{t,x}}
\le M_\omega.
\]

Together with PDE time-derivative bounds these are sufficient for standard local compactness mechanisms.  Stronger bounded V2 provides an `H2` reserve for seeking strong local `H1` vorticity compactness.

Failure of any compactness bound is retained as an explicit concentration branch.

A terminal point value is not enough for nontriviality; use a thick terminal core or a spacetime local-enstrophy lower bound.

---

## 6. Magnitude/direction palinstrophy split

Let

\[
\rho=|\omega|,
\qquad
P=\int|\nabla\omega|^2,
\]

\[
P_{\rm mag}=\int|\nabla\rho|^2,
\qquad
P_{\rm ang}=P-P_{\rm mag}\ge0.
\]

On the nonzero set,

\[
P_{\rm ang}=\int\rho^2|\nabla\xi|^2.
\]

Since scalar Sobolev acts on `rho`,

\[
\boxed{
|Q|
\le
C_*E^{3/4}(P-P_{\rm ang})^{3/4}.
}
\]

Define

\[
\eta_{\rm ang}=P_{\rm ang}/P.
\]

Then

\[
\boxed{
|Q|
\le
C_*E^{3/4}P^{3/4}
(1-\eta_{\rm ang})^{3/4}.
}
\]

Thus direction variation creates a strict dimensionless source deficit.

---

## 7. Magnitude-heterogeneity deficit

Using the enstrophy-weighted probability measure

\[
d\mu=\rho^2dx/E,
\]

define

\[
\chi_{\rm mag}
=\frac{\operatorname{Var}_\mu(\rho)}
{(\mathbb E_\mu\rho)^2}.
\]

The exact critical interpolation ratio satisfies

\[
\boxed{
\frac{\|\rho\|_3^3}
{\|\rho\|_2^{3/2}\|\rho\|_6^{3/2}}
\le
(1+\chi_{\rm mag})^{-1/2}.
}
\]

Hence

\[
\boxed{
|Q|
\le
C_*E^{3/4}P^{3/4}
(1-\eta_{\rm ang})^{3/4}
(1+\chi_{\rm mag})^{-1/2}.
}
\]

A generic critical source can be saturated only if both directional and magnitude-distribution deficits become small.

---

## 8. First compactness-rigidity obstruction

For a cutoff normalized magnitude

\[
f_j=\chi|\Omega_j|\in H_0^1(B_R),
\]

suppose

\[
f_j\to f_\infty
\quad\text{strongly in }H^1
\]

and

\[
\|f_j\|_2\ge c_0>0.
\]

If

\[
\chi_{\rm mag}[f_j]\to0,
\]

then the limiting magnitude takes only values `{0,c}` almost everywhere.  An `H_0^1` function with values in a discrete set has zero weak gradient and therefore must vanish on the connected ball, contradicting the nontrivial `L2` lower bound.

Thus

\[
\boxed{
\liminf_j\chi_{\rm mag}[f_j]>0
}
\]

on every strongly `H1`-compact nontrivial cutoff family.

This is the first actual compactness-rigidity obstruction to simultaneous critical saturation in the current route.

---

## 9. Angular-palinstrophy source/dissipation threshold

For fixed

\[
A=P_{\rm ang}>0,
\]

the ratio of the refined source bound to viscous dissipation is optimized at

\[
\boxed{P=4A.}
\]

Moreover,

\[
\boxed{
A>
\frac{27C_*^4}{256\nu^4}E^3
\Longrightarrow
Q<\nu P
}
\]

for every `P>A`.

With magnitude heterogeneity retained, the threshold is further reduced by the factor

\[
(1+\chi_{\rm mag})^{-2}.
\]

Thus a source-saturating residual state must also approach the special ratio `P/A=4` together with the other equality regimes.

---

## 10. Far strain cannot refill the gap on the bounded normalized-enstrophy branch

Define

\[
\boxed{
\mathfrak e_j
=\|\Omega_j\|_2^2
=r_j\|\omega(t_j)\|_2^2.
}
\]

If

\[
\mathfrak e_j\le M_E,
\]

then the normalized strain generated outside radius `R` satisfies

\[
\boxed{
|S_{>R}(0)|
\lesssim
R^{-3/2}M_E^{1/2},
}
\]

and

\[
\boxed{
\sup_{B_1}|S_{>R}(y)-S_{>R}(0)|
\lesssim
R^{-5/2}M_E^{1/2}.
}
\]

Hence remote strain can be made uniformly small by taking one fixed large normalized buffer radius.

If `mathfrak e_j` is unbounded, retain a separate global-enstrophy concentration branch.

For a finite buffered far constant strain `S0`, local projective covariance supplies the complementary exact bound

\[
\boxed{
\left|
\int_B\omega\cdot S_0\omega
\right|
\le
E_B|S_0|_F
\sqrt{\frac23-J_B}.
}
\]

Thus projective roughness depletes both near magnitude/Sobolev source and the common far strain coupling.

---

# 11. Current bounded-branch rigidity state

A hypothetical singular amplification sequence that keeps every tracked normalized channel bounded must now maintain all of the following simultaneously:

1. a non-sparse terminal intense core;
2. positive normalized persistence time;
3. strong enough compactness to avoid a derivative-concentration branch;
4. nontrivial local vorticity mass;
5. failure of projective coherence/anisotropic regularity gates;
6. enough I/V amplification to increase the maximum by factor `q`;
7. near-source coupling despite the angular-palinstrophy deficit;
8. interpolation coupling despite the magnitude-heterogeneity rigidity gap;
9. `P/P_ang` near the source/dissipation-optimal value `4` if the refined upper bound is near saturation;
10. far strain unable to vanish or unable to exploit the covariance gap;
11. derivative covariance activity without entering the high-order anisotropic gate.

The residual state is therefore no longer an arbitrary turbulent profile.  It must approach a highly constrained **simultaneous near-extremizer** for several mutually interacting inequalities.

---

# 12. Current narrow frontier

The next proof-producing task is now:

\[
\boxed{
\text{bounded normalized residual state class}
\Longrightarrow
\text{uniform strict source/dissipation gap}
}
\]

or else show that failure of this gap necessarily makes at least one already-typed channel unbounded.

Concretely:

1. finish constant tracking in the buffered local source inequality;
2. make the strong-`H1` vorticity compactness branch rigorous from the bounded V2 block;
3. combine time persistence with compactness to force a nontrivial time slice carrying the magnitude-interpolation rigidity gap;
4. prove that the resulting strict near-source gap cannot be compensated by the bounded far/background strain and shell terms;
5. route every failure into the unbounded normalized channel classes.

No final contradiction for arbitrary smooth data has yet been proved.

Status: **OPEN UNIFORM STRICT-GAP / COMPACTNESS-OR-CONCENTRATION EXHAUSTION**.
