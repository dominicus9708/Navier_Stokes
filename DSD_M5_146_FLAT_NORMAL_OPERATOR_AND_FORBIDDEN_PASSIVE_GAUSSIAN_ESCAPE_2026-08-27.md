# DSD M5-146 — Flat Normal Operator and Forbidden Passive Gaussian Escape

Date: 2026-08-27

Status: **P1_B NORMAL-OPERATOR AUDIT / THE PURE VISCOUS-PASSIVE FUCHSIAN NORMAL EQUATION HAS NO NONTRIVIAL BOUNDED FLAT MODE AT `z=0`; ITS SECOND FUNDAMENTAL BRANCH GROWS LIKE `exp(+1/(4nu z))` / THEREFORE A SURVIVING FLAT SAME-TAIL FIBER CANNOT BE A FREE HEAT-KERNEL-TYPE NORMAL MODE AND MUST USE PRESSURE/TRANSPORT/BACKGROUND COUPLING / FULL FLAT UNIQUENESS REMAINS OPEN / GLOBAL REGULARITY UNPROVED.**

---

## 1. Why the flat normal operator matters

M5-145 reduces every noninjective same-tail pair to a difference that is flat at the Fuchsian boundary:

\[
Z(z,\eta,\theta)=O(z^N)\qquad\forall N.
\]

Before invoking a global backward-uniqueness theorem, test whether the degenerate viscous normal direction by itself already admits familiar flat modes such as

\[
e^{-c/z}.
\]

A wrong sign here would create a false escape or false rigidity claim.

---

## 2. Passive scalar normal reduction

Take the exact M5-136 velocity equation and suppress, only for this normal-model audit,

- `eta` dependence;
- angular dependence;
- nonlinear transport;
- pressure coupling.

For one scalar component `f(z)`,

\[
D=-2z\partial_z,
\]

and

\[
(D^2-D)f
=4z^2f''+6zf'.
\]

The Fuchsian equation

\[
H_z=-\nu(D^2-D)H
\]

becomes

\[
\boxed{
4\nu z^2 f''+(1+6\nu z)f'=0.
}
\]

---

## 3. Exact solution of the normal equation

Set

\[
g=f'.
\]

Then

\[
4\nu z^2g'+(1+6\nu z)g=0,
\]

so

\[
\frac{g'}g
=-\frac{1}{4\nu z^2}-\frac{3}{2z}.
\]

Integration gives

\[
\boxed{
g(z)=Cz^{-3/2}\exp\!\left(\frac{1}{4\nu z}\right).}
\]

Thus the two local branches are

1. the constant branch;
2. a branch obtained by integrating `g`, which has essential growth `exp(+1/(4nu z))` as `z downarrow0`.

There is no nonconstant bounded branch that is flat at `z=0`.

In particular

\[
\boxed{
e^{-c/z}\text{ is not a solution of the passive viscous normal equation with the Navier--Stokes sign.}}
\]

---

## 4. Physical sign check

Since

\[
z=\frac{T_*-t}{|x-x_*|^2},
\]

the forbidden decaying ansatz would have the physical shape

\[
\exp\!\left[-c\frac{|x-x_*|^2}{T_*-t}\right].
\]

Such a profile resembles a Gaussian concentrating toward the terminal point.

The normal calculation shows that the forward viscous sign does not freely support this as a terminally flat mode.  The second fundamental normal branch instead corresponds to the opposite essential sign and is inadmissibly large at the Fuchsian boundary.

This is consistent with diffusion opposing spontaneous terminal concentration in the uncoupled normal model.

---

## 5. What a nonzero flat fiber would therefore require

A nontrivial same-tail flat difference cannot be generated by the scalar passive-normal operator alone.

It must use at least one of the terms omitted in the normal model:

\[
\boxed{
\text{pressure coupling}
\quad\text{or}\quad
\text{transport/strain by the common W1 background}
\quad\text{or their joint nonlocal effect}.
}
\]

Hence `P1_B` is not a generic defect of the degenerate coordinate transformation.  It is a genuinely coupled Navier--Stokes boundary-to-core problem.

---

## 6. DSD four-chain audit

### Formation — GREEN

The model is explicitly marked as the normal operator obtained by deleting coupling terms.  Its conclusion is not promoted to the full system.

### Axis — GREEN

The calculation isolates only the Fuchsian normal `z` axis. Genealogical and angular channels are deliberately frozen.

### Static aggregation — GREEN

The constant branch is the already formed boundary datum; the explosive branch is not counted as an admissible flat state.

### Dynamics — GREEN

The sign comes from the actual forward-viscous Leray equation, not from a reversed heat equation analogy.

### Cross-audit — GREEN

The result supports but does not prove flat uniqueness.  Any full counterexample must explicitly exploit the omitted coupled channels.

---

## 7. RED firewall

Do not argue

\[
\text{normal operator has no flat mode}
\Rightarrow
\text{full NSE Fuchsian extension is unique}.
\]

Pressure is nonlocal, transport is order-critical, and the common background may alter a Carleman/energy estimate.

Conversely, do not introduce a decaying `exp(-c/z)` heat-kernel mode as an automatic flat escape; it has the wrong sign for the uncoupled forward-viscous normal equation.

---

## 8. Next gate

The next task is a coupled flat-boundary uniqueness estimate for

\[
Z=H^V-H^W,
\]

with

\[
Z=O(z^N)\quad\forall N,
\]

using either

1. a Fuchsian/Carleman estimate that treats pressure and common-background transport at critical strength; or
2. an equivalent physical backward-uniqueness theorem compatible with the singular Type-I center.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]