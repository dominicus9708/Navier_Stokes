# DSD M5-542 — Remote dust has vanishing cumulative core action on every bounded generation

Date: 2026-09-01

Status: **DYNAMIC SPECTATOR-TAIL DECOUPLING / M5-534--535 SHOW THAT VORTICITY OUTSIDE A LARGE SIMILARITY RADIUS GENERATES UNIFORMLY VANISHING VELOCITY AND STRAIN ON ANY FIXED ACTIVE CORE / THE SUSPENSION ROOF TIMES ARE UNIFORMLY BOUNDED ABOVE, SO THESE INSTANTANEOUS ESTIMATES INTEGRATE TO VANISHING CUMULATIVE VELOCITY, AFFINE-STRAIN, PROJECTIVE-TILT, AND LOCAL-PRODUCTION EFFECT OVER EACH COMPLETE GENERATION / CONSEQUENTLY THE ADAPTIVE ENDPOINT DUST CANNOT BECOME A HIDDEN LONG-TIME PAYER OF THE POSITIVE RATCHET OR AXIAL-PRODUCTION MARKS / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Fixed active observation window

The marked hull retains its active carrier, persistent flux lineages, and dual-pair marks in a fixed normalized observation region.

Fix

\[
B_L
\]

large enough to contain the retained active core and all marked material trajectories during one normalized generation, after the already performed compact extraction.

The suspension roof satisfies

\[
\boxed{
0<\Theta_-\le \Theta(Y)\le\Theta_+<\infty
}
\]

uniformly on the hard hull.

Thus every generation occupies a uniformly bounded similarity-time interval.

---

## 2. Near/far Biot--Savart split

For a radius

\[
R>4L,
\]

split the Biot--Savart velocity into

\[
U=U_{<R}+U_{>R},
\]

where `U_(>R)` is generated by the vorticity source in `|z|>R`.

Inside `B_L`, this remote velocity is harmonic, divergence free, and curl free because its source lies outside the observation ball.

M5-534 gives the uniform estimates

\[
\boxed{
\|U_{>R}\|_{L^\infty(B_L)}
\le
C_LR^{-1/2}
E_{tail}(R)^{1/2},
}
\]

and

\[
\boxed{
\|\nabla U_{>R}\|_{L^\infty(B_L)}
+
\|\Sigma_{>R}\|_{L^\infty(B_L)}
\le
C_LR^{-3/2}
E_{tail}(R)^{1/2}.
}
\]

Here

\[
E_{tail}(R)
:=
\sup_{Y\in\widehat{\mathfrak H}}
\int_{|z|>R}|W_Y(z)|^2dz
\to0.
\]

Therefore both right sides tend to zero uniformly on the entire hull.

---

## 3. Cumulative remote velocity displacement

Take one generation interval

\[
I_Y=[\theta_0,\theta_0+\Theta(Y)].
\]

Then

\[
\int_{I_Y}
\|U_{>R}(\theta)\|_{L^\infty(B_L)}d\theta
\le
\Theta_+
C_LR^{-1/2}E_{tail}(R)^{1/2}.
\]

Hence

\[
\boxed{
\sup_Y
\int_{I_Y}
\|U_{>R}\|_{L^\infty(B_L)}d\theta
\to0
\qquad(R\to\infty).
}
\]

Thus remote dust cannot cause an order-one displacement of any active material marker during one generation.

---

## 4. Cumulative remote affine strain

Likewise,

\[
\int_{I_Y}
\|\Sigma_{>R}(\theta)\|_{L^\infty(B_L)}d\theta
\le
\Theta_+
C_LR^{-3/2}E_{tail}(R)^{1/2}.
\]

Therefore

\[
\boxed{
\sup_Y
\int_{I_Y}
\|\Sigma_{>R}\|_{L^\infty(B_L)}d\theta
\to0.
}
\]

This is exactly the cumulative affine-action quantity relevant to the M5-469 projective-ratchet audit.

A remote field whose one-generation affine action tends to zero cannot supply the fixed order-one stretching action required by the retained singular genealogy.

---

## 5. Remote projective tilt is negligible

Along any active material lineage with unit vorticity direction `xi`, the remote strain contributes

\[
\tau_{far}
:=(I-\xi\otimes\xi)\Sigma_{>R}\xi.
\]

Pointwise,

\[
|\tau_{far}|
\le
\|\Sigma_{>R}\|.
\]

Hence

\[
\boxed{
\sup_Y
\int_{I_Y}|\tau_{far}|d\theta
\to0.
}
\]

Thus the positive-density order-one projective ratchet cannot be hidden in arbitrarily remote vorticity dust.

---

## 6. Remote contribution to local axial production

On the active observation ball define

\[
Q_{far\to core}
:=
\int_{B_L}
W\cdot\Sigma_{>R}Wdy.
\]

Then

\[
|Q_{far\to core}|
\le
\|\Sigma_{>R}\|_{L^\infty(B_L)}
\int_{B_L}|W|^2dy
\le
Z_*
\|\Sigma_{>R}\|_{L^\infty(B_L)}.
\]

Consequently

\[
\boxed{
\sup_Y
\int_{I_Y}
|Q_{far\to core}|d\theta
\to0.
}
\]

So remote dust cannot pay a fixed positive fraction of the axial stretching budget on the active core.

---

## 7. Higher-order remote effects

Because `U_(>R)` is harmonic in `B_L`, differentiating its Biot--Savart kernel gives for every fixed `m>=0`

\[
\|\nabla^mU_{>R}\|_{L^\infty(B_L)}
\le
C_{L,m}
R^{-1/2-m}
E_{tail}(R)^{1/2}
\]

up to harmless improvements in the exponent for `m>=1`.

Thus every fixed-order remote harmonic coefficient has vanishing one-generation time integral.

The spectator-tail decoupling is therefore not restricted to velocity and strain.

---

## 8. Per-generation versus infinite-time audit

The conclusion is deliberately stated **per generation**.

A quantity whose action is `o_R(1)` on each bounded roof interval need not have finite integral over an infinite number of generations at one fixed `R`.

However the proof program only needs a fixed positive action threshold on each recurrent generation.

Given any retained action threshold

\[
a_0>0,
\]

choose one finite `R_core` so large that all remote cumulative errors are smaller than, for example,

\[
a_0/100
\]

on every generation.

Then the remote field cannot be responsible for the marked order-one event on any generation.

No interchange of `R->infinity` with infinite similarity time is used.

---

## 9. Dynamic spectator-tail conclusion

M5-533--534 described the endpoint tail as a vanishing-amplitude spectator in an instantaneous sense.

M5-542 strengthens this to

\[
\boxed{
\text{remote endpoint dust}
\Longrightarrow
\text{vanishing cumulative core action per generation}.
}
\]

Thus the infinite first radial moment and the failure of `L3` can persist at infinity without supplying the order-one mechanisms needed by the active singular genealogy.

---

## 10. Updated structural split

The hard recurrent state now separates into

\[
\boxed{
\text{finite-radius active recurrent core}
+
\text{adaptive endpoint spectator dust}.
}
\]

The dust may be responsible for

\[
U\notin L^3,
\]

but it cannot be the payer of

- fixed axial production;
- fixed projective ratchet action;
- recurrent dual-source formation;
- or fixed-order local affine deformation.

Those mechanisms must be realized in a finite-radius core.

---

## 11. Highest-value next target

Use the fixed positive thresholds from M5-455, M5-485, and M5-514 together with the uniform remote-error estimate to choose **one radius `R_core<infinity` valid on the whole recurrent component** such that

\[
\boxed{
\langle Q_{B_{R_core}}\rangle>0,
}
\]

and every retained dual/ratchet mark is realized inside `B_(R_core)` up to an arbitrarily small error.

This would convert the active mechanism from a global endpoint object to a genuinely finite-core recurrent system, leaving the non-`L3` tail completely outside the causal proof mechanism.

---

## 12. Status

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]