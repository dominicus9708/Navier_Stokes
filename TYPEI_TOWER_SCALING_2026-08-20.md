# Exact Type-I Scaling of the First-Hitting Tower — 2026-08-20

Overall status: **PARTIAL GLOBAL/LOCAL BRIDGE — GLOBAL REGULARITY NOT PROVED.**

This note identifies an exact scaling feature of the first-hitting tower: once stage lengths and natural-scale local profiles are uniformly controlled, the contributions from earlier geometric stages have precisely the scaling required by the Type-I local quantities. The remaining obstruction is supercritical influx into parent balls, which should be classified as turnover `T`.

---

## 1. Relative scale of an earlier stage

Fix a late stage `j` and rescale by

\[
r_j=W_j^{-1/2}.
\]

For the stage `j-m`,

\[
W_{j-m}=q^{-m}W_j
\]

and

\[
r_{j-m}=q^{m/2}r_j.
\]

Define

\[
R_m=q^{m/2}.
\]

Thus the natural spatial and temporal scales of stage `j-m`, viewed in stage-`j` variables, are

\[
\boxed{
\text{radius }R_m,
\qquad
\text{time width }R_m^2.
}
\]

---

## 2. Amplitude scaling inside the stage-j variables

A natural stage-`j-m` velocity has size `sqrt(W_{j-m})`. After division by `sqrt(W_j)`, its stage-`j` amplitude is

\[
\boxed{
|U|\sim\sqrt{\frac{W_{j-m}}{W_j}}=R_m^{-1}.
}
\]

Likewise vorticity and strain scale as

\[
\boxed{
|\Omega|,|\Sigma|\sim\frac{W_{j-m}}{W_j}=R_m^{-2}.
}
\]

Pressure scales as velocity squared, hence

\[
\boxed{|P|\sim R_m^{-2}.}
\]

These are exactly the standard Type-I backward decay rates.

---

## 3. Scale contribution to local energy A

At a time belonging to the `m`-th backward scale, a natural core has volume `~R_m^3` and velocity amplitude `~R_m^{-1}`. Therefore its kinetic energy is

\[
\int|U|^2dy
\sim
R_m^{-2}R_m^3
=R_m.
\]

Hence

\[
\boxed{
R_m^{-1}\int|U|^2dy\sim1.
}
\]

Thus the natural core itself has the exact scale needed for bounded `A(R_m)`.

The only way `A(R_m)` can become much larger is through additional kinetic energy entering the parent ball beyond the tracked natural core. This is not a scaling necessity; it is a transport/influx phenomenon.

---

## 4. Scale contribution to cubic velocity C

On one parabolic stage,

\[
|U|^3\sim R_m^{-3},
\]

while the spacetime volume is

\[
R_m^3R_m^2=R_m^5.
\]

Therefore

\[
\int|U|^3dyd\tau
\sim R_m^2
\]

and

\[
\boxed{
R_m^{-2}\int|U|^3dyd\tau\sim1.
}
\]

Again this is exactly Type-I scaling.

---

## 5. Scale contribution to dissipation E

Since

\[
|\nabla U|\sim R_m^{-2},
\]

one stage contributes

\[
\int|\nabla U|^2dyd\tau
\sim
R_m^{-4}R_m^5
=R_m.
\]

Hence

\[
\boxed{
R_m^{-1}\int|\nabla U|^2dyd\tau\sim1.
}
\]

If the no-`H` hypothesis gives a uniform natural-scale profile bound at every first-hitting stage, each backward annulus has bounded Type-I dissipation contribution.

Moreover, because `R_l=q^(l/2)` is geometric,

\[
\sum_{l=0}^{m}R_l\lesssim R_m.
\]

Therefore the sum of all later-stage dissipation packets inside `Q(R_m)` also satisfies

\[
\boxed{E(R_m)\lesssim1}
\]

provided no additional non-core derivative mass enters the parent region.

Such additional derivative mass is precisely an `H`/multicore-transport defect rather than part of the compact `P_V` tower.

---

## 6. Scale contribution to pressure D

With pressure amplitude `~R_m^-2`,

\[
|P|^{3/2}\sim R_m^{-3}.
\]

Multiplication by spacetime volume `R_m^5` gives

\[
\int|P|^{3/2}dyd\tau\sim R_m^2.
\]

Thus

\[
\boxed{
R_m^{-2}\int|P|^{3/2}dyd\tau\sim1.
}
\]

The near-pressure part therefore has exactly Type-I scaling when generated by the controlled natural core. The remaining issue is the remote/affine pressure gauge, already isolated in the compactness checklist.

---

## 7. Geometric summation over all backward stages

For the quantities integrated over time, the geometric weights are

\[
E:\ R_l,
\qquad
C,D:\ R_l^2.
\]

Because `R_l` grows geometrically,

\[
\sum_{l=0}^{m}R_l\lesssim R_m,
\]

\[
\sum_{l=0}^{m}R_l^2\lesssim R_m^2.
\]

Therefore a tower made only of uniformly controlled natural-scale cores automatically has bounded `C,D,E` at every expanding Type-I radius.

This is stronger than merely having a bounded unit core at the final stage: it uses the entire geometric first-hitting hierarchy.

---

## 8. The remaining A obstruction is influx, hence T

Unlike the time-integrated quantities, `A(R_m)` contains a supremum in time over the full parent ball `B_{R_m}`. Later, smaller cores alone contribute at most their natural energy `~R_l`, which is no larger than `R_m`. Thus they do not violate Type-I scaling.

A violation

\[
A(R_m)\to\infty
\]

requires kinetic energy in `B_{R_m}` substantially exceeding the natural `O(R_m)` scale.

Relative to the tracked compact core, this can occur only through one of:

1. large material/kinetic-energy influx through the parent shell;
2. accumulation of many active cores inside the parent ball;
3. a low-frequency/mean drift component not removed by the coherent frame.

The first two are `T` by construction. The third is the remaining drift-gauge issue and should be separated from true internal energy.

Thus the compactness program should redefine/strengthen `T` so that **super-Type-I parent-ball energy influx or multicore accumulation is explicitly a turnover event**.

---

## 9. Consequence for the Type-I bridge

Subject to:

- no-`T` center nesting and no supercritical parent-ball influx;
- no-`H` natural-scale derivative profile control;
- coherent drift/pressure gauge;

all four Type-I quantities `A,C,D,E` inherit uniform bounds from the geometric first-hitting tower.

The key new point is that the Type-I scaling is not an external assumption on the tower: it is already encoded by

\[
W_{j-m}/W_j=R_m^{-2}.
\]

The main remaining work is therefore to convert the qualitative labels `H` and `T` into quantitative definitions strong enough that failure of the uniform `A,D,E` bounds is automatically routed into those branches.

Status: **FIRST-HITTING TOWER HAS EXACT TYPE-I SCALING. C,D,E SUM GEOMETRICALLY UNDER NATURAL-SCALE CONTROL. THE MAIN REMAINING TYPE-I COMPACTNESS OBSTRUCTION IS SUPERCRITICAL PARENT-BALL INFLUX/MULTICORE ACCUMULATION (T) PLUS THE DRIFT/PRESSURE GAUGE.**