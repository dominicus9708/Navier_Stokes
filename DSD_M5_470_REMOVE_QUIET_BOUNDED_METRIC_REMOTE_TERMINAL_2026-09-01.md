# DSD M5-470 — Quiet bounded-metric remote payer is not an independent terminal

Date: 2026-09-01

Status: **MASTER ROUTING CONSEQUENCE OF M5-469 / IF A REMOTE OUTER SOURCE SUPPLIES A FIXED POSITIVE FRACTION OF FIRST-HITTING STRETCHING ON POSITIVE GENERATION DENSITY WHILE THE AFFINE PROPAGATOR REMAINS UNIFORMLY BOUNDED, THEN LOCAL PROJECTIVE/DIFFUSIVE/REFORMATION ACTION MUST GROW LINEARLY WITH THE NUMBER OF GENERATIONS / HENCE THE REMOTE BOUNDED-METRIC LANE CANNOT REMAIN QUIET / IF THE REMOTE FRACTION VANISHES, THE PAYER RETURNS TO THE LOCAL SOURCE NETWORK / THE TIME-DEPENDENT METRIC ANCIENT LIOUVILLE PROGRAM IS THEREFORE AUXILIARY RATHER THAN A REQUIRED MAIN-TREE CLOSURE FOR THIS REMOTE BRANCH / GLOBAL REGULARITY REMAINS UNPROVED.**

## 1. Fixed-fraction remote payer

Let `gamma_A=e^T A e` denote the longitudinal stretching supplied by the remote harmonic/affine source. The remote branch means that on a positive-density collection of late first-hitting stages,

\[
\boxed{
\int_{I_j}\gamma_A^+(t)dt
\ge a_0>0.
}
\]

Over `N` such stages in a long block,

\[
\boxed{
\int_{block}\gamma_A^+dt
\ge a_0N.
}
\]

Up to the already typed sign/cancellation exit, the retained productive remote source gives a comparable positive signed action in the projective-ratchet estimate.

## 2. Bounded deformation plus quiet projective dynamics is impossible

M5-469 gives

\[
\int_{block}e^TAe\,dt
\le
2\log K_F
+C K_F^4
\int_{block}|r|dt,
\]

where `r` is the deviation from the pure outer projective flow.

Suppose the branch is quiet in the sense that it avoids

- local tilt action;
- directional metric diffusion;
- material/flux replacement or projective reformation.

Then the cumulative control action is sublinear in the number of productive remote stages:

\[
\int_{block}|r|dt=o(N).
\]

Therefore

\[
\int e^TAe\,dt=o(N)+O(1),
\]

contradicting the fixed positive remote action `>=cN` for arbitrarily long blocks.

Thus

\[
\boxed{
\text{bounded }F
+\text{positive-density fixed-fraction remote payer}
\Longrightarrow
H_{projective/directional\ diffusion}^{crit}
\lor
T_{flux\ reformation}.
}
\]

M5-456 and M5-397 route the reformation lane back into the existing critical flux/direction/remote throughput ledger.

## 3. Vanishing remote fraction returns to the local branch

The alternative is that the fraction of first-hitting stretching supplied by the selected remote affine source tends to zero along every late block.

But the total first-hitting stretching action remains bounded below by `log q` per generation. Therefore the missing action must be supplied by

\[
\boxed{
\text{local natural source}
\lor
\text{another remote/multiscale source}.
}
\]

The first is the M5-394 dual-source/local critical branch. The second restarts the remote selection at a source that carries a non-negligible fraction, unless the action is genuinely diffuse across infinitely many scales; that is already the multiscale critical-throughput branch.

Thus there is no additional quiet bounded-metric endpoint created by letting one selected remote source fade out.

## 4. Consequence for M5-459--468

The metric weak-`L3`/Liouville transfer program remains mathematically useful:

- it describes the exact bounded-metric ancient equation;
- it may produce an independent theorem extending standard critical theory;
- it clarifies which parts of Albritton--Barker are stable under metric variation.

However, for the **main proof tree generated by a fixed-fraction remote payer**, M5-469 already forces exit to strong local critical action before a quiet metric W1 terminal is reached.

Therefore do not make completion of the metric Albritton--Barker transfer a prerequisite for the current main routing.

## 5. Updated remote branch

The remote branch is now

\[
\boxed{
H_{remote}
\Longrightarrow
H_{Euler\text{-}enstrophy/multiscale}^{strong}
\lor
H_{local\ projective/diffusive}^{strong}
\lor
T_{flux\ reformation}.
}
\]

After the existing reformation routing,

\[
\boxed{
H_{remote}
\Longrightarrow
H_{crit}^{strong}.
}
\]

This is a classification, not a contradiction: strong critical throughput remains compatible with hypothetical blowup.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]