# M19-223 — The canonical adjoint covector from the isometric scattering metric is parallel and produces only a constant norm pairing

**Date:** 2026-09-14  
**Status:** ACTIVE CALCULATION / ADJOINT-SECTION REDUCTION + METRIC-PAIRING NO-GO

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Why test an adjoint section

M19-219--221 reduce a zero-q kernel candidate to an invariant hard section

\[
s(Y):=s_B(Y)
\]

with constant scattering label \(B\), satisfying

\[
\Phi_t(Y)s(Y)=s(\sigma_tY).
\]

M19-222 shows that the direct vorticity cross-enstrophy pairing with the background is sign-indefinite.

A standard Fredholm alternative is to pair the kernel mode with a canonical adjoint cokernel mode. We now test the adjoint covector supplied by the exact isometric hard metric.

## 2. Invariant hard metric

M19-133 gives the scattering-pullback norm

\[
\|v\|_{sc,Y}=\|D\mathscr S_Yv\|_{X_{sc}}
\]

and exact fiberwise isometry

\[
\boxed{
\|\Phi_t(Y)v\|_{sc,\sigma_tY}
=
\|v\|_{sc,Y}.
}
\]

On the finite-dimensional complete hard bundle choose an equivalent invariant inner product

\[
G_Y(\cdot,\cdot)
\]

compatible with this isometric structure. Then

\[
\boxed{
G_{\sigma_tY}(\Phi_t(Y)v,\Phi_t(Y)w)
=G_Y(v,w).
}
\]

## 3. Canonical dual covector of the zero-q section

Define

\[
\boxed{
\ell_Y(w)
:=
G_Y(s(Y),w).
}
\]

For every hard vector \(w\),

\[
\begin{aligned}
\ell_{\sigma_tY}(\Phi_t(Y)w)
&=
G_{\sigma_tY}(s(\sigma_tY),\Phi_t(Y)w)\\
&=
G_{\sigma_tY}(\Phi_t(Y)s(Y),\Phi_t(Y)w)\\
&=
G_Y(s(Y),w)\\
&=
\ell_Y(w).
\end{aligned}
\]

Hence

\[
\boxed{
\ell_{\sigma_tY}\circ\Phi_t(Y)
=
\ell_Y.
}
\]

Thus \(\ell\) is an exact invariant adjoint/dual section.

## 4. The self-pairing is only a conserved norm

Pair \(\ell_Y\) with its own primal section:

\[
\ell_Y(s(Y))
=
G_Y(s(Y),s(Y))
=
\|s(Y)\|_{G_Y}^2.
\]

Using invariance,

\[
\boxed{
\ell_{\sigma_tY}(s(\sigma_tY))
=
\ell_Y(s(Y))
=\text{constant}>0
}
\]

for a nonzero section.

This is simply conservation of the hard isometric norm. It carries no contradiction and no sign-changing flux.

## 5. Relative-periodic Fredholm form

For a bounded-period relative orbit, let \(P_{rel}\) be the symmetry-quotient relative return and suppose

\[
P_{rel}s=s.
\]

Because the return is orthogonal in \(G\),

\[
P_{rel}^{*G}=P_{rel}^{-1}.
\]

Therefore the metric dual \(\ell=G(s,\cdot)\) satisfies

\[
P_{rel}^{*}\ell=\ell.
\]

This recovers the M19-211 identity

\[
\ker(I-P_{rel})
=
\ker(I-P_{rel})^*
\]

in covector form.

The existence of an adjoint witness is automatic once a kernel exists; it does not exclude the kernel.

## 6. Parameter derivative reproduces the M19-212 skew no-go

For a \(C^1\) family \(P(\lambda)\in SO(N,G)\), define

\[
F(\lambda)=I-P(\lambda).
\]

At a fixed vector \(s\in\ker F(\lambda_0)\), the metric adjoint covector is \(\ell=G(s,\cdot)\).

Orthogonality implies

\[
A=P^*\dot P
\]

is \(G\)-skew. Consequently

\[
\boxed{
\ell(\dot F s)
=-G(s,\dot P s)
=0.
}
\]

Thus the canonical metric adjoint reduces exactly to the scalar same-vector Melnikov pairing already ruled out in M19-212.

## 7. General consequence

There is therefore a structural distinction between two types of adjoint objects:

1. **metric adjoint generated by the kernel section itself** — automatic, parallel, and non-obstructive;
2. **external PDE adjoint functional** — potentially useful only if it is not reducible to the invariant hard metric and if the PDE supplies a nonzero signed or topological pairing.

Hence

\[
\boxed{
\text{kernel-induced canonical adjoint covector}
\neq
\text{Fredholm kernel-exclusion mechanism}.
}
\]

## 8. Relation to M19-222

M19-222 showed that a natural background-dependent physical pairing is merely the tangent of the unsigned enstrophy balance.

M19-223 now shows that the opposite extreme, the intrinsic metric dual of the kernel mode, is merely the conserved isometric norm.

Thus two obvious pairing constructions are exhausted:

\[
\boxed{
\begin{array}{c}
\text{background norm variation}\to\text{sign-indefinite},\\
\text{kernel metric dual}\to\text{tautologically constant}.
\end{array}
}
\]

## 9. Revised pairing frontier

Any successful signed/Fredholm pairing must therefore involve a genuinely distinct adjoint object selected by the linearized Navier--Stokes operator or by a nontrivial conserved/current structure.

It must not be obtainable simply by:

- differentiating a positive scalar norm of the background; or
- applying the invariant hard metric to the kernel vector itself.

Possible remaining classes include a PDE adjoint solution fixed by a boundary/current normalization, a nonlocal stress/helicity functional surviving the earlier local-moment firewalls, or a profile-specific cokernel functional with an externally fixed orientation.

## 10. Firewall

This module does not prove that no useful adjoint functional exists. It eliminates only the canonical metric-dual choice.

The zero-q invariant-section theorem remains OPEN.

---

\[
\boxed{
\text{M19-223: THE ADJOINT MUST COME FROM NEW PDE STRUCTURE, NOT FROM THE ISOMETRIC HARD METRIC ITSELF.}
}
\]
