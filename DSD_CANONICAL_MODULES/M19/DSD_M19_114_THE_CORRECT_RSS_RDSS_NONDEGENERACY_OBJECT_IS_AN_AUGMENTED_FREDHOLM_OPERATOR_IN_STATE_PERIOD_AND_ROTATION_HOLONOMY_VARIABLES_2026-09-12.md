# DSD M19-114 — The correct RSS/RDSS nondegeneracy object is an augmented Fredholm operator in state, period, and rotation-holonomy variables

Date: 2026-09-12

Status: **ACTIVE M19 CALCULATION / CORRECTED PERIODIC-ORBIT LINEARIZATION AFTER M19-113 / INTRINSIC PERIOD AND ROTATION DRIFT ARE INCLUDED AS UNKNOWNS WITH PHASE/GROUP GAUGES / THE RESULTING AUGMENTED JACOBIAN IS THE RIGHT FREDHOLM OBJECT FOR ISOLATED RSS/RDSS ORBITS / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Relative-periodic equation

Let

\[
\Phi_S
\]

be the similarity Navier--Stokes flow map over time `S`.

An RDSS orbit satisfies

\[
\boxed{
Q_*^{-1}\Phi_S(U_0)-U_0=0,
}
\]

where both

\[
S>0,
\qquad
Q_*\in SO(3)
\]

are unknown moduli selected by the orbit.

Write

\[
Q_*=\exp(B_*),
\qquad
B_*\in\mathfrak{so}(3)
\]

locally in a chosen group chart.

---

## 2. Symmetry gauges

The fixed-point equation has the usual neutral directions generated by:

- similarity-time phase;
- spatial rotation along the group orbit.

Choose a phase condition

\[
\mathcal G_0(U_0)=0
\]

and rotation gauge conditions

\[
\mathcal G_a(U_0)=0,
\qquad a=1,\ldots,d_{rot},
\]

where `d_rot` is the dimension of the nontrivial rotation orbit after isotropy is removed.

The unknowns are therefore

\[
\boxed{
(U_0,S,B_*)
}
\]

and the equations are the relative-periodic fixed-point equation plus the phase/group gauges.

---

## 3. Linearization of the relative-periodic equation

Let

\[
\mathcal M_S^{tw}
=Q_*^{-1}D\Phi_S(U_0)
\]

be the twisted monodromy.

Vary the state, period, and rotation holonomy:

\[
\delta U,
\qquad
\delta S,
\qquad
\delta B\in\mathfrak{so}(3).
\]

The first variation of

\[
Q_*^{-1}\Phi_S(U_0)-U_0
\]

has the schematic exact structure

\[
\boxed{
(\mathcal M_S^{tw}-I)\delta U
+X(U_0)\,\delta S
-\mathcal J_{\delta B}U_0
}
\]

up to the fixed rotation representation convention, where

\[
X(U_0)=\partial_sU|_{s=0}
\]

is the similarity-flow tangent.

Thus the time tangent and rotation tangents are paired with variations of the intrinsic period and holonomy parameters rather than being discarded by hand.

---

## 4. Augmented Jacobian

The correct linearized operator is therefore the block map

\[
\boxed{
\mathbb D_{aug}
:
(\delta U,\delta S,\delta B)
\mapsto
\begin{pmatrix}
(\mathcal M_S^{tw}-I)\delta U
+X\delta S
-\mathcal J_{\delta B}U_0\\
D\mathcal G_0(U_0)\delta U\\
D\mathcal G_{rot}(U_0)\delta U
\end{pmatrix}.
}
\]

The phase and group rows remove the exact symmetry degeneracies, while the `delta S` and `delta B` columns restore the corresponding missing directions in the range.

This is the standard square augmented formulation for an isolated relative periodic orbit.

---

## 5. Fredholm index

M19-109 gives

\[
r_{ess}(\mathcal M_S^{tw})<1.
\]

Hence

\[
\mathcal M_S^{tw}-I
\]

is Fredholm of index zero modulo its finite-dimensional unit-multiplier eigenspace.

Adding finitely many symmetry-parameter columns and the matching number of gauge rows preserves the Fredholm index.

Therefore

\[
\boxed{
\mathbb D_{aug}
\text{ is Fredholm of index }0
}
\]

on the retained smooth corridor.

---

## 6. Correct nondegeneracy notion

An RSS/RDSS orbit is **augmented nondegenerate** when

\[
\boxed{
\ker\mathbb D_{aug}=\{0\}.
}
\]

This means that all unit Floquet directions are exhausted by the exact time/rotation symmetries already paired with `delta S` and `delta B`.

Equivalently, there is no additional symmetry-transverse unit multiplier producing an independent kernel after augmentation.

If `D_aug` is invertible, the orbit is locally isolated modulo its intrinsic phase and group representation.

This is exactly what one expects for a hyperbolic periodic/relative-periodic orbit of an autonomous flow.

---

## 7. Relation to the extra-center theorem

For aperiodic recurrent dynamics, the live theorem is

\[
E^c_{extra}=0.
\]

For an RSS/RDSS orbit, the periodic analogue is

\[
\boxed{
\ker\mathbb D_{aug}=0.
}
\]

They are closely related spectral statements, but their consequences differ:

- `E_extra^c=0` can reduce a recurrent quotient component toward one-dimensional flow;
- `ker D_aug=0` only says a given RSS/RDSS orbit is isolated modulo symmetry.

It does **not** imply that the isolated orbit does not exist.

---

## 8. Corrected proof architecture

The periodic branch must now be attacked by one of:

1. a genuine nonlinear Liouville identity excluding the isolated orbit;
2. an external theorem such as the Pineau--Vicol extreme-parameter results;
3. a valid global degree argument with a true external parameter and certified compactness;
4. another invariant/monotonicity principle.

The extra-center theorem alone is not sufficient.

---

## 9. Permanent firewall

\[
\boxed{
\text{augmented nondegeneracy of an RSS/RDSS orbit}
\neq
\text{nonexistence of that orbit}.
}
\]

This replaces the overstrong continuation shortcut corrected in M19-113.

---

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
