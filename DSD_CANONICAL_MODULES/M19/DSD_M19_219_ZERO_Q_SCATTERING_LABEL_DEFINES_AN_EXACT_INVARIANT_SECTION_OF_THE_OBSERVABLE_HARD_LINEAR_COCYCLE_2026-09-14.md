# M19-219 — A zero-q scattering label defines an exact invariant section of the observable hard linear cocycle

**Date:** 2026-09-14  
**Status:** ACTIVE CALCULATION / ZERO-Q PARALLEL-SECTION REDUCTION

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Setup

Let \(U\) lie in the retained compact recurrent hard corridor and let

\[
v\in H_{hard}(U)
\]

be a nonzero hard tangent with scattering signature

\[
B:=D\mathscr S_Uv.
\]

Assume the zero-q condition

\[
\boxed{\partial_qB=0.}
\]

For the bounded-period kernel sector one also imposes the holonomy-fixed condition

\[
Q_*^{-1}B=B.
\]

M19-218 shows that the local tail expansion does not eliminate such a \(B\). We now use exact scattering covariance instead.

## 2. Differentiated scattering covariance

Let

\[
U_t:=\sigma_tU,
\qquad
v_t:=\Phi_t(U)v,
\]

where \(\Phi_t(U)\) is the linearized Navier--Stokes cocycle.

M19-132--133 give the exact identity

\[
\boxed{
D\mathscr S_{U_t}v_t
=
T_{-t/2}B.
}
\]

Since \(B\) is independent of \(q\),

\[
T_{-t/2}B=B
\qquad\forall t.
\]

Therefore

\[
\boxed{
D\mathscr S_{U_t}v_t=B
\qquad\forall t.
}
\]

Thus a zero-q hard mode carries an exactly constant scattering label along the full linearized orbit.

## 3. Uniform observability makes the label unique

On the certified complete hard bundle M19-131--133 give

\[
c_{sc}\|w\|_H
\le
\|D\mathscr S_Yw\|_{X_{sc}}
\le
C_{sc}\|w\|_H.
\]

Hence

\[
D\mathscr S_Y|_{H_{hard}(Y)}
\]

is injective for every retained base state \(Y\).

Consequently, whenever the label \(B\) is realized in the hard fiber over \(Y\), there is at most one hard vector satisfying

\[
D\mathscr S_Yw=B.
\]

Write that vector as

\[
\boxed{
s_B(Y):=(D\mathscr S_Y|_{H_{hard}(Y)})^{-1}B
}
\]

on the realized domain of the label.

## 4. Extension from the orbit to its compact hull

Let

\[
\mathcal H(U):=\overline{\{\sigma_tU:t\in\mathbb R\}}
\]

be the retained compact hull.

Take a sequence \(t_n\) with

\[
U_{t_n}\to Y\in\mathcal H(U).
\]

Exact scattering isometry gives

\[
\|v_{t_n}\|_{sc,U_{t_n}}=\|v\|_{sc,U},
\]

and uniform norm equivalence bounds \(v_{t_n}\) in the finite-dimensional hard bundle.

Under the same continuous-bundle/continuous-scattering-identification gate already used in M19-202, M19-216, pass to a convergent subsequence

\[
v_{t_{n_k}}\to w\in H_{hard}(Y).
\]

Continuity gives

\[
D\mathscr S_Yw=B.
\]

Injectivity makes \(w\) unique. Hence every convergent subsequence has the same limit, so the whole fiber limit is unique and defines

\[
\boxed{s_B(Y).}
\]

Therefore the zero-q label extends from one orbit to a continuous hard section over its compact hull, unless the retained hard-bundle identification itself fails. Such failure is an already typed compactness/bundle exit.

## 5. Exact invariance of the section

For every retained \(Y\in\mathcal H(U)\), differentiated covariance gives

\[
D\mathscr S_{\sigma_tY}
\bigl(\Phi_t(Y)s_B(Y)\bigr)
=
T_{-t/2}B
=B.
\]

By uniqueness in the target hard fiber,

\[
\boxed{
\Phi_t(Y)s_B(Y)
=
s_B(\sigma_tY).
}
\]

Thus \(s_B\) is an exact invariant/parallel section of the observable hard linear cocycle.

This is stronger than merely saying that \(v\) has zero Lyapunov exponent. The entire vector is transported coherently through the changing fibers while retaining the same scattering label.

## 6. Periodic/RDSS compatibility

For an RDSS orbit with

\[
U(s+S)=Q_*U(s),
\qquad L=S/2,
\]

a fixed-moduli kernel additionally satisfies

\[
Q_*^{-1}\Phi_S(U)v=v.
\]

On scattering data,

\[
B(q+L)=Q_*^{-1}B(q).
\]

If \(B\) is zero-q, this becomes

\[
\boxed{Q_*^{-1}B=B,}
\]

exactly the holonomy-fixed condition of M19-217.

Hence a zero-q RDSS kernel is equivalently a holonomy-compatible invariant hard section with constant scattering label.

## 7. Revised theorem target

The missing zero-q theorem can therefore be restated as

\[
\boxed{
\mathcal T_{q0}^{section}:
\text{every holonomy-compatible invariant hard section with constant scattering label is generated by an exact symmetry.}
}
\]

After the complete time/rotation symmetry quotient, this becomes

\[
\boxed{
\mathcal T_{q0}^{section}:
 s_B\equiv0.
}
\]

On the certified observable hard bundle this is equivalent to

\[
E_{q0}^{nsym}=\{0\}.
\]

## 8. Why this is a genuine sharpening

M19-217 formulated the zero-q problem as a linearized scattering-rigidity statement.

M19-219 shows that one does not need to classify arbitrary linearized solutions with q-invariant tails. The only dangerous objects are much more structured:

\[
\boxed{
\text{constant scattering label}
\Longrightarrow
\text{continuous invariant hard section over the recurrent hull}.
}
\]

Thus the remaining problem is a **parallel-section rigidity theorem** for a finite-dimensional isometric Navier--Stokes cocycle.

## 9. Integrability firewall

An invariant tangent section is not automatically a second nonlinear flow on the hull.

To integrate \(s_B\) into a commuting one-parameter family of nonlinear solutions one would additionally need a suitable invariant manifold/tangency theorem and local integrability of the section.

Therefore

\[
\boxed{
\text{invariant linear section}
\neq
\text{nonlinear commuting symmetry}
}
\]

without an extra theorem.

The present reduction does not assume such integrability.

## 10. Next target

The remaining zero-q section also satisfies the physical rotation-holonomy selection rule from M19-214. The next calculation should classify the angular content of a q-invariant holonomy-fixed label and determine how much of the section survives purely kinematic constraints before a new PDE rigidity theorem is required.

---

\[
\boxed{
\text{M19-219: ZERO-Q KERNEL RIGIDITY IS A FINITE-DIMENSIONAL INVARIANT-SECTION RIGIDITY PROBLEM.}
}
\]
