# M18-044 — Stage clock bounds are derived, and variance-floor loss is a paid turnover event

**Date:** 2026-09-11  
**Status:** AUTHORITATIVE RECURRENCE-CLOCK AUDIT / STAGE-LENGTH DEGENERATION REMOVED AS PRIMITIVE ROOT / PAID TURNOVER REDUCTION

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Purpose

M18-043 left

\[
G_{stage\text{-}length\ degeneration}
\]

as one of the remaining upstream complements that could obstruct the first-hitting ratchet and second-generation ancestry construction.

A direct audit of the older first-hitting clock and moving-ball variance modules shows that the two stage-time bounds

\[
0<L_-\le L_j\le L_+<\infty
\]

must not be treated as two free recurrence hypotheses.

The lower bound is generated by first-hitting amplification itself, and the upper bound is generated on the pure low-turnover lane by the compensated moving-ball variance ledger. Failure of the upper-bound mechanism is already a quantitatively paid turnover branch.

This module also removes a remaining normalization loophole in that turnover price: the reference compensated variance cannot quietly collapse to zero on the compact first-hitting core without itself paying fixed action.

## 2. The lower stage length is not primitive

Across one first-hitting stage,

\[
W_{j+1}=qW_j,
\qquad q>1.
\]

The standard vorticity-maximum inequality gives

\[
\frac{d}{dt}\log \overline W(t)
\le \|S(t)\|_\infty
\]

at differentiability times and hence

\[
\boxed{
\log q
\le
\int_{t_j}^{t_{j+1}}\|S(t)\|_\infty\,dt.
}
\]

Define

\[
L_j
:=
\int_{t_j}^{t_{j+1}}\overline W(t)\,dt.
\]

On the bounded normalized-strain corridor,

\[
\|S(t)\|_\infty
\le B_+\overline W(t),
\]

so

\[
\boxed{
L_j\ge \frac{\log q}{B_+}.
}
\]

Thus

\[
\boxed{L_-\text{ is generated by amplification + bounded strain.}}
\]

It is not an independent recurrence parameter.

If the normalized strain ceiling fails, the trajectory has already left the compact first-hitting survivor toward a previously typed high-strain/frequency/remote branch.

## 3. The upper stage length is also not primitive on the pure lane

The older tightness-radius variance module evaluates the moving-ball variance identity on the same radius \(R_Z\) used for normalized-enstrophy tightness.

After the later absolute-action correction, introduce the scale-compensated variance

\[
\mathcal W(s)=e^{-A(s)}V(s)
\]

and the weighted viscous and boundary actions

\[
\mathscr D_J
:=
\nu\int_JE(s)\,ds,
\qquad
\mathscr B_J
:=
\int_J|F_w(s)|\,ds.
\]

The exact identity is

\[
\boxed{
\frac12\mathcal W'(s)+\nu E(s)=F_w(s).
}
\]

Hence

\[
\boxed{
\operatorname{Var}_J(\mathcal W)
\le
2(\mathscr B_J+\mathscr D_J).
}
\]

On the absolute-action pure corridor,

\[
\mathscr B_{I_j}\le \mathcal W_-,
\qquad
\operatorname{Var}_{I_j}(\mathcal W)\le \mathcal W_-,
\]

and Euclidean-ball Poincare gives the exact stage ceiling

\[
\boxed{
L_j
\le
L_{abs,+}
:=
\frac6{\pi^2}\frac{R_Z^2}{\nu}.
}
\]

Thus

\[
\boxed{L_+\text{ is also derived on the pure low-turnover lane.}}
\]

Failure of this lane is not an untyped long-stage branch. It is

\[
\boxed{T_{abs}(R_Z)}
\]

with order-one absolute boundary work or compensated-variance turnover.

## 4. Remaining apparent loophole: can the turnover price vanish?

The absolute-action module prices a turnover relative to

\[
\mathcal W_-
:=
\inf_{I_j}\mathcal W.
\]

A possible bookkeeping loophole is therefore

\[
\mathcal W_-\to0
\]

across successive recurrent stages.

If this were allowed without further analysis, a formally order-one relative turnover could have arbitrarily small absolute normalized cost.

The first-hitting core excludes a quiet version of this scenario.

## 5. Fixed local vorticity witness at the first-hitting mark

In the stage normalization, choose the marked first-hitting center as the spatial origin at the initial marked slice and normalize

\[
|\Omega(0)|=1.
\]

On the retained compact analytic core, there is a uniform local first-derivative bound

\[
\|\nabla\Omega\|_{L^\infty(B_{r_a})}
\le M_1<\infty
\]

for some fixed analytic radius \(r_a>0\).

Let

\[
e:=\Omega(0),
\qquad |e|=1,
\]

and choose

\[
r_*
:=
\min\left\{
\frac{r_a}{2},
\frac{1}{4M_1},
\frac{R_Z}{4}
\right\}>0,
\]

with the harmless convention that the \(1/(4M_1)\) restriction is omitted when \(M_1=0\).

Then on \(B_{r_*}\),

\[
\boxed{
\Omega(x)\cdot e\ge \frac34.
}
\]

Thus the normalized active core contains a fixed directed vorticity witness on a fixed ball.

## 6. Convert vorticity to a velocity-variance floor by \(H^{-1}\) duality

Choose a fixed nonnegative cutoff

\[
\eta\in C_c^\infty(B_{r_*}),
\]

with

\[
\eta=1
\quad\text{on }B_{r_*/2},
\qquad
|\nabla\eta|\le C_\eta r_*^{-1}.
\]

Set

\[
\phi:=\eta e.
\]

Because

\[
\Omega=\nabla\times U,
\]

and \(\phi\) is compactly supported,

\[
\int \Omega\cdot\phi
=
\int U\cdot(\nabla\times\phi).
\]

Let \(\bar U_{R_Z}\) be the mean of \(U\) over the moving ball \(B_{R_Z}\). Since

\[
\int \nabla\times\phi=0,
\]

we may subtract the mean exactly:

\[
\int \Omega\cdot\phi
=
\int_{B_{R_Z}}
(U-\bar U_{R_Z})\cdot(\nabla\times\phi).
\]

The directed vorticity floor gives

\[
\int \Omega\cdot\phi
\ge c_0 r_*^3
\]

for a universal positive geometric constant \(c_0\).

Meanwhile

\[
\|\nabla\times\phi\|_2
\le C_1 r_*^{1/2}.
\]

Therefore Cauchy--Schwarz yields

\[
 c_0r_*^3
\le
V_{R_Z}^{1/2}
C_1r_*^{1/2},
\]

where

\[
V_{R_Z}
:=
\int_{B_{R_Z}}|U-\bar U_{R_Z}|^2.
\]

Hence

\[
\boxed{
V_{R_Z}\ge v_*
:=c_*r_*^5>0.
}
\]

This is a uniform first-hitting velocity-variance floor derived from the normalized vorticity mark; no pointwise velocity lower bound is assumed.

## 7. A collapsing compensated variance is itself a paid event

Normalize the compensating factor so that

\[
A(s_0)=0
\]

at the marked initial slice of the stage. Then

\[
\mathcal W(s_0)=V_{R_Z}(s_0)\ge v_*.
\]

Now split.

### Branch A: compensated variance stays nondegenerate

If

\[
\inf_{I_j}\mathcal W
\ge \frac12v_*,
\]

then

\[
\boxed{\mathcal W_-\ge v_*/2.}
\]

The absolute-action turnover price is therefore uniformly nondegenerate.

### Branch B: compensated variance falls below the floor

If at some time

\[
\mathcal W(s)<\frac12v_*,
\]

then necessarily

\[
\operatorname{Var}_{I_j}(\mathcal W)
\ge \frac12v_*.
\]

The total-variation action inequality gives

\[
\boxed{
\mathscr B_{I_j}+\mathscr D_{I_j}
\ge \frac14v_*.
}
\]

Thus loss of the variance floor is itself a fixed paid turnover event.

Therefore

\[
\boxed{
\text{uniform compensated-variance floor}
\quad\lor\quad
\text{fixed boundary/dissipation action payment}.
}
\]

## 8. Consequence for positive-frequency turnover

On the compact center-nested first-hitting corridor, successive first-hitting stages are disjoint time intervals.

Hence if the paid alternative in Section 7 occurs with positive stage frequency, each selected event carries a fixed normalized local action floor

\[
\boxed{c_{turn}:=v_*/4>0.}
\]

No event may evade this price by sending its reference variance to zero.

This does **not** yet imply a physical/global contradiction, because the exact rescaling/ancestry weight of the boundary and viscous actions must still be inserted before summing across generations.

The gain is more precise:

\[
\boxed{
T_{abs}(R_Z)
\text{ is a genuinely paid branch, not a clock-degeneration loophole.}
}
\]

## 9. Updated stage-clock split

The former primitive label

\[
G_{stage\text{-}length\ degeneration}
\]

should be replaced by

\[
\boxed{
\begin{aligned}
&\text{bounded normalized strain}
+\text{ pure compensated variance}
\\
&\qquad\Longrightarrow
\frac{\log q}{B_+}
\le L_j
\le
\frac6{\pi^2}\frac{R_Z^2}{\nu},
\\[1mm]
&\text{failure}
\qquad\Longrightarrow
\text{typed high-strain/frequency/remote branch}
\lor
T_{abs}(R_Z),
\end{aligned}
}
\]

with \(T_{abs}\) carrying fixed normalized action whenever it is realized on the compact first-hitting core.

## 10. DSD audit

### Removed as primitive recurrence inputs

\[
\boxed{L_-,\qquad L_+.}
\]

They are derived conditionally from already typed corridor structure.

### Removed as quiet escape

\[
\boxed{\mathcal W_-\to0.}
\]

Either the floor persists or its loss already pays fixed action.

### Still open

The paid turnover event must still be connected to a parent/global ancestry ledger with correct scaling and nonreuse. A fixed normalized payment is not automatically a divergent physical cost.

Thus the open object is now an **ancestry pricing problem**, not a stage-clock existence problem.

## 11. Updated upstream menu

After M18-043 and the present audit, the upstream complement list becomes

\[
\boxed{
\mathcal R_{remote/II}
\lor
G_{high\ remote\ frequency}
\lor
G_{weak\text{-}L3\ escalation}
\lor
G_{vanishing/escaping\ recurrent\ core}
\lor
G_{first\text{-}hitting\ genealogy\ loss}
\lor
G_{paid\ turnover\ ancestry}.
}
\]

The old stage-length degeneration label is removed.

## 12. Audit verdict

### Certified

1. The lower first-hitting stage duration is forced by factor-\(q\) amplification under bounded normalized strain.
2. The upper stage duration is forced by the absolute-action moving-ball variance gate on the pure low-turnover lane.
3. Failure of the upper-stage mechanism is a quantitatively paid turnover branch.
4. The first-hitting vorticity mark plus compact local analyticity forces a fixed velocity-variance floor by \(H^{-1}\) curl duality.
5. Collapse of the compensated variance below that floor itself forces fixed boundary/dissipation action.

### Not certified

1. A global contradiction from infinitely many paid turnover stages.
2. Cross-generation ancestry/nonreuse for the turnover action.
3. Closure of the remote/Type-II root.
4. Closure of first-hitting material genealogy loss.
5. Global 3D Navier--Stokes regularity.

## 13. Next target

M18-045 should audit

\[
G_{first\text{-}hitting\ genealogy\ loss}
\]

against the existing material-formation, renormalization-transition, replacement, remote-satellite, and packet-dwell modules.

The objective is to determine whether genealogy loss is a genuinely independent root or merely another union of already typed deformation/diffusion/turnover/remote branches.
