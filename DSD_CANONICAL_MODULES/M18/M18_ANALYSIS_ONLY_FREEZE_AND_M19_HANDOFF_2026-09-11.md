# M18 — Analysis-only freeze and M19 handoff

**Date:** 2026-09-11  
**Status:** AUTHORITATIVE SCOPE FREEZE / ANALYSIS-AUDIT ONLY / HANDOFF TO M19 CALCULATION

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Scope decision

From this point onward, **M18 is an analysis/audit-only module family**.

M18 is used to:

- classify branches and root complexes;
- certify which identities are exact, conditional, heuristic, or blocked;
- detect double counting, rerecording, ancestry-orientation, and topology errors;
- expose missing bridges and representation changes;
- provide firewalls against false closure claims;
- compress many local mechanisms into a smaller set of mathematically distinct endpoints.

M18 is **not** used from this point onward as the main calculation/proof-closure chain.

Any new calculation intended to close a branch, derive a contradiction, prove a coercive inequality, or convert an analyzed endpoint into a theorem belongs in **M19**.

M18 may still receive a later correction only when necessary to repair provenance, audit status, a false assumption, or the statement of an analysis result.

## 2. Canonical M18 handoff tip

The analysis line has been extended beyond the older `CURRENT_FRONTIER.md` snapshot through the material-current/network audit.

For the present handoff, the active analysis tip is:

\[
\boxed{\text{M18-090}.}
\]

The old M18 frontier file remains historical provenance until separately consolidated; this handoff file is authoritative for the M18/M19 scope boundary.

## 3. Upstream root complexes delivered by M18

The current upstream singularity route is compressed into three root complexes:

\[
\boxed{
\mathcal R_{remote}
\lor
\mathcal R_{critical}
\lor
\mathcal R_{AC}
}
\]

plus the compact hard survivor.

### R-remote

Remote / Type-II / historical recycling, including Euler-scale compact or noncompact ancient behavior.

### R-critical

Escaping critical tail, W1 boundary/realization, low-frequency inheritance, and related critical trace defects.

### R-AC

Ancestry conversion: fixed-parent embedding, nonreuse/bounded overlap, and conversion of own-scale payments into a finite parent resource.

M18 does not claim these roots closed.

## 4. CE-H downstream analysis package delivered to M19

M18 has reduced the compact CE-H material-current route to a finite set of exact or typed mechanisms.

Important certified inputs include:

1. on a Frobenius vortex-transverse material patch,
   \[
   \operatorname{div}_\Sigma J_\Sigma=-\kappa\rho;
   \]
2. the redistributive Hodge component satisfies
   \[
   J_G=\nabla_\Sigma\psi,
   \qquad
   -\Delta_\Sigma\psi=\kappa\rho;
   \]
3. exact pure label-invisible circulation is impossible on a nonzero controlled active patch;
4. on a controlled compact patch family the redistributive component has a quantitative floor unless geometry degenerates;
5. material amplitude-area density obeys
   \[
   D_B\log(\rho A_\Sigma)=\kappa;
   \]
6. fixed controlled current patches carry fixed positive material vorticity flux;
7. finite-memory genealogy therefore reduces recurrent current activity to persistent fixed-flux populations or already typed costed exits;
8. population-restricted amplitude moments obey
   \[
   \frac1p(M_{p,i})'
   =A_{p,i}-D_{p,i}+B_{p,i}-c_pM_{p,i};
   \]
9. internal population boundary exchange is antisymmetric,
   \[
   e_{ij}^{(p)}=-e_{ji}^{(p)};
   \]
10. signed interface exchange admits positive/negative sector decomposition, but net multi-p current need not be monotone in p.

## 5. Mandatory M19 input rule

M19 may use an M18 result as an unconditional calculation input **only if M18 marked the needed statement as exact/certified**.

If an M18 result is conditional, compactness-dependent, geometry-dependent, or only a branch classification, M19 must carry the condition explicitly.

No M19 module may silently upgrade

\[
\text{analysis endpoint}
\quad\text{to}\quad
\text{proved theorem input}.
\]

## 6. Permanent firewalls inherited by M19

M19 must preserve:

\[
\boxed{\text{pointwise blowup}\neq\text{integrated payer}},
\]

\[
\boxed{\text{rerecording}\neq\text{new multiplicity}},
\]

\[
\boxed{\text{high-frequency control}\neq\text{low-frequency tightness}},
\]

\[
\boxed{\text{own-scale payment}\neq\text{fixed-parent ancestry payment}},
\]

\[
\boxed{\text{CE-H closure}\neq\text{global Navier--Stokes closure}}.
\]

Also:

\[
\boxed{\text{snapshot payer}\neq\text{spacetime contradiction}}.
\]

## 7. First M19 calculation target

The highest-value immediate downstream calculation from M18-089--090 is the interface-current reduction.

For

\[
u_p:=\frac{\rho^p}{p},
\]

the population edge current is

\[
\boxed{
e_{ij}^{(p)}
=
\int_{S_{ij}}\partial_{n_i}u_p\,dS.
}
\]

The continuum operator is gradient diffusion, while the finite lineage graph can contain cycle-space current.

M19 should determine whether the finite-network current splits exactly into

\[
\boxed{
\text{symmetric coarse conductance gradient}
+
\text{boundary-mode / bulk-source / geometry defect}
}
\]

and whether a nonzero conservative cycle forces a quantitative lower bound on the defect.

This becomes M19-001 and following calculation modules.

## 8. Module-family policy

From this handoff:

- **M18 = analysis / audit / classification / provenance**;
- **M19 = calculation / coercive estimate / contradiction attempt / closure**.

If M19 exposes a hidden assumption, the correction is written back to M18 only as an audit correction; the calculation remains in M19.

---

\[
\boxed{\text{M18 FROZEN AS ANALYSIS-ONLY; M19 IS THE ACTIVE CALCULATION LINE.}}
\]
