# M18-055 — Positive similarity-time density and finite material memory give only order-one record multiplicity and cannot overcome geometric ancestry weights

**Date:** 2026-09-11  
**Status:** AC NONREUSE / MULTIPLICITY THRESHOLD AUDIT / NEGATIVE CLOSURE RESULT

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Purpose

M18-054 shows that only the correctly oriented second-generation blow-down provides the current certified fixed-parent record ledgers.

For palinstrophy, a contradiction would require a record-cell payment count/charge satisfying

\[
\boxed{
\sum_m\frac{N_m}{R_m}=\infty
}
\]

when each independent event pays order one in cell variables.

Since

\[
R_m\asymp q^{m/2},
\]

this requires multiplicity essentially comparable to \(R_m\), up to a nonsummable correction.

The present module asks whether the already proved recurrence mechanisms can supply such multiplicity.

The answer is negative: fixed-thickness positive-density similarity recurrence, finite material-label memory, and fixed-core packet packing provide only order-one multiplicity per geometric record cell.

## 2. Record annulus in first ancient time

For the second-generation record factor

\[
R_m^2=T_m\asymp q^m,
\]

fix

\[
I=[-b,-a],
\qquad 0<a<b<\infty.
\]

The corresponding first ancient interval is

\[
\boxed{
I_m^{anc}
=[-bR_m^2,-aR_m^2].
}
\]

Introduce the standard backward similarity time

\[
\theta=-\log(-t)
\]

(up to an irrelevant additive convention).

The two endpoints give

\[
\theta_{m,-}
=-\log(bR_m^2),
\]

and

\[
\theta_{m,+}
=-\log(aR_m^2).
\]

Therefore the similarity-time width of every fixed record annulus is

\[
\boxed{
\Delta\theta_I
=
\theta_{m,+}-\theta_{m,-}
=
\log\frac ba,
}
\]

independent of \(m\).

This is the central multiplicity fact.

## 3. Positive similarity-time density does not grow with record scale

M5-589--590 construct production events with positive similarity-time frequency and fixed positive event duration after bounded-overlap selection.

Let one selected event occupy a similarity-time window of length at least

\[
\delta_\theta>0.
\]

A record annulus has only the fixed time width

\[
\Delta\theta_I=\log(b/a).
\]

Hence the number of pairwise disjoint fixed-thickness production windows that can fit inside one record annulus is bounded by

\[
\boxed{
N_m^{time}
\le
1+rac{\log(b/a)}{\delta_\theta}
=:
N_{time}<\infty.
}
\]

The bound is independent of \(m\) and therefore independent of \(R_m\).

Thus

\[
\boxed{
\text{positive similarity-time event density}
\not\Rightarrow
N_m\to\infty.
}
\]

It supplies recurrence, not scale-growing multiplicity.

## 4. The same conclusion holds for the M18 robust-event thickness mechanism

M18-020--021 replace pointwise event detection by positive active-time sets and, on the compact equicontinuous branch, prevent arbitrary event thinning.

This strengthens each event from a zero-time witness to a fixed-thickness spacetime payment block.

But once event thickness has a fixed lower scale, the packing number inside a fixed log-time record annulus remains order one.

Hence temporal thickening improves **legitimacy of one charge**, not **number of charges per geometric generation**.

## 5. Ergodic density also gives only linear growth in similarity time

Suppose an event indicator \(a(\theta)\) has invariant mean

\[
\langle a\rangle=d_*>0.
\]

Over a long similarity interval of length \(L\), the number/occupation measure of events grows like

\[
O(L).
\]

But a fixed record cell samples only

\[
L=\log(b/a)=O(1).
\]

Therefore its expected/typical occupation remains

\[
\boxed{O(1)}
\]

per record generation.

This remains true whether the recurrent process is periodic or aperiodic ergodic.

## 6. Why enlarging the record annulus does not give a free gain

One might try to choose an \(m\)-dependent normalized window

\[
I_m=[-b_m,-a_m]
\]

with

\[
\log(b_m/a_m)\to\infty
\]

so that many positive-density events lie inside one record cell.

To obtain

\[
N_m\sim R_m,
\]

from a fixed event density would require roughly

\[
\boxed{
\log\frac{b_m}{a_m}
\gtrsim R_m.
}
\]

Equivalently,

\[
\frac{b_m}{a_m}
\gtrsim e^{cR_m}.
\]

Such a cell spans an enormous range of ancestral physical times and is no longer a fixed annular record cell.

The parent windows then overlap many geometric generations, destroying the uniform bounded-overlap property that made the cross-generation ledger finite.

Thus record-window enlargement trades event count against parent overlap; it does not create a free ancestry contradiction.

## 7. Quantitative overlap tradeoff

For geometric record centers

\[
R_m^2\asymp q^m,
\]

a fixed annulus ratio \(b/a\) intersects only a bounded number of neighboring record scales.

If the log-width is

\[
L_m:=\log(b_m/a_m),
\]

then the number of geometric scales whose centers can fall inside the same logarithmic parent-time span is of order

\[
\boxed{
1+rac{L_m}{\log q}.
}
\]

Thus increasing event capacity by a factor \(L_m\) increases the worst-case cross-generation parent overlap by the same logarithmic order.

A multiplicity argument based only on occupying more log-time cannot beat the ledger by changing the window definition.

## 8. Finite material-lineage memory gives only order-one multiplicity

The replacement/multiflux audit gives a uniform local label bound

\[
\boxed{
N_{labels}\le N_{max}<\infty.
}
\]

After finitely many new labels, recurrent replacement must route to a typed exit.

Therefore distinct material labels inside one compact production region can contribute at most

\[
O(1)
\]

simultaneous independent payers before an exit is declared.

Material-label multiplicity cannot supply

\[
N_m\sim R_m.
\]

## 9. Fixed-core spatial packet packing is also order one

On the compact analytic production core, fixed positive flux/amplitude packets have a uniform positive normalized radius after the usual smoothness thickening.

A fixed bounded spatial region can contain only finitely many pairwise disjoint packets of this fixed size.

Therefore purely spatial packing inside the finite-depth production annulus also gives

\[
\boxed{N_m^{space}=O(1).}
\]

Allowing packet radii to shrink with \(m\) would be a new concentration/decompactification branch and cannot be counted as the same fixed payer theorem.

## 10. Combined ordinary multiplicity bound

On the compact regular branch, combine:

- fixed log-time record width;
- fixed event-time thickness;
- finite material-label memory;
- fixed packet radius/flux scale in the local production core.

Then the number of certified independent order-one standard payer blocks inside one record cell satisfies schematically

\[
\boxed{
N_m\le N_*<\infty
}
\]

unless one of the already typed decompactifications/exits occurs.

Thus palinstrophy ancestry gives only

\[
\sum_m\frac{N_m}{R_m}
\lesssim
N_*\sum_mR_m^{-1}
<\infty.
\]

No contradiction follows.

## 11. The failure is even stronger for raw-H2 and D3

For fixed order-one record-cell charges,

\[
\sum_mR_m^{-3}N_m<\infty,
\]

and

\[
\sum_mR_m^{-5}N_m<\infty
\]

under the same order-one multiplicity bound.

Therefore recurrence multiplicity cannot rescue higher derivative payers.

The derivative-order firewall becomes:

\[
\boxed{
\text{fixed-density recurrence}
+\text{higher derivative payer}
\text{ is ancestry-cheaper, not stronger}.}
\]

## 12. Consequence for the non-CE-H branches

M18-040 reduces

\[
CP-E,\ CP-S,\ CE-T,\ Migration
\]

to palinstrophy/raw-H2 resources.

M18-055 now shows that their known positive-density terminal recurrence and finite genealogy are **not sufficient** to overcome the generic \(R^{-1}\) or \(R^{-3}\) ancestry weights.

Thus those branches need one of:

1. lower-order descent to standard-energy spacetime enstrophy;
2. scale-growing event amplitude;
3. scale-growing independent multiplicity from a genuinely new mechanism;
4. a signed/monotone/rigidity obstruction not based on unsigned additive ancestry.

Repeatedly proving another order-one \(P/H\) event is low value.

## 13. Consequence for CE-H

The same negative result applies to the CE-H internal payer tree.

Order-one recurrent payments in

\[
P,\quad H,\quad D3
\]

cannot close merely by positive density.

Therefore the high-value CE-H routes are exactly those already highlighted by M18:

- descent to the positive-\(R\) standard-energy ledger through coefficient control;
- coefficient/internal-scale growth so fast that the descent itself becomes quantitative;
- a signed or monotone structure;
- a true scale-growing payer amplitude;
- a rigidity theorem for the surviving compact class.

## 14. Could event amplitude grow with R_m?

The multiplicity failure does not exclude a charge-growth route.

If one record event pays

\[
c_m
\]

instead of a fixed \(c_*>0\), palinstrophy contradicts the parent budget if

\[
\boxed{
\sum_m\frac{c_m}{R_m}=\infty.
}
\]

For geometric \(R_m\), a sufficient scale is roughly

\[
 c_m\gtrsim R_m
\]

up to a nonsummable correction.

Likewise raw-H2 needs roughly

\[
 c_m\gtrsim R_m^3,
\]

and D3 roughly

\[
 c_m\gtrsim R_m^5.
\]

These are severe thresholds.

This makes lower-order descent much more plausible than direct high-derivative growth.

## 15. A new AC fork

After the multiplicity audit, \(\mathcal R_{AC}\) should be split into:

\[
\boxed{
\begin{aligned}
AC\text{-}D &: \text{lower-order descent to a stronger finite parent budget},\\
AC\text{-}G &: \text{scale-growing event charge},\\
AC\text{-}S &: \text{signed/monotone/rigidity route},\\
AC\text{-}B &: \text{boundary-only payer coercion},\\
AC\text{-}W &: \text{ancestral dwell/return-weight amplification}.
\end{aligned}
}
\]

Ordinary positive-density multiplicity is retired as a plausible closure mechanism on the compact branch.

## 16. Audit verdict

### Certified

1. A fixed second-generation annular cell has similarity-time width \(\log(b/a)\), independent of record scale.
2. Fixed-duration positive-density production therefore supplies only order-one temporal multiplicity per record generation.
3. Finite material-label memory supplies only order-one label multiplicity.
4. Fixed-core packet packing supplies only order-one spatial multiplicity.
5. Enlarging the log-time cell increases parent overlap and does not give a free multiplicity gain.
6. Order-one multiplicity cannot overcome geometric \(R^{-1},R^{-3},R^{-5}\) ancestry weights.
7. Positive-density recurrence alone is therefore not a viable additive ancestry closure mechanism.

### Still open

1. Lower-order resource descent for each branch.
2. Scale-growing charge theorems.
3. Signed/monotone/rigidity structures.
4. Boundary-only turnover coercivity.
5. Ancestral dwell/return-weight amplification.
6. Global 3D Navier--Stokes regularity.

## 17. Next target

M18-056 should prioritize **AC-D: lower-order descent**.

Start with the non-CE-H branches and ask whether their local \(P/H\) payers can be converted, under already inherited coefficient/strain bounds, to a fixed positive spacetime-enstrophy payment in the *same correctly aligned record cell*.

For CE-H, use the exact effective coefficient ratios from M18-032--035 rather than the crude supremum \(K_m\).

The target is the positive-\(R\) standard-energy ancestry ledger, because ordinary multiplicity has now been shown too weak.
