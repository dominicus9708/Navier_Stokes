# DSD M17-406 — Correction: raw-`H2` is now a finite `R^{-3}` ancestral resource, while log-`kappa` pullback remains CE-H genealogy conditional

Date: 2026-09-08  
Canonical ID: **M17-406**

Status: **ACTIVE CORRECTION NOTICE / RESOURCE-STATUS UPDATE / REPRESENTATION-GENEALOGY FIREWALL**

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Correction to M17-400 and M17-402

M17-400 and M17-402 correctly reduce their respective phase-tilt and cutoff-replenishment payers to fixed positive normalized raw-`H2` occupancy.

Those modules also stated, using the information then available, that no finite first-generation raw-`H2` spacetime total was certified.

M17-404 now proves

\[
\boxed{
\int_{-\infty}^{0}
\|\Delta\Omega(t)\|_2^2dt
<\infty
}
\]

for the first-generation marked ancient element.

M17-405 therefore proves the true cross-generation ledger

\[
\boxed{
\sum_m
R_m^{-3}
\int_I
\|\Delta\Omega_m(s)\|_2^2ds
<\infty.
}
\]

Accordingly, the phrase **unbudgeted raw-H2 firewall** in M17-400/402 is superseded.

The correct phrase is

\[
\boxed{
\text{finite }R_m^{-3}\text{-weighted raw-`H2` ancestral ledger}
\text{ / cubic multiplicity firewall}.
}
\]

No local inequality in M17-400/402 is retracted.

## 2. Consequence for phase-tilt payers

Suppose a mapped descendant event in record cell `m` forces an order-one normalized raw-`H2` spacetime payment `h_{m,j}`.

Then M17-405 gives

\[
\boxed{
\sum_mR_m^{-3}\sum_jh_{m,j}<\infty
}
\]

for pairwise disjoint or uniformly bounded-overlap mapped events.

Thus M17-400/402 can produce a contradiction only if their descendant architecture forces

\[
\boxed{
\sum_mR_m^{-3}\sum_jh_{m,j}=\infty.
}
\]

For order-one events this requires essentially record-cubic multiplicity.

## 3. Why log-`kappa` is different

M17-403 computes the formal exact scaling

\[
\mathscr D_{\log\kappa,R}(I)
=R\,
\mathscr D_{\log\kappa}^{anc}(R^2I)
\]

whenever the CE-H multiplier relation

\[
\Delta\Omega=\kappa\Omega
\]

is defined on both sides of the scaling map on the same sign-preserving material population.

However, the late M17 CE-H architecture belongs to a descendant/canonical branch.

A record cell obtained from the first-generation ancient element by

\[
\Omega_m(y,s)=R_m^2\Omega(R_my,R_m^2s)
\]

is not automatically certified to carry the same exact CE-H multiplier field on the required parent-to-M17 genealogy merely because a later subsequential/canonical survivor does.

Therefore the log-`kappa` ancestry identity is **representation-conditional** in the current proof tree.

It may be used only after the parent-to-record CE-H/domain/genealogy persistence debt is paid.

## 4. Raw-`H2` does not have this representation debt

The quantity

\[
\|\Delta\Omega\|_2^2
\]

is defined for every smooth vorticity field independently of CE-H.

Hence the M17-404--405 raw-`H2` ledger passes through the first-to-second generation record map without requiring a coefficient field, a sign choice, or a nodal extension.

This makes raw-`H2` a genuinely upstream resource, unlike log-`kappa` diffusion.

## 5. Updated resource hierarchy

The late compact CE-H analytic payer hierarchy should now be read as

\[
\boxed{
\begin{aligned}
\text{palinstrophy payer}
&\to R_m^{-1}\text{-weighted finite ancestor},\\
\text{raw-`H2` payer}
&\to R_m^{-3}\text{-weighted finite ancestor},\\
\text{away-zero log-`kappa` diffusion}
&\to R_m^{-1}\text{ formal scaling}\n&\quad\text{but parent/CE-H genealogy certification still OPEN},\\
\text{near-zero log branch}
&\to\text{zero-corridor/crossing architecture}.
\end{aligned}
}
\]

Thus the raw-`H2` budget problem is solved, but with a severe record discount.

## 6. Revised narrow frontier

There are now three quantitatively distinct ways forward on the compact CE-H branch:

1. force enough raw-`H2` descendant multiplicity to defeat
   \[
   R_m^{-3};
   \]
2. establish representation-safe parent-to-record CE-H persistence and then seek a finite/rigid log-`kappa` ledger with the milder
   \[
   R_m^{-1}
   \]
   scaling;
3. force the branch into the already spatialized zero-corridor/interface/genealogy exits.

The first route needs essentially cubic record multiplicity and is therefore quantitatively demanding.

The second route has a better scaling exponent but a more serious representation/genealogy debt.

## 7. DSD audit role

This is a resource-status correction and representation audit. No DSD axiom enters the PDE statements.

## 8. Audit verdict

**PASS-CORRECTION.**

M17-400/402 retain their local payer reductions, but their old `no finite raw-H2 ancestor` interpretation is superseded by M17-404--405.

The current raw-`H2` obstruction is not absence of a budget; it is the exact `R_m^{-3}` ancestry discount.

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
