# DSD M17-443 — Positive-flux label reweighting is exact replicator/Gibbs dynamics with no signed entropy budget, so closure requires a spatial coefficient-action ledger or geometry loss

Date: 2026-09-09  
Canonical ID: **M17-443**

Status: **ACTIVE FLUX-MEASURE REPLICATOR REDUCTION / ENTROPY NO-GO / SPATIAL-ACTION FRONTIER**

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Goal

M17-442 identifies coefficient/amplitude flux-measure redistribution as the narrow aggregate survivor after the single-loop zero-transition branches are closed.

The present module solves the measure reweighting equation exactly and audits whether redistribution itself carries a monotone entropy cost.

It does not.

The entire positive-flux label redistribution is an exact replicator/Gibbs reweighting driven by cumulative coefficient action.

## 2. Fixed material-label reference space

Let `(Lambda,mu)` be a fixed material-label reference space on a coherent positive CE-H tube family.

Write the evolving positive flux measure as

\[
d\Phi_t=f_t(\lambda)d\mu(\lambda),
\qquad
f_t>0.
\]

Exact CE-H material flux evolution gives

\[
\boxed{
\partial_tf_t
=\nu\kappa(\lambda,t)f_t.
}
\]

The total positive flux is

\[
\Phi_t=\int_\Lambda f_t\,d\mu.
\]

Define the normalized flux probability density

\[
\boxed{
p_t(\lambda)
:=\frac{f_t(\lambda)}{\Phi_t}.
}
\]

## 3. Exact replicator equation

Differentiate `Phi_t`:

\[
\dot\Phi_t
=\nu\int\kappa f_t\,d\mu
=\nu\Phi_t\langle\kappa\rangle_{p_t}.
\]

Therefore

\[
\begin{aligned}
\partial_tp_t
&=\frac{\nu\kappa f_t}{\Phi_t}
-\frac{f_t\dot\Phi_t}{\Phi_t^2}\\
&=\nu(\kappa-\langle\kappa\rangle_{p_t})p_t.
\end{aligned}
\]

Hence

\[
\boxed{
\partial_tp_t
=\nu(\kappa-\langle\kappa\rangle_{p_t})p_t.
}
\]

This is an exact replicator equation on material-label space.

## 4. Exact Gibbs representation

Integrating the unnormalized flux equation gives

\[
f_t(\lambda)
=f_0(\lambda)
\exp\left(
\nu\int_0^t\kappa(\lambda,s)ds
\right).
\]

Normalize by total flux:

\[
\boxed{
 p_t(\lambda)
=
\frac{
 p_0(\lambda)
 \exp\left(\nu\int_0^t\kappa(\lambda,s)ds\right)
}{
 \int_\Lambda
 p_0(\eta)
 \exp\left(\nu\int_0^t\kappa(\eta,s)ds\right)d\mu(\eta)
}.
}
\]

Thus every flux-measure redistribution is generated by the cumulative coefficient action

\[
\boxed{
\mathcal K_t(\lambda)
:=\nu\int_0^t\kappa(\lambda,s)ds.
}
\]

## 5. Pairwise redistribution identity

For any two material labels with positive density,

\[
\boxed{
\log\frac{p_t(\lambda)}{p_t(\eta)}
=
\log\frac{p_0(\lambda)}{p_0(\eta)}
+
\nu\int_0^t
\big(\kappa(\lambda,s)-\kappa(\eta,s)\big)ds.
}
\]

Therefore an `M`-fold relative redistribution requires

\[
\boxed{
\nu\left|
\int_0^t
(\kappa_\lambda-\kappa_\eta)ds
\right|
\gtrsim
\log M
}
\]

up to the initial density ratio.

No other label-space mechanism exists inside exact coherent CE-H.

## 6. Entropy no-go

A tempting shortcut is to assert that repeated redistribution must spend a positive entropy budget.

Let

\[
D(t)
:=
\int p_t\log\frac{p_t}{p_0}d\mu
\]

be relative entropy to the initial flux probability.

Using the replicator equation,

\[
\boxed{
\dot D(t)
=\nu\operatorname{Cov}_{p_t}
\left(
\kappa,
\log\frac{p_t}{p_0}
\right).
}
\]

The right-hand side has no fixed sign.

Likewise the Shannon entropy

\[
S(t):=-\int p_t\log p_t d\mu
\]

satisfies

\[
\boxed{
\dot S(t)
=-\nu\operatorname{Cov}_{p_t}(\kappa,\log p_t),
}
\]

again with no fixed sign.

Therefore

\[
\boxed{
\text{flux-label redistribution}
\not\Rightarrow
\text{monotone entropy dissipation}.
}
\]

No finite entropy budget follows from normalization alone.

## 7. Relation to spatial coefficient contrast

The pairwise identity shows that any large reweighting must be paid by a large integrated coefficient contrast.

Suppose two currently represented labels lie inside a common cross-sectional chart of diameter

\[
D_A(t)\le C_Dr
\]

and normalized coefficient gradient is bounded:

\[
\boxed{
r^3\|\nabla_\perp\kappa\|_\infty\le G_*.
}
\]

Then

\[
|\kappa(\lambda,t)-\kappa(\eta,t)|
\le
C_Dr\|\nabla\kappa\|_\infty
\le
C_DG_*r^{-2}.
\]

Over one own-time `Delta t ~ r^2`,

\[
\boxed{
\nu\left|
\int_{I_r}(\kappa_\lambda-\kappa_\eta)dt
\right|
\le C(G_*,C_D,\nu).
}
\]

Thus one own-time can change relative flux weights by at most a bounded factor under normalized gradient/chart compactness.

This is the redistribution counterpart of the M17-442 one-own-time amplitude firewall.

## 8. Why this still does not close gradual redistribution

M17-441 only forces flux-weighted normalized amplitude to decay summably across records. A power-model survivor needs merely

\[
\mathfrak a_{\Phi,m}\sim m^{-p},
\qquad p>1.
\]

Producing a record-index weight ratio of order `m^p` requires cumulative coefficient-action contrast only

\[
\log m^p
=p\log m
\sim p\log\log R_m.
\]

A parent record contains `O(R_m^2)` own-time units. Therefore a bounded `O(1)` redistribution per own-time can easily accumulate the required double-logarithmic action.

Hence normalized coefficient-gradient compactness alone is insufficient.

## 9. Exact remaining need

To turn M17-443 into a contradiction one would need one of the following genuinely stronger statements:

1. a finite ancestral budget for integrated transverse coefficient-action contrast;
2. a coercive link from cumulative label reweighting to the M17-307 palinstrophy ledger;
3. a coercive link to M17-404--405 raw-`H2` without losing the summable amplitude factor;
4. a spatial zero-corridor theorem with flux-linear rather than amplitude-quadratic payment;
5. a rigidity theorem preventing indefinite coefficient-action segregation on a compact material label family.

None is currently certified.

## 10. Geometry/representation split

If the common cross-sectional chart or diameter bound used in Section 7 fails, retain

\[
G_{cross\text{-}sectional\ chart/geometry\ decompactification}.
\]

If the physical coefficient gradient bound fails, retain normalized coefficient-gradient/high-jet decompactification.

If the material labels cannot be represented through one coherent CE-H genealogy, retain genealogy/interface/domain loss.

Thus the redistribution branch remains fully typed rather than hidden inside an abstract probability measure.

## 11. Updated narrow branch

\[
\boxed{
\begin{aligned}
G_{coefficient/amplitude\ flux\text{-}measure\ redistribution}
\Longrightarrow{}&
G_{cumulative\ transverse\ coefficient\text{-}action\ contrast}\\
&\lor G_{cross\text{-}sectional\ chart/geometry\ loss}\\
&\lor G_{normalized\ coefficient\ gradient/high\text{-}jet\ decompactification}\\
&\lor G_{genealogy/interface/domain\ loss}.
\end{aligned}
}
\]

The first branch has an exact pairwise logarithmic representation but no certified finite ancestral budget.

## 12. DSD audit role

DSD is used only to distinguish normalized probability redistribution from physical spatial coefficient action. The proof is the exact CE-H flux law, normalization of positive measures, and elementary entropy differentiation.

## 13. Audit verdict

**PASS as an exact reduction and a no-go against an entropy shortcut.**

Flux-measure redistribution is completely represented by cumulative coefficient-action contrast, but that contrast currently lacks a sign-definite entropy or ancestral budget. The next high-value calculation is to seek a spatial coefficient-action budget or prove that none follows from the present compactness assumptions.

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
