# DSD M17-295 — A nonvanishing heat-cutoff boundary remainder forces super-Gaussian spatial-infinity growth

Date: 2026-09-06  
Canonical ID: **M17-295**

Status: **INFINITY-BOUNDARY QUANTIFICATION / M17-287/294 RETAIN A FAR-BOUNDARY REMAINDER WHEN THE BACKWARD HEAT-KERNEL REPRESENTATION CANNOT BE CLOSED AT SPATIAL INFINITY. FOR FIXED BACKWARD TIME `T`, ALL HEAT-KERNEL AND KERNEL-DERIVATIVE FACTORS ON A CUTOFF ANNULUS OF RADIUS `R` DECAY LIKE A POLYNOMIAL FACTOR TIMES `exp(-cR^2/T)`. THEREFORE A BOUNDARY/COMMUTATOR REMAINDER THAT STAYS ORDER ONE AS `R->infinity` REQUIRES THE ANNULAR `V`/`grad V` NORMS TO GROW AT LEAST LIKE `exp(cR^2/T)` UP TO POLYNOMIAL FACTORS. THUS THE FAR-BOUNDARY BRANCH IS NOT MERE POLYNOMIAL DECOMPACTIFICATION: IT IS A SUPER-GAUSSIAN SPATIAL-INFINITY FEED. COMBINED WITH M17-294, THE UNBOUNDED ANCIENT DERIVATIVE-FEED LANE REDUCES TO GROWING NORMALIZED PALINSTROPHY OR SUPER-GAUSSIAN INFINITY GROWTH. GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Cutoff representation remainder

Let `chi_R` be a smooth cutoff equal to one on `B_R` and zero outside `B_{2R}`.

The cutoff backward representation for the present second derivative has the form

\[
\Delta V(z_*,0)
=
\int\Delta p_T(z_*-y)\chi_R(y)V(y,-T)dy
+\mathcal B_{R,T}.
\]

The remainder `B_{R,T}` is supported where

\[
R\lesssim|y-z_*|\lesssim2R
\]

and contains terms generated by

\[
\nabla\chi_R,
\qquad
\Delta\chi_R,
\]

paired with the heat kernel / its derivatives and `V`, `grad V` over the backward spacetime cylinder.

---

## 2. Gaussian kernel suppression on the far annulus

For fixed `T>0` and `0<s<=T`, the heat kernel satisfies

\[
|\nabla^m p_s(z_*-y)|
\le
C_m
s^{-(3+m)/2}
P_m\!\left(\frac{|y-z_*|}{\sqrt s}\right)
\exp\!\left(-\frac{|y-z_*|^2}{C_ms}\right),
\]

where `P_m` is a fixed polynomial.

On the cutoff annulus and for `s<=T`, this implies

\[
\boxed{
|\nabla^m p_s|
\le
C_{m,T}
\operatorname{poly}(R)
\exp\!\left(-c\frac{R^2}{T}\right).
}
\]

Also

\[
|\nabla\chi_R|\lesssim R^{-1},
\qquad
|\Delta\chi_R|\lesssim R^{-2}.
\]

Thus every commutator coefficient inherits the same Gaussian suppression.

---

## 3. Annular norm bound for the remainder

Let

\[
\mathcal A_R:=\{R\le|y-z_*|\le2R\}.
\]

After Cauchy--Schwarz in space and time, the boundary remainder obeys a schematic bound of the form

\[
\boxed{
|\mathcal B_{R,T}|
\le
C_T\operatorname{poly}(R)
 e^{-cR^2/T}
\mathcal N_{R,T},
}
\]

where `N_R,T` is an annular spacetime norm built from the relevant `L2` quantities, for example

\[
\mathcal N_{R,T}^2
\lesssim
\int_{-T}^{0}\int_{\mathcal A_R}
\left(
|V|^2+|\nabla V|^2
\right)dy\,d\tau
\]

up to fixed polynomial `R` factors already displayed.

The exact polynomial power is irrelevant to the exponential classification.

---

## 4. Nonvanishing remainder implies super-Gaussian feed

Suppose

\[
\boxed{
\limsup_{R\to\infty}|\mathcal B_{R,T}|
\ge b_0>0
}
\]

for one fixed `T>0`.

Then Section 3 forces

\[
\boxed{
\mathcal N_{R,T}
\ge
c_T\operatorname{poly}(R)^{-1}
\exp\!\left(c\frac{R^2}{T}\right)
}
\]

along a sequence `R->infinity`.

Hence the corresponding squared annular mass/gradient action has at least

\[
\boxed{
\exp\!\left(c'\frac{R^2}{T}\right)
}
\]

growth after adjusting constants.

This is **super-Gaussian spatial-infinity feed** relative to any polynomial growth class.

---

## 5. Updated unbounded-nodal/infinity gate

M17-294 gives, when the boundary remainder vanishes,

\[
H_{growing\ normalized\ palinstrophy}.
\]

M17-295 gives, when it does not vanish,

\[
G_{super\text{-}Gaussian\ spatial\ infinity\ feed}.
\]

Therefore

\[
\boxed{
G_{unbounded\ ancient\ derivative\ survivor}
\Longrightarrow
H_{growing\ normalized\ palinstrophy}
\lor
G_{super\text{-}Gaussian\ infinity\ growth}
\lor
G_{coefficient/interface\ failure}.
}
\]

The former generic `far-boundary remainder` label is no longer untyped.

---

## 6. Relation to polynomial-growth branches

The super-Gaussian branch is strictly beyond

\[
R^7,
\quad
R^N
\]

for every fixed polynomial exponent `N` at fixed `T`.

Therefore M17-291's polynomial minimal-growth analysis and M17-295's nonvanishing-boundary analysis are disjoint and exhaustive with respect to the cutoff representation mechanism.

---

## 7. DSD audit

- The statement is for fixed backward horizon `T`; it does not silently interchange `T->infinity` with `R->infinity`.
- Only the exponential rate is retained; polynomial prefactors are not overinterpreted.
- Failure of the representation is converted to a quantitative infinity-growth branch, not discarded.
- This is still not a contradiction in the packet-normalized tangent because the normalization may itself vanish pre-limit.
- Global 3D Navier--Stokes regularity remains unproved.

---

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
