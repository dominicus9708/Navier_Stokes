# DSD M17-147 — Lifting to the full `kappa` gradient removes the component-commutator source and forces a log-amplitude Hessian threshold

Date: 2026-09-05  
Canonical ID: **M17-147**

Status: **FULL-GRADIENT RECOMPRESSION / M17-146 WRITES THE SINGLE FOLD-DRIVER COMPONENT `K_xi=D_xi kappa` AS A DAMPED WEIGHTED-DIFFUSION EQUATION WITH THE COMMUTATOR `[D_xi,L_rho]kappa`. THIS MODULE SHOWS THAT THE COMMUTATOR IS NOT AN INDEPENDENT CONSTITUTIVE RECHARGE WHEN THE FULL VECTOR `G:=grad kappa` IS TRACKED. DIFFERENTIATING THE M5-682 CONSTITUTIVE LAW GIVES THE EXACT VECTOR EQUATION `D_BG=L_rho G+[2 Hess(log rho)-(grad B)^T-I]G+grad L_rho sigma+grad R_geom`. ON THE QUIET LOW-AMPLITUDE HIGH-JET PURE-KERNEL BRANCH, `Sigma->0`, THE ANTISYMMETRIC VELOCITY GRADIENT IS `O(rho)->0`, AND THE EXPLICIT STRAIN/AMPLITUDE FORCING VANISHES, SO `D_BG=L_rho G+[2 Hess(log rho)-(3/2)I]G+o(1)`. THE LEADING NORMALIZED SYSTEM IS HOMOGENEOUS. A BOCHNER/MAXIMUM-PRINCIPLE CALCULATION THEN SHOWS THAT IF `lambda_max(Hess log rho) <= 3/4-delta` THROUGH A CONTROLLED INTERIOR CORRIDOR, `|grad kappa|` CANNOT MAINTAIN OR REGENERATE AN ORDER-ONE MAXIMUM THERE. CONSEQUENTLY ANY QUIET GENERIC FOLD WITH `|D_xi kappa|` ORDER ONE MUST EITHER ENCOUNTER `lambda_max(Hess log rho) >= 3/4-o(1)`, RECEIVE GRADIENT THROUGH A DOMAIN/GEOMETRY BOUNDARY, OR EXIT THE COMPACT HIGH-JET REGIME. THE FREQUENT-FOLD FIREWALL IS THEREFORE SHARPENED TO A SCALE-FREE LOG-AMPLITUDE-CONVEXITY GATE. GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Why lift from one component to the full gradient

M17-146 gives

\[
D_B(D_\xi\kappa)
=
L_\rho(D_\xi\kappa)
-\frac32D_\xi\kappa
+[D_\xi,L_\rho]\kappa
+o(1)
\]

on the quiet low-amplitude hard hull.

The commutator is real, but it mixes the scalar diffusion of `kappa` with the spatially varying projection direction `xi`.
To decide whether it is a genuine external source or a projection artifact, define the full Euclidean gradient

\[
\boxed{G:=\nabla\kappa.}
\]

Then

\[
K_\xi=\xi\cdot G.
\]

---

## 2. Exact material law for a scalar gradient

For any scalar `f`,

\[
D_B\nabla f
=
\nabla(D_Bf)
-(\nabla B)^T\nabla f.
\]

Apply this to `f=kappa`:

\[
\boxed{
D_BG
=
\nabla h
-(\nabla B)^TG,
}
\]

where

\[
h=D_B\kappa.
\]

---

## 3. Differentiate the M5-682 constitutive law

M5-682 gives

\[
\boxed{
h=L_\rho\kappa+L_\rho\sigma-\kappa+\mathcal R_{geom}.}
\]

Write

\[
\psi:=\log\rho,
\qquad
L_\rho=\Delta+2\nabla\psi\cdot\nabla.
\]

For a scalar `f`, Cartesian differentiation gives the exact identity

\[
\boxed{
\nabla(L_\rho f)
=
L_\rho(\nabla f)
+2(\nabla^2\psi)\nabla f.
}
\]

Indeed, componentwise,

\[
\partial_i(L_\rho f)
=
L_\rho(\partial_i f)
+2(\partial_{ij}\psi)(\partial_jf).
\]

Therefore

\[
\nabla h
=
L_\rho G
+2(\nabla^2\psi)G
+\nabla L_\rho\sigma
-G
+\nabla\mathcal R_{geom}.
\]

Substitute into the material gradient law:

\[
\boxed{
D_BG
=
L_\rho G
+\left[2\nabla^2\psi-(\nabla B)^T-I\right]G
+\mathcal F_G,
}
\]

where

\[
\boxed{
\mathcal F_G
:=
\nabla L_\rho\sigma
+\nabla\mathcal R_{geom}.
}
\]

This equation is exact on the active CE-H set where the M5-682 scalarized formula is used.

---

## 4. Interpretation of the M17-146 commutator

Project the full-gradient equation onto the spatially varying director `xi`.
When `L_rho` passes through the projection `xi·G`, derivatives of `xi` appear and reproduce

\[
[D_\xi,L_\rho]\kappa.
\]

Thus

\[
\boxed{
[D_\xi,L_\rho]\kappa
}
\]

is not an additional constitutive source beyond the full `G` dynamics.
It is the finite-jet coupling generated by observing a homogeneous vector diffusion through a nonuniform moving director field.

This corrects the strongest possible reading of M17-146: the commutator is a **component recharge**, not an independent full-gradient creation term.

---

## 5. Quiet low-amplitude limit of `(grad B)^T`

Decompose

\[
\nabla U=\Sigma+\mathcal R_U,
\]

where `R_U` is antisymmetric.
For incompressible three-dimensional flow, `R_U` is determined by the vorticity `W` and has magnitude `O(|W|)=O(rho)`.

Since

\[
B=U+\frac12y,
\]

we have

\[
\nabla B
=
\Sigma+\mathcal R_U+\frac12I.
\]

On the quiet low-amplitude branch,

\[
\Sigma=o(1),
\qquad
\mathcal R_U=O(\rho)=o(1).
\]

Therefore

\[
\boxed{
(\nabla B)^T
=
\frac12I+o(1).
}
\]

---

## 6. Quiet forcing `F_G` vanishes under the same hard-hull extension

M17-146 already rewrites `R_geom` as strain-weighted normalized geometry plus the explicit amplitude term

\[
\rho(\nabla\times\xi)\cdot\nabla\psi.
\]

With one further normalized-jet bound where needed,

\[
\boxed{
\nabla\mathcal R_{geom}=o(1).
}
\]

Likewise sufficiently high strain-jet compactness gives

\[
\boxed{
\nabla L_\rho\sigma=o(1).
}
\]

Hence

\[
\boxed{
\mathcal F_G=o(1).
}
\]

Failure of these hypotheses is retained as the high-strain-jet / normalized-geometry exit.

---

## 7. Leading normalized full-gradient equation

Combining Sections 5--6,

\[
\boxed{
D_BG
=
L_\rho G
+\left(2\nabla^2\psi-\frac32I\right)G
+o(1).
}
\]

Equivalently,

\[
\boxed{
D_B\nabla\kappa
=
\left(\Delta+2\nabla\log\rho\cdot\nabla\right)\nabla\kappa
+\left(2\nabla^2\log\rho-\frac32I\right)\nabla\kappa
+o(1).
}
\]

The leading quiet system is linear and homogeneous in `grad kappa`.

Therefore an order-one `kappa` gradient must be transported/amplified by the normalized log-amplitude geometry; there is no remaining order-one additive source under the retained hard-hull assumptions.

---

## 8. Bochner identity for `|G|^2`

For the vector weighted Laplacian,

\[
G\cdot L_\rho G
=
\frac12L_\rho|G|^2
-|\nabla G|^2.
\]

Hence the leading quiet equation gives

\[
\boxed{
\begin{aligned}
D_B\frac{|G|^2}{2}
={}&
\frac12L_\rho|G|^2
-|\nabla G|^2\\
&+2G\cdot(\nabla^2\psi)G
-\frac32|G|^2
+o(|G|^2+|G|).
\end{aligned}
}
\]

This is the pointwise gradient-amplitude ledger.

---

## 9. Interior maximum principle and the `3/4` threshold

Consider a controlled interior spacetime corridor where the hard-hull error is uniformly small.
At an interior spatial maximum of

\[
|G|^2,
\]

we have

\[
\nabla|G|^2=0
\]

and

\[
L_\rho|G|^2
=\Delta|G|^2\le0.
\]

Let

\[
\lambda_{max}(\nabla^2\psi)
\]

be the largest eigenvalue of the log-amplitude Hessian.
Then

\[
G\cdot(\nabla^2\psi)G
\le
\lambda_{max}(\nabla^2\psi)|G|^2.
\]

Therefore

\[
D_B\frac{|G|^2}{2}
\le
\left(
2\lambda_{max}(\nabla^2\psi)-\frac32
\right)|G|^2
+o(1)
\]

at such a maximum.

If for some fixed `delta>0`,

\[
\boxed{
\lambda_{max}(\nabla^2\log\rho)
\le
\frac34-\delta
}
\]

through the controlled interior corridor, then for sufficiently remote `R`,

\[
\boxed{
D_B|G|^2
\le
-4\delta|G|^2+o(1)
}
\]

at every positive interior maximum.

Thus an order-one interior maximum cannot be recurrently regenerated there.

---

## 10. Necessary gate for generic fold turnover

M17-144 requires an order-one fold driver

\[
|D_\xi\kappa|\gtrsim1
\]

for a uniformly nondegenerate quiet generic fold.
Since

\[
|D_\xi\kappa|\le|\nabla\kappa|=|G|,
\]

such a fold requires order-one `|G|`.

The maximum-principle result implies the following necessary alternative for repeated interior fold turnover:

\[
\boxed{
\lambda_{max}(\nabla^2\log\rho)
\ge
\frac34-o(1)
}
\]

must occur somewhere in the controlled genealogy needed to maintain/rebuild the required `kappa` gradient, unless the gradient is imported through a domain/geometry boundary or one of the hard-hull assumptions fails.

Thus the generic fold firewall is sharpened to a **log-amplitude convexity gate**.

---

## 11. Exact trace identity for the log-amplitude Hessian

M17-144 gives

\[
\kappa
=
\Delta\psi+|\nabla\psi|^2-|\nabla\xi|^2.
\]

Hence

\[
\boxed{
\operatorname{tr}(\nabla^2\psi)
=
\kappa-|\nabla\psi|^2+|\nabla\xi|^2.
}
\]

The `3/4` gate is therefore a finite normalized geometry condition constrained by the CE-H multiplier and director metric.

However the trace alone does not control the largest eigenvalue: strong positive convexity in one direction can coexist with negative curvature in others.
No contradiction is inferred from the trace identity alone.

---

## 12. Relation to line maxima

At a line-amplitude peak,

\[
g=D_\xi\psi=0.
\]

A strict line maximum additionally has negative second derivative along the vortex integral curve,

\[
D_\xi g<0.
\]

But

\[
D_\xi g
=
\nabla^2\psi(\xi,\xi)
+(D_\xi\xi)\cdot\nabla\psi.
\]

Therefore the line-maximum condition does **not** by itself imply

\[
\nabla^2\psi(\xi,\xi)<0,
\]

because the vortex-line curvature term can shift the sign.

Likewise the `3/4` convexity direction need not be `xi`.
This prevents a false immediate contradiction between line maximality and the new Hessian gate.

---

## 13. DSD audit

### Audit A — `[D_xi,L_rho]kappa` is an independent source of full `grad kappa`

Rejected.
It is generated by projecting the full gradient diffusion onto a nonuniform director field.

### Audit B — the full gradient still has an order-one additive source on the quiet hard hull

Not under the retained higher-jet/low-amplitude assumptions: the leading equation is homogeneous.

### Audit C — the `3/4` threshold proves singularity

Rejected.
It is a necessary normalized convexity condition for recurrent interior gradient maintenance, not a singularity criterion.

### Audit D — the trace identity forces `lambda_max<3/4`

Rejected.
Trace information alone cannot bound the largest Hessian eigenvalue from above.

### Audit E — line maximum means the full Hessian is negative

Rejected.
Only the second derivative along the curved vortex trajectory is negative; curvature coupling remains.

---

## 14. Updated frontier

The quiet generic-fold branch is now reduced from an apparently forced scalar component equation to the homogeneous normalized vector system

\[
\boxed{
D_B\nabla\kappa
=
L_\rho\nabla\kappa
+\left(2\nabla^2\log\rho-\frac32I\right)\nabla\kappa
+o(1).
}
\]

Therefore asymptotically full frequent generic-fold turnover requires at least one of

\[
\boxed{
\begin{aligned}
&\lambda_{max}(\nabla^2\log\rho)\ge3/4-o(1)
&&\text{normalized convexity gate},\\
&\text{gradient inflow through geometry/domain boundary}
&&\text{fresh-gradient import},\\
&\text{loss of strain/normalized-jet compactness}
&&\text{high-jet exit},\\
&H_{1,crit}^{spacetime}
&&\text{critical shell burst}.
\end{aligned}
}
\]

The next efficient calculation is to evaluate the `3/4` Hessian gate in the pure-kernel peak frame using

\[
g=0,
\quad
D_kg=0,
\quad
D_k\xi=0,
\quad
D_\xi\xi=pk+qn,
\quad
D_n\xi=rk,
\]

and determine whether line maximality plus flatness/compensation restricts which Hessian direction can carry the required convexity.

---

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
