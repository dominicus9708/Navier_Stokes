# M17 Current Frontier

Date: 2026-09-08  
Current tip: **M17-420**  
Status: **AUTHORITATIVE M17 NAVIGATION / CORRECTION INDEX**

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

This file takes precedence over historical M17 companion indexes when status labels conflict. Historical modules remain preserved for provenance.

## 1. Permanent correction firewalls

- M17-304 is **SUPERSEDED** by M17-306.
- M17-306--307 separate first- and second-generation ancient solutions.
- M17-313 gives exact CE-H line constancy
  \[
  D_\xi\kappa=0,
  \qquad
  \nabla\kappa\perp W.
  \]
- Nonzero longitudinal `D_xi kappa` recharge readings of M17-144--146 are superseded.
- M17-330, M17-336, and M17-337 are **QUARANTINED for cross-generation use** by M17-338.
- M17-331--335 remain fixed-similarity-generation statements only.
- M17-367 forbids any `kappa`-only positive lower bound on raw `H2`; amplitude scaling leaves `kappa` unchanged while scaling `E` and raw `H2` quadratically.
- M17-383 forbids identifying coefficient thickness with solution-mass thickness without a doubling/frequency input.
- M17-396 corrects the physical `Phi^2/R` log-diffusion cost to a scale-critical normalized order-one charge.
- M17-404--406 **supersede** the earlier M17-385/400/402 interpretation that first-generation raw-`H2` lacks a finite spacetime budget. The local payer reductions remain valid, but the correct terminal ledger is now the finite `R_m^{-3}` ancestral raw-`H2` firewall.
- M17-408 permanently separates all-sign log-`kappa` coordinates from the regular zero corridor because truncated log-diffusion diverges like `1/epsilon` at a regular zero.
- M17-420 supersedes the need for a minimal omega-limit assumption in M17-415--419 on the retained compact loop branch. Those modules remain valid subroutes.
- The temporal-fragmentation caution in M17-414 is not terminal when a pointwise-in-time loop/corridor raw-`H2` lower bound is available: M17-420 integrates directly over the measurable good-time set.

## 2. DSD scope

DSD is used only as a heuristic selector for variables, representation audits, payer-equivalence checks, failure typing, and candidate invariant descriptors.

It is **not** a Navier--Stokes/PDE hypothesis. Every active canonical statement is derived independently by standard mathematics.

## 3. Global proof-tree status

The proof attempt remains conditional on the upstream reduction

\[
\text{hypothetical singularity}
\to
\underbrace{\text{ROOT-CERT}}_{\text{OPEN}}
\to
\text{canonical survivor}.
\]

M5-598 then splits into

\[
CP\!-\!E
\lor
CP\!-\!S
\lor
CE\!-\!T
\lor
Migration
\lor
CE\!-\!H.
\]

The long M12--M17 line is conditional on `CE-H`. The four non-CE-H roots remain independent OPEN roots.

## 4. Certified ancestral resource ledgers

For

\[
\Omega_R(y,s)=R^2\Omega(Ry,R^2s),
\]

\[
\int_I\|D^k\Omega_R\|_p^qds
=
R^{q(2+k-3/p)-2}
\int_{R^2I}\|D^k\Omega\|_p^qdt.
\]

### Palinstrophy — M17-307

\[
\boxed{
\sum_mR_m^{-1}
\int_I\|\nabla\Omega_m\|_2^2ds
<\infty.
}
\]

A contradiction from order-one descendant palinstrophy requires a nonsummable record-linear multiplicity/charge enhancement.

### Raw-H2 — M17-404--405

M17-404 proves for the first-generation ancient element

\[
\boxed{
\int_{-\infty}^{0}
\|\Delta\Omega(t)\|_2^2dt
<\infty,
}
\]

with dyadic backward tail

\[
\int_{-2T}^{-T}\|\Delta\Omega\|_2^2dt
\lesssim T^{-3/2}.
\]

Hence M17-405 gives

\[
\boxed{
\sum_mR_m^{-3}
\int_I\|\Delta\Omega_m(s)\|_2^2ds
<\infty.
}
\]

A contradiction from order-one normalized raw-`H2` packets requires

\[
\boxed{
\sum_m\frac{N_m^{H2}}{R_m^3}=\infty.
}
\]

Any fixed power deficit `N_m=O(R_m^{3-epsilon})` remains summable on geometric records.

## 5. Exact CE-H constitutive and zero-level structure

On exact CE-H,

\[
\Delta W=\kappa W,
\qquad
\nabla\cdot W=0,
\qquad
D_\xi\kappa=0.
\]

M17-338 gives the mandatory physical/similarity dictionary and M17-339 gives

\[
D_t\kappa
=
L_\rho\kappa
+L_\rho\sigma
+\mathcal R_{geom},
\]

with no physical `-kappa` relaxation term.

M17-340 gives the record-scale-critical regular-zero currency

\[
\mathcal Q_0
=
\int
(D_t\kappa)_-\delta(\kappa)
\rho^2|\nabla\kappa|^{-1/3}dxdt.
\]

M17-343--346 thicken regular zero crossings.

M17-408 shows log-`kappa` is intrinsically singular at a regular active zero and must not be used across the zero level.

M17-409 uses coarea plus a coefficient-gradient ceiling to return a uniform regular zero corridor to raw-`H2`:

\[
\boxed{
H_{0,\delta}
\ge
c_{0H}A_{\kappa\kappa}(0).
}
\]

Thus regular zero corridors are no longer an unbudgeted resource; they inherit the `R_m^{-3}` raw-`H2` firewall.

## 6. Geometry, nodal flux, and scale ownership: M17-349--386

- M17-349--354 close the stated uniform-onset harmonic/unbounded-line branch under their compactness hypotheses.
- M17-355--361 analyze bounded/winding geometry and recurrent loop payers.
- M17-361 gives a positive recurrent normalized palinstrophy density for a retained positive-flux recurrent loop family, but by itself returns to M17-307.
- M17-368 gives the flux-bearing bridge
  \[
  E_T\gtrsim\phi^2r^{-1},
  \qquad
  H_T\gtrsim\phi^2r^{-5}.
  \]
- M17-379 corrects flux-evacuation exposure to the true own-scale residence ledger
  \[
  \boxed{
  \sum_j\tau_j^{own}
  \gtrsim\log(1/r).
  }
  \]
- M17-381 gives exact snapshot coefficient-scale raw-`H2` ownership
  \[
  \sum_jH_j=H_{raw},
  \qquad
  r_j^4H_j\asymp E_j.
  \]
- M17-382 upgrades the coefficient scale to a genuine spatial own-scale cell when `r^3|grad kappa|` is bounded.
- M17-386 extends raw-`H2` ownership exactly through time:
  \[
  \boxed{
  \sum_j\int_IH_j(t)dt
  =
  \int_IH(t)dt.
  }
  \]

Raw-`H2` scale-time double counting is therefore closed on exact CE-H.

## 7. Local doubling and standard-energy deformation: M17-383--392

M17-383 introduces finite-scale doubling and proves coefficient regularity alone does not bound solution vanishing order.

M17-384 proves physical exact CE-H material multiplier transport

\[
D_t\Omega=(\sigma+\nu\kappa)\Omega
\]

and preservation of pointwise vanishing order under smooth same-material CE-H evolution.

M17-387 gives on a persistent own-scale CE-H cell

\[
\|\Sigma\|_{L^\infty(B_R)}
\lesssim
R^{-3/2}\|\Omega\|_2.
\]

M17-388 converts this into

\[
\boxed{
\sum_jR_jK_j^2<\infty,
\qquad
\sum_jR_j\mathfrak X_j^2<\infty
}
\]

for deformation and newly generated finite-scale doubling excess.

M17-389 shows negative `kappa` flux exposure is representation safe but satisfies

\[
D_t\log\Phi=\nu\kappa,
\]

so strain cancels from the material flux law.

M17-390--392 show compact recurrence, retained flux, and turnover have only `R`-weighted standard-energy ledgers; order-one or logarithmic recurrence remains summable on geometric records.

## 8. Coefficient migration and M5-688 payer classification: M17-393--402

M17-393 reclassifies `|kappa|R^2 >> 1` as a smaller intrinsic coefficient scale or a normalized-gradient/interface exit.

M17-394 gives on sign-preserving intervals

\[
D_t\log|\kappa|
=
L_\rho\log|\kappa|
+|\nabla\log|\kappa||^2
+\frac{L_\rho\sigma+\mathcal R_{geom}}{\kappa}.
\]

M17-395--396 show retained positive flux removes the low-amplitude firewall but yields only a critical normalized log-diffusion charge.

M17-397 proves M5-688 is a recurrent **cycle-work balance**, not a finite ancestral budget.

M17-398--402 classify all non-topological M5-688 payers:

\[
\boxed{
\begin{aligned}
D_\sigma,P_W,B_\rho,B_\sigma
&\to \text{palinstrophy},\\
\mathcal P_{\kappa\sigma}^{tilt}
&\to \text{raw-`H2`},\\
B_\kappa^{away}
&\to \text{away-zero log-`kappa` diffusion},\\
B_\kappa^{near}
&\to \text{zero corridor},\\
\mathcal C_2^{positive}
&\to \text{raw-`H2`}.
\end{aligned}
}
\]

After M17-404--406, the raw-`H2` entries terminate at the finite `R_m^{-3}` ancestral ledger, not an unbudgeted firewall.

## 9. Exact ancestry scaling and raw-H2 budget correction: M17-403--409

M17-403 computes the record pullbacks:

\[
\int\|\Delta\Omega_R\|_2^2ds
=
R^3\int\|\Delta\Omega\|_2^2dt,
\]

while sign-preserving away-zero log-`kappa` diffusion has formal ancestry exponent `R^{-1}` when the same CE-H multiplier/genealogy persists.

M17-404--405 establish the genuine finite raw-`H2` ancestor.

M17-406 corrects M17-400/402 accordingly.

M17-407 blocks a false strong-`L3` Liouville shortcut: finite raw-`H2` is high-frequency information and does not remove the full velocity's low-frequency exterior tail.

M17-408--409 separate the zero corridor from log coordinates and return regular zero geometry to raw-`H2`.

## 10. Raw-H2 multiplicity thresholds: M17-410--414

### M17-410 — logarithmic residence no-go

Even under the optimistic assumption that every unit own-time from M17-379 pays fixed normalized raw-`H2`, the forced contribution is only

\[
R_m^{-3}\log R_m,
\]

which is summable on geometric records.

### M17-411 — cubic spatial capacity

Inside a fixed parent 3D cell, descendant radius `R_m^{-1}` balls satisfy

\[
N_m^{space}\lesssim R_m^3.
\]

The raw-`H2` contradiction threshold is of the same order as the **maximum three-dimensional spatial packing capacity**. A subcubic power-law population is insufficient.

### M17-412 — temporal capacity

One parent parabolic record window contains at most

\[
N_m^{time}\lesssim R_m^2
\]

descendant own-time units.

Duration alone therefore contributes at most `R_m^{-1}` after the cubic ancestry weight and remains summable. At full temporal saturation, the remaining spatial requirement is record-linear.

### M17-413--414 — loop factorization and exact occupancy threshold

For a parent-length positive-flux exact CE-H loop with own-scale tubular reach,

- line constancy supplies one coefficient scale along the loop;
- loop length supplies `O(R_m)` spatial own-scale segments;
- good-time measure `alpha_m` supplies `alpha_mR_m^2` normalized temporal measure.

Hence

\[
\boxed{
H_m^{norm}
\gtrsim
c\Phi_*^2\alpha_mR_m^3,
}
\]

so the ancestry weight cancels exactly:

\[
\boxed{
R_m^{-3}H_m^{norm}
\gtrsim
c\Phi_*^2\alpha_m.
}
\]

The exact loop closure condition is therefore

\[
\boxed{
\sum_m\alpha_m=\infty.
}
\]

M17-420 later removes the need to interpret `alpha_mR_m^2` as a count of complete intervals; measurable good-time occupation suffices whenever the spatial raw-`H2` lower bound holds pointwise in time.

## 11. Compact loop analytic closure: M17-415--419

M17-415 shows that if the whole omega-limit is minimal, one robust good loop state is visited syndetically with positive lower time density.

M17-416 then closes every minimal-loop state with a nonzero loop coefficient somewhere.

M17-417 closes a record-matched regular transverse zero-gradient loop by M17-409 raw-`H2` corridor payment.

M17-418 defines the higher-order intrinsic scale

\[
\boxed{
r_p
=|D_\perp^p\kappa|^{-1/(p+2)}
}
\]

and closes every finite record-matched transverse/mixed coefficient jet order by the same cubic raw-`H2` packet mechanism.

M17-419 closes the remaining infinite-flat analytic case:

- all `kappa` jets zero at a regular loop point imply `kappa=0` on an open ball;
- exact CE-H gives `Delta Omega=0` on that open ball;
- global spatial analyticity of `Delta Omega` gives `Delta Omega=0` on all `R^3`;
- finite enstrophy `Omega in L2` then forces `Omega=0` by Fourier rigidity.

Thus no analytic infinite-flat positive-flux loop survives.

## 12. M17-420 — retained compact loop branch closed without minimality

For the original compact loop orbit, define empirical measures

\[
\mu_T
=
\frac1T\int_0^T\delta_{Z(\theta)}d\theta.
\]

A subsequence converges weak-* to a probability measure `mu` on the compact loop-state closure.

For each finite jet order `p`, define

\[
M_p(Z)=\max_{x\in\Gamma(Z)}|D^p\kappa(x)|.
\]

M17-419 implies every retained nonzero loop state has at least one finite nonzero jet. Therefore

\[
\mathcal K
=
\bigcup_{p\ge0}\bigcup_{n\ge1}
\{M_p>1/n\}
\]

is a countable cover by robust finite-jet classes.

Since `mu(K)=1`, at least one fixed class has positive `mu` mass. Portmanteau then gives a positive asymptotic time fraction of the **original orbit** in that single fixed finite-jet class.

That class supplies a uniform parent-length arc, linear spatial multiplicity, and a pointwise-in-time raw-`H2` lower bound. Hence its record occupancies satisfy

\[
\sum_m\alpha_m=\infty,
\]

and M17-414 contradicts the M17-405 finite raw-`H2` ancestor.

Therefore, under the retained hypotheses,

\[
\boxed{
\text{a precompact positive-flux exact CE-H closed-loop orbit cannot persist.}
}
\]

Minimality or a separate nonminimal omega-limit branch is no longer needed.

## 13. Current narrow late-CE-H frontier after M17-420

The compact retained closed-loop branch is closed. Its surviving exits are now explicit failures of the retained hypotheses:

\[
\boxed{
\begin{aligned}
&G_{loop\ state\ decompactification},\\
&G_{positive\ flux\ thinning/loss},\\
&G_{nodal/amplitude\ loss},\\
&G_{tubular\ reach/self\text{-}clustering},\\
&G_{jet\text{-}scale/record\text{-}scale\ mismatch},\\
&G_{parent\text{-}to\text{-}record\ scale\text{-}map/genealogy\ failure},\\
&G_{interface/rank/domain\ loss}.
\end{aligned}
}
\]

The highest-value next target is no longer recurrence/minimality. It is to attack the **explicit decompactification and scale-map/genealogy exits**, especially:

1. whether tubular reach/self-clustering can fail indefinitely without paying an already certified palinstrophy/raw-`H2` resource;
2. whether coefficient jet scale can systematically mismatch record scale without triggering the M17-393 migration/gradient-decompactification machinery;
3. whether positive-flux thinning can coexist with the M17-368/379 evacuation ledgers across the same first-generation genealogy;
4. parent-to-M17 logarithmic lookback persistence and bounded-overlap record mapping.

## 14. Separate inherited OPEN dependencies

The proof attempt still inherits:

- `ROOT-CERT` upstream recertification;
- the four non-CE-H M5-598 roots `CP-E`, `CP-S`, `CE-T`, `Migration`;
- parent-to-M17 scale-map/domain/genealogy persistence;
- coefficient-gradient / jet-scale decompactification;
- tubular reach / spatial self-clustering;
- positive-flux thinning / amplitude loss;
- interface / rank / domain exits;
- upstream branch completeness connecting a hypothetical singularity to the retained CE-H scenarios analyzed here.

No statement in M17-420 closes these independent upstream or decompactification branches.

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]