# M17 corrected frontier companion — through M17-314

Date: 2026-09-07  
Status: **AUTHORITATIVE COMPANION FOR THE M17-308--314 CRITICAL-CURRENCY AND DIVERGENCE-FREE CE-H AUDIT**

This companion supplements the historical M17 indexes and the M17-307 cross-generation correction companion.

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

---

## 1. M17-308 — record blow-down scaling atlas

For

\[
\Omega_R(y,s)=R^2\Omega(Ry,R^2s),
\]

one has

\[
\boxed{
\int_I\|D^k\Omega_R\|_p^qds
=
R^{q(2+k-3/p)-2}
\int_{R^2I}\|D^k\Omega\|_p^qdt.
}
\]

Hence the M17-307 `R^-1` ancestry loss for vorticity palinstrophy is dimensionally forced because

\[
q(2+k-3/p)-2
=1
\quad
(k,p,q)=(1,2,2).
\]

Lossless cross-generation candidates must be scale critical, including

\[
\int\|\Omega\|_3^2dt,
\qquad
\int\|S\|_3^2dt,
\qquad
\|V\|_3,
\qquad
\|\kappa\|_{3/2}.
\]

---

## 2. M17-309 — record-window palinstrophy saturation

M5-475/477 give

\[
E(t)=\|\Omega(t)\|_2^2\lesssim(-t)^{-1/2}
\]

and stretching production

\[
|Q(t)|\lesssim(-t)^{-3/2}.
\]

Therefore on every geometric backward window

\[
[-bR^2,-aR^2],
\]

\[
\boxed{
\int\|\nabla\Omega\|_2^2dt
\lesssim R^{-1}.
}
\]

Equivalently every fixed second-generation record window has uniformly bounded palinstrophy.

A fixed descendant payment forces saturation

\[
\boxed{
R\int_{-bR^2}^{-aR^2}
\|\nabla\Omega\|_2^2dt
\asymp1
}
\]

on the selected record subsequence, which is compatible with finite total first-generation palinstrophy.

---

## 3. M17-310 — lossless negative-`kappa` critical threshold

On the regular global CE-H coefficient branch,

\[
\Delta W=\kappa W,
\qquad
W\in H^1,
\quad
W\ne0.
\]

Testing against `W` and using Sobolev gives

\[
\boxed{
\|\kappa_-\|_{L^{3/2}}
\ge S_3>0.
}
\]

This threshold is amplitude independent and exactly invariant under record blow-down.

If the global `L^(3/2)` coefficient branch fails, retain explicit

\[
G_{critical\ kappa_-\ tail}
\lor
G_{nodal/coefficient}.
\]

---

## 4. M17-311 — high-amplitude capture versus nodal concentration

The critical negative coefficient mass obeys the dichotomy

\[
\boxed{
H_{critical\ kappa_-}
\Longrightarrow
H_{uniform\ high\text{-}amplitude\ capture}
\lor
G_{\kappa_-\text{-}nodal}^{crit}.
}
\]

On the high-amplitude branch, compact coefficient bounds convert the critical coefficient mass into

\[
\boxed{
\int\chi_*\rho^2\kappa_-dy
\ge d_->0.
}
\]

Thus the amplitude firewall is removed unless critical negative coefficient mass concentrates near `rho=0`.

---

## 5. M17-312 — spatial negative phase to flux-length occupancy

Using exact flux-coordinate Jacobians,

\[
dy=\frac{d\Phi\,ds}{\rho}
\]

on regular vortex-line flux families, and

\[
dy=\frac{d\Phi_J\,ds}{|J_\xi|}
\]

on nondegenerate Rank-2 ribbons, the M17-311 spatial charge forces a fixed negative `kappa` flux-length occupancy.

This does **not** by itself identify the M5-681 material label distribution.

---

## 6. M17-313 — major divergence-free CE-H correction

Exact CE-H must also satisfy vorticity incompressibility:

\[
\nabla\cdot W=0.
\]

Therefore

\[
0
=\nabla\cdot\Delta W
=\nabla\cdot(\kappa W)
=W\cdot\nabla\kappa.
\]

Hence on the active set

\[
\boxed{D_\xi\kappa=0.}
\]

Consequences:

1. `grad kappa` is purely transverse to vorticity;
2. M17-143's exact generic fold coefficient reduces to
   \[
   \boxed{A_T=D_\xi\sigma;}
   \]
3. under M17-144 quiet high-jet decay,
   \[
   A_T\to0,
   \]
   so a uniformly nondegenerate generic fold cannot survive there;
4. M17-145's
   \[
   K_\xi=D_\xi\kappa
   \]
   is identically zero and its formal PDE becomes the compatibility identity
   \[
   \boxed{\mathcal F_\xi=0;}
   \]
5. under M17-146's quiet reduction,
   \[
   \boxed{\mathcal C_\xi[\kappa]=o(1),}
   \]
   rather than an order-one longitudinal recharge;
6. full transverse `grad kappa` modules such as M5-687 and M17-234/235 remain valid after imposing
   \[
   \nabla\kappa\perp W.
   \]

This supersedes all exact-CE-H uses of nonzero `D_xi kappa` as a fold driver.

---

## 7. M17-314 — quantitative material zero-level current

M17-313 also gives

\[
D_\xi(D_B\kappa)=0.
\]

Thus `kappa` and its material velocity are constant along each connected regular vortex line.

On a bounded-capture-length vortex-line family, M17-312 therefore upgrades to a true material flux-label moment:

\[
\boxed{
\int_{k<0}(-k)F(k,\theta)dk
\ge d_{flux}>0.
}
\]

After recurrent averaging, M5-681's exact stationary equation

\[
\partial_k\overline G=k\overline F
\]

gives

\[
\boxed{
\overline G(0)
\le-d_{flux}<0.
}
\]

Thus the retained branch has a quantitatively nonzero same-material `kappa`-space current through zero.

Failure is routed to

\[
G_{\kappa_-\text{-}nodal}^{crit}
\lor
G_{line\ winding/decompactification}
\lor
G_{rank/interface/flux\ capture}
\lor
G_{coefficient\ noncompact}.
\]

---

## 8. Corrected late CE-H frontier

The current late CE-H tree should be read schematically as

\[
\boxed{
\begin{aligned}
H_{late\ CEH}
\Longrightarrow{}&
G_{strict\ subscale/nodal}\\
&\lor G_{cross\text{-}scale\ H2\ allocation}\\
&\lor G_{parent/record\ genealogy}\\
&\lor H_{critical\ palinstrophy\ saturation}\\
&\lor H_{critical\ transverse\ kappa\ geometry}\\
&\lor H_{quantitative\ material\ kappa\ current}\\
&\lor G_{degenerate\ fold/higher\ tangency}\\
&\lor G_{rank/interface/noncompact}.
\end{aligned}
}
\]

The old order-one longitudinal `D_xi kappa` recharge branch is removed.

---

## 9. Highest-value next targets

1. insert `D_xi kappa=0` and `D_xi h=0` into the M5-682/688 constitutive current and determine whether a fixed `Gbar(0)` can still be sustained;
2. audit M17-147 and later modules whose entry condition depends specifically on nonzero `K_xi`;
3. exploit that all surviving `grad kappa` is transverse, potentially reducing the coefficient geometry to two-dimensional cross-vortex structure;
4. separately continue the unresolved M17-298 cross-scale raw-H2 allocation theorem;
5. separately close the top-level M5-598 non-CE-H branches.

---

## 10. Audit firewall

- `D_xi kappa=0` is exact only on the regular active CE-H coefficient branch; nodal/global-coefficient failures remain explicit.
- Full `grad kappa` is not set to zero.
- Spatial, flux-length, and material-label measures remain distinct until the appropriate Jacobian/line-constancy bridge is inserted.
- M17-304 remains superseded by M17-306.
- No unweighted second-generation finite palinstrophy budget is restored.
- No new global-regularity claim is made.
