# DSD M17-032 — Recurrent curved oblique rank-two with nonzero transverse area has incompatible mean kappa laws

Date: 2026-09-04
Canonical ID: **M17-032**

Status: **INTERNAL OBLIQUE RANK-TWO RECURRENCE CLOSURE / M17-031 GIVES `D_B log|b|=-sigma-1/2`, WHILE RAW VORTICITY SATISFIES `D_B log rho=sigma+kappa-1`; HENCE EVERY CURVED CE-H VORTEX MARKER OBEYS `D_B log(rho|b|)=kappa-3/2`. IF `rho` AND NONZERO CURVATURE `|b|` ARE UNIFORMLY RECURRENT, THIS FORCES `mean kappa=3/2`. ON THE TRANSVERSE-RANK-TWO OBLIQUE SUBBRANCH WITH `j_xi!=0`, RECURRENT NONZERO DIRECTOR-AREA DENSITY GIVES `mean sigma=1`; RECURRENT `rho` THEN INSTEAD FORCES `mean kappa=0`. THESE TWO MEAN LAWS ARE INCOMPATIBLE. THEREFORE A SAME-MARKER COMPACT RECURRENT CURVED OBLIQUE RANK-TWO COMPONENT WITH `rho`, `|b|`, AND `|j_xi|` ALL BOUNDED ABOVE AND BELOW IS EXCLUDED. THE ONLY GENUINE RANK-TWO OBLIQUE SURVIVORS MUST USE MATERIAL TURNOVER, TRANSVERSE-AREA DEGENERATION `j_xi->0`, CURVATURE/PARALLEL EXIT, OR THE EXACT `j_xi=0` PURE-TRANSVERSE-KERNEL CLASS. GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Curvature–amplitude identity

M17-031 gives

\[
D_B\log|b|
=-\sigma-\frac12
\]

for

\[
b=(\xi\cdot\nabla)\xi.
\]

The CE-H raw vorticity amplitude satisfies

\[
D_B\log\rho
=\sigma+\kappa-1.
\]

Add the two laws:

\[
\boxed{
D_B\log(\rho|b|)
=\kappa-\frac32.
}
\]

The aligned strain cancels exactly.

---

## 2. Recurrent curved marker forces mean kappa = 3/2

Suppose on a marked material trajectory

\[
0<c_\rho\le\rho\le C_\rho<\infty
\]

and

\[
0<c_b\le|b|\le C_b<\infty
\]

through a recurrent long-time branch.

Then the logarithm of `rho|b|` has zero long-time drift, so

\[
\boxed{
\langle\kappa\rangle
=\frac32.
}
\]

This is the curved-vortex analogue of the M17-010 nodal Jacobian multiplier law.

It does not use director rank yet.

---

## 3. Nonzero transverse director area forces mean sigma = 1

On the subbranch

\[
j_\xi=J_\xi\cdot\xi\ne0,
\]

M16-025/M17-026 give

\[
D_B\log|j_\xi|
=\sigma-1.
\]

If

\[
0<c_j\le|j_\xi|\le C_j<\infty
\]

on the same recurrent material marker, then

\[
\boxed{
\langle\sigma\rangle=1.
}
\]

---

## 4. Recurrent raw vorticity then forces mean kappa = 0

The raw-amplitude law is

\[
D_B\log\rho
=\sigma+\kappa-1.
\]

Recurrent nonzero `rho` gives

\[
\langle\sigma+\kappa-1\rangle=0.
\]

Using

\[
\langle\sigma\rangle=1
\]

gives

\[
\boxed{
\langle\kappa\rangle=0.
}
\]

But Section 2 already gives

\[
\boxed{
\langle\kappa\rangle=\frac32.
}
\]

Contradiction.

Therefore

\[
\boxed{
\text{recurrent }(\rho,|b|,|j_\xi|)>0
\Longrightarrow\bot.
}
\]

---

## 5. Why this applies to the genuinely oblique transverse-rank-two branch

On full rank two, the kernel of `d xi` is generated by `J_xi`.
M17-031 gives

\[
(K_\xi\cdot\nabla)\xi
=-c\widetilde\rho\,b.
\]

When

\[
j_\xi\ne0,
\]

the transverse restriction

\[
A_\perp=d\xi|_{\xi^\perp}
\]

is invertible.
Thus on the genuinely oblique branch `K_xi!=0`, one has

\[
\boxed{b\ne0.}
\]

Hence the curvature observable required in Section 2 is intrinsic to this branch rather than an optional extra feature.

---

## 6. Exact recurrence trilemma

A same-marker oblique transverse-rank-two component cannot keep all three quantities

\[
\boxed{
\rho,
\qquad
|b|,
\qquad
|j_\xi|
}
\]

uniformly bounded above and below.

At least one must fail:

\[
\boxed{
\rho\text{-turnover/drift}
\ \lor\ 
|b|\to0/\infty
\ \lor\ 
|j_\xi|\to0/\infty.
}
\]

Inside the compact regular hard hull, unbounded derivative growth is disallowed, so the meaningful structural exits are

1. material turnover of the marked population;
2. `b -> 0`, approaching the parallel branch;
3. `j_xi -> 0`, losing transverse rank even if full rank two may temporarily remain.

---

## 7. The exact j_xi = 0 survivor

The contradiction above uses

\[
j_\xi\ne0.
\]

Therefore the structurally distinct class

\[
\boxed{
R_{2,j=0}^{pure-transverse-kernel}
}
\]

remains.

There

\[
J_\xi\perp\xi
\]

and the one-dimensional kernel of `d xi` lies entirely in the plane `xi^perp`.

This class cannot be treated by the `D_B log|j_xi|` recurrence law because `j_xi` vanishes identically.

---

## 8. Mean structure of the pure-transverse-kernel class

Even when `j_xi=0`, full rank two plus a transverse kernel forces curved vortex direction.
Indeed if

\[
b=(\xi\cdot\nabla)\xi=0
\]

and the transverse kernel direction also annihilates `xi`, then `d xi` would have at least a two-dimensional kernel, forcing rank at most one.
Thus

\[
\boxed{
j_\xi=0,\quad\operatorname{rank}d\xi=2
\Longrightarrow
b\ne0.}
\]

If `rho` and `|b|` recur on a marked trajectory, the universal curvature-amplitude law still gives

\[
\boxed{
\langle\kappa\rangle=\frac32.
}
\]

This is the canonical mean constraint inherited by the exceptional pure-transverse-kernel survivor.

---

## 9. DSD analysis

The same scalar `kappa` is reached through two independent descriptor chains:

### Curvature chain

\[
\rho
+ b
\Longrightarrow
\langle\kappa\rangle=\frac32.
\]

### Director-area chain

\[
\rho
+j_\xi
\Longrightarrow
\langle\sigma\rangle=1
\Longrightarrow
\langle\kappa\rangle=0.
\]

On the nonzero-transverse-area recurrent branch, the two descriptions are incompatible.

This is a DSD-style contradiction obtained by restoring two independently suppressed descriptors of the same material marker.

---

## 10. DSD audit

### Audit A — applying the contradiction to j_xi = 0
Rejected. The exceptional zero-projection branch is explicitly retained.

### Audit B — assuming every Eulerian recurrent core follows the same marker forever
Rejected. Material turnover remains a valid escape and must be audited separately.

### Audit C — deriving mean kappa = 3/2 without curvature recurrence
Rejected. Both `rho` and nonzero `|b|` must have zero logarithmic mean drift.

### Audit D — treating b=0 as possible oblique full rank two with a transverse kernel
Rejected by rank counting: together with the transverse kernel it would force `rank dxi<=1`.

### Audit E — proof status
A broad oblique Rank-2 recurrent subbranch is closed, not the entire Rank-2 branch.

---

## 11. Updated Rank-2 frontier

After M17-030 and M17-032,

\[
\boxed{
R_2
\Longrightarrow
T_{mat}^{(2)}
\ \lor\ 
T_{2\to1}
\ \lor\ 
I_{parallel}^{exit}
\ \lor\ 
R_{2,j=0}^{pure-transverse-kernel}.
}
\]

The complete persistent parallel branch is closed by M17-030.
The same-marker recurrent curved oblique branch with nonzero transverse director area is closed here.

The cleanest remaining intrinsic Rank-2 geometry is therefore

\[
\boxed{R_{2,j=0}^{pure-transverse-kernel}.}
\]

---

## 12. Next target

Let

\[
k:=J_\xi/|J_\xi|,
\qquad
n:=\xi\times k.
\]

On `j_xi=0`,

\[
(k\cdot\nabla)\xi=0
\]

while full rank two requires the two derivatives

\[
(\xi\cdot\nabla)\xi,
\qquad
(n\cdot\nabla)\xi
\]

to span `T_xi S^2`.

The next calculation should write the weighted harmonic-director and material-freezing equations in this canonical frame and determine whether the pure-transverse-kernel branch is dynamically compatible with the strain eigenline.

This is the **Pure Transverse Kernel Gate (PTKG)**.

---

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
