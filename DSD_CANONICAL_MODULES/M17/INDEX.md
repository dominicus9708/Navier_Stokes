# M17 — Material director geometry, weighted-harmonic rank, and nodal topology

Canonical continuation split from M16 after M16-023.

## Independent question
Once the M16 kappa/residence audit forces same-tube axial strain heterogeneity, can the CE-H material director field sustain a recurrent nontrivial spatial geometry under `D_B xi = 0` and the weighted harmonic-director equation?

## Canonical reassignment
- **M17-001** = provisional M16-024 — axial strain channels collapse to same-tube director geometry.
- **M17-002** = provisional M16-025 — frozen transverse director deformation and material director-area invariant.
- **M17-003** = provisional M16-026 — rank-one weighted-harmonic director collapses to a great-circle phase.
- **M17-004** = provisional M16-027 — great-circle branch reduces to a semilinear 2.5D streamfunction system.
- **M17-005** = provisional M16-028 — bounded phase lift is trivial; nonzero rank-one survivor requires winding around the vorticity zero set.

The provisional M16 source files are retained for provenance and are superseded by these canonical IDs.

## Native M17 continuation
- **M17-006** — great-circle winding becomes a complex real-potential Schrödinger nodal-defect problem `Delta f=kappa f`.
- **M17-007** — regular winding zeros have analytic `kappa` extension and are material nodal filaments.
- **M17-008** — axisymmetric Navier--Stokes without swirl is an exact known-regular firewall model for material great-circle winding.
- **M17-009** — topology-changing nodal events have uniformly finite analytic jet order and a nonzero jet floor on the compact hard hull.
- **M17-010** — regular winding core forces `spec Sigma={lambda,lambda,-2lambda}`; recurrent regular nodal Jacobian gives `⟨kappa⟩_nodal=3/2`, with slanted recurrence also forcing `⟨lambda⟩=0`.
- **M17-011** — the nodal skeleton is `|W|^2`-measure thin but its first jet obeys the exact third-jet law `Delta G=kappa_0 G`.
- **M17-012** — compactness transfers positive nodal `kappa` into a finite-radius positive sheath and forces a fixed negative-`kappa` payer.
- **M17-013** — the rank-one semilinear branch has a closed `(q,x_3)` material label flow whose divergence is exactly `kappa`; its label-area Jacobian is the M5 amplification factor.
- **M17-014** — normalized nodal-Jacobian shape, singular-value ratio and winding sign are material invariants.
- **M17-015** — a vertical nonconformal regular core forces `G_q=1` and `partial_3 lambda=0`.
- **M17-016** — angular defect `chi=Lq` obeys `Delta chi=kappa chi`; `chi identically 0` is the axisymmetric no-swirl firewall in the retained decay class.
- **M17-017** — `chi` and `psi=Lphi` satisfy the exact coupled material-elliptic shape/hysteresis system.
- **M17-018** — every nonzero finite-energy angular defect obeys `int kappa chi^2=-int|grad chi|^2<0`; the defect must use a negative-`kappa` payer.
- **M17-019** — the first nonzero angular-defect jet is a harmonic multipole; every positive-core defect lobe contains a same-lobe `kappa` sign reversal.
- **M17-020** — a bounded recurrent `chi` lobe requires strict signed boundary material turnover.
- **M17-021** — transverse `chi=kappa=0` intersections are dynamically compatible; tangency requires the exact compatibility `h=-lambda T`.
- **M17-022** — every bounded defect lobe has a spectral-gap negative payer `⟨kappa⟩_{chi^2}<=-lambda_1`.
- **M17-023** — simple `kappa=0` components are `q`-level contours; double zeros are radial extrema and tangency is a degenerate radial-extremum event.
- **M17-024** — slanted nodal slope `p=-G_h^{-1}G_3` obeys `D_Bp=3lambda p`; its azimuth is frozen and recurrent nonzero slant gives `⟨lambda⟩=0`.
- **M17-025** — a slanted nonconformal core survives only under the exact trace-free tensor alignment `TF[(p dot grad_h)H_phi]=-(G_q-1)Q_TF`.
- **M17-026** — rank two has a divergence-free director-area current `J_xi`; `J_xi` and rescaled vorticity `W/a` obey the same Cauchy transport law.
- **M17-027** — `Delta rho=(kappa+|grad xi|^2)rho` and `|grad xi|^2>=2|J_xi|`; rank-two amplitude maxima are strictly negative-`kappa` payers.
- **M17-028** — rank-two obliquity splits materially as `J_xi=c(W/a)+K_xi`; recurrent obliquity with recurrent transverse area gives mean strain frame `(1,1,-2)` and recurrent raw amplitude gives `⟨kappa⟩=0`.
- **M17-029** — the parallel rank-two branch is a straight-line congruence with `A'=-A^2`; complete smooth rank two requires twist-dominated no-real-focus transverse geometry.
- **M17-030** — the complete persistent parallel rank-two branch is excluded in the retained finite-energy/decay class: the strain eigenline plus line completeness forces `U=u xi`, while nonzero twist and `curl U=W` force `u=0`, contradicting activity.
- **M17-031** — vortex-direction curvature `b=(xi dot grad)xi` obeys `D_Bb=-(sigma+1/2)b`; together with `D_Bj_xi=(sigma-1)j_xi`, the product obeys `D_B(j_xi b)=-(3/2)j_xi b`.
- **M17-032** — recurrent curved oblique rank two with recurrent nonzero `rho`, `|b|`, and `|j_xi|` is excluded because curvature recurrence gives `⟨kappa⟩=3/2`, while director-area plus amplitude recurrence gives `⟨kappa⟩=0`.
- **M17-033** — on the exceptional full-rank-two class `j_xi=0`, with `k=J_xi/|J_xi|` and `n=xi×k`, the two nonzero jets `b=(xi dot grad)xi` and `a=(n dot grad)xi` obey scalar material multiplier laws. A recurrent same-marker branch has resonant mean frame `(-1/2,1,-1/2)` and `⟨kappa⟩=3/2`.
- **M17-034** — `Q=|j_xi||b|` satisfies the source-free Eulerian continuity law `partial_theta Q+div(BQ)=0`; recurrent oblique geometry therefore requires exact charge-carrying material turnover rather than internal regeneration.
- **M17-035** — the weighted-harmonic stress kernel projection reduces to geometric integrability. On the conformal pure-kernel subbranch the stress collapses and forces `(xi dot grad)rho=0` and `div xi=0`.
- **M17-036** — Euclidean flatness closes the complete conformal pure-kernel branch: its frame amplitude obeys `D_n lambda=2lambda^2` along a straight `n` line, so every nonzero solution blows up at finite signed distance.
- **M17-037** — pure-kernel anisotropy splits into a frozen target-angle `c=ahat dot bhat` and a strain-driven magnitude ratio `r=|a|/|b|`, with `D_B log r=sigma-sigma_n`. If `c!=0`, the marker has a strict conformal-defect floor and can never enter the conformal branch while regular.
- **M17-038** — on the orthogonal stretch class `a dot b=0`, weighted harmonic stress is diagonal. With `E=|a|^2+|b|^2` and `d=(|b|^2-|a|^2)/2`, the exact vortex-line law is `D_xi d=E div xi=-E D_xi log rho`; the transverse balance is signed but not monotone.

---

## Current director split

\[
\boxed{
B_{dir}
\Longrightarrow
R_2^{director-area}
\ \lor\ 
R_1^{great-circle/winding}.
}
\]

Global regularity remains unproved; the purpose of M17 is to reduce each survivor to explicit quantitative obligations or a known regular firewall.

---

## Rank one — current hard structure

The rank-one branch simultaneously carries:

\[
\boxed{
\begin{aligned}
&\operatorname{div}_{(q,x_3)}(H,K)=\kappa,\\
&J_L'=\kappa J_L,\\
&D_BG_h=(\kappa-3/2)G_h,\\
&D_Bp=3\lambda p,\\
&\Delta\chi=\kappa\chi.
\end{aligned}
}
\]

For every nontrivial bounded angular-defect lobe,

\[
\boxed{
\int_\Omega\kappa\chi^2=-\int_\Omega|\nabla\chi|^2<0
}
\]

and recurrent bounded lobes require strict signed boundary turnover. At positive recurrent winding-core phases, every core-emergent lobe contains

\[
\boxed{
\kappa>0\to\kappa=0\to\kappa<0.
}
\]

A nonvertical nonconformal nodal core must additionally remain on the tensor-alignment submanifold

\[
\boxed{
TF[(p\cdot\nabla_h)H_\phi]=-(G_q-1)Q_{TF}.
}
\]

Thus the main unresolved Rank-1 issues are persistent alignment, lobe/zero-network turnover, and whether the required M5 flux-weighted hysteresis can recur without finite-jet degeneration or reduction to the axisymmetric no-swirl firewall.

---

## Rank two — current hard structure

The director-area current satisfies

\[
\boxed{
\nabla\cdot J_\xi=0,
\qquad
(J_\xi\cdot\nabla)\xi=0,
\qquad
D_BJ_\xi=(\nabla B)J_\xi-\frac32J_\xi.
}
\]

The complete persistent parallel branch is closed by M17-030.
A same-marker recurrent curved oblique branch with nonzero transverse director area is closed by M17-032.
The remaining intrinsic full-rank-two class is concentrated on the pure-transverse-kernel geometry

\[
\boxed{
j_\xi=J_\xi\cdot\xi=0.}
\]

There

\[
\boxed{
(k\cdot\nabla)\xi=0,
\qquad
b=(\xi\cdot\nabla)\xi\ne0,
\qquad
a=(n\cdot\nabla)\xi\ne0.
}
\]

The target directions `ahat,bhat` are materially fixed. The intrinsic branch splits into

\[
\boxed{
R_{2,j=0}^{pure-kernel}
\Longrightarrow
R_{angle}^{frozen}
\ \lor\ 
R_{stretch}^{orthogonal}
\ \lor\ 
I_2^{turnover/interface}.
}
\]

### Frozen angular anisotropy
If

\[
c=\widehat a\cdot\widehat b\ne0,
\]

then `c` is materially invariant and

\[
\delta_{conf}\ge c^2>0.
\]

The same marker cannot enter the conformal class without rank loss or turnover.

### Orthogonal stretch anisotropy
If

\[
a\cdot b=0,
\qquad
|a|\ne|b|,
\]

then, with

\[
E=|a|^2+|b|^2,
\qquad
d=(|b|^2-|a|^2)/2,
\]

weighted harmonicity gives

\[
\boxed{
D_\xi d=-E D_\xi\log\rho.
}
\]

The conformal interface `d=0` is itself not a persistent complete escape: M17-036 proves that a complete nonzero conformal component has finite-distance Riccati blowup and therefore must exit through rank loss, anisotropy, or an interface before completion.

### Turnover ledger
For nonzero transverse director area and curvature,

\[
Q=|j_\xi||b|
\]

satisfies

\[
\boxed{
\partial_\theta Q+\nabla\cdot(BQ)=0.
}
\]

Hence a recurrent bounded oblique core cannot recreate area-curvature charge internally; it must service a material import/export ledger in parallel with the M5 vorticity-flux/hysteresis ledger.

---

## DSD audit conclusion through M17-038

The following shortcuts are explicitly rejected:

1. positive nodal `kappa` does not directly contradict the negative global weighted mean;
2. nonzero great-circle winding is not singular because axisymmetric no-swirl is a known regular firewall;
3. two directed zero-set currents are compatible while the zero surfaces remain transverse;
4. tensor alignment/codimension is not a contradiction unless the dynamics fails to preserve it;
5. two co-frozen Rank-2 fluxes can coexist smoothly;
6. pointwise `3/2` decay of area-curvature density is not charge destruction because material volume grows at exactly `3/2`;
7. the pure-kernel resonant mean frame is internally consistent;
8. the weighted-harmonic kernel projection is not independent—it reduces to geometric integrability;
9. anisotropy is not one scalar channel: target angle is frozen while stretch ratio is dynamical;
10. orthogonal stretch weighted harmonicity supplies an exact line law but no universal monotone sign.

The corresponding genuine closures are:

- complete persistent parallel Rank-2: **closed** by M17-030;
- same-marker recurrent curved oblique Rank-2 with nonzero transverse area: **closed** by M17-032;
- complete conformal pure-kernel Rank-2: **closed** by M17-036;
- axisymmetric no-swirl Rank-1 firewall: retained as known regular geometry.

---

## Highest-value next calculations

### Rank two
**OSLEG — Orthogonal Stretch Line-End Gate:** use

\[
D_\xi d=-E D_\xi\log\rho
\]

with complete-line decay/finite-enstrophy endpoint behavior, and determine whether a nonzero stretch defect can remain global without hitting the conformal/rank-loss/interface exits.

Then treat the irreducible frozen-angle class `c!=0` separately through its nonzero off-diagonal weighted-harmonic stress.

### Rank one
**Persistent slanted-alignment audit:** materially differentiate the slanted trace-free alignment condition and determine whether its codimension is dynamically invariant or produces another finite higher-jet constraint. In parallel, continue the lobe/double-zero turnover network toward a finite-jet tangency/reconnection or regular hysteresis classification.

The overall proof cannot be completed until both remaining Rank-1 and Rank-2 survivors are either excluded or reduced to known regular classes.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
