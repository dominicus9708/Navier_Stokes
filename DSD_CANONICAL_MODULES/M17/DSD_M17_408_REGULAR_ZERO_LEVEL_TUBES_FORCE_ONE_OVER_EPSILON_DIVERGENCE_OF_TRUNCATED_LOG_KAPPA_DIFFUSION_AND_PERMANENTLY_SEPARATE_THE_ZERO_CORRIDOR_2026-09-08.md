# DSD M17-408 — Regular zero-level tubes force `1/epsilon` divergence of truncated log-`kappa` diffusion and permanently separate the zero corridor

Date: 2026-09-08  
Canonical ID: **M17-408**

Status: **ACTIVE ZERO-CORRIDOR RIGIDITY / LOG-COORDINATE SINGULARITY AUDIT / M17-397 NEAR-ZERO SPLIT SHARPENING**

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Regular zero-tube input

On the compact high-amplitude CE-H branch, M17-343 considers the coefficient level density

\[
A_{\kappa\kappa}(k,\theta)
=
\int
\delta(k-\kappa)
\chi(\rho)\rho^2
|\nabla\kappa|^2dy.
\]

By coarea,

\[
A_{\kappa\kappa}(k,\theta)
=
\int_{\{\kappa=k\}}
\chi(\rho)\rho^2|\nabla\kappa|dS.
\]

On a uniformly regular zero-level tube, M17-343 proves

\[
\boxed{
A_{\kappa\kappa}(k,\theta)
\ge
 e^{-C_{tube}|k|}
A_{\kappa\kappa}(0,\theta)
}
\]

for

\[
|k|\le\delta_0.
\]

## 2. Truncated log-`kappa` diffusion

Fix

\[
0<\varepsilon<\delta\le\delta_0.
\]

Define the two-sided truncated log-diffusion charge

\[
\boxed{
D_{\log\kappa}^{\varepsilon,\delta}(\theta)
:=
\int_{\varepsilon\le|k|\le\delta}
 k^{-2}
A_{\kappa\kappa}(k,\theta)dk.
}
\]

By the delta-function representation, this is exactly

\[
\boxed{
D_{\log\kappa}^{\varepsilon,\delta}
=
\int_{\varepsilon\le|\kappa|\le\delta}
\chi(\rho)\rho^2
|\nabla\log|\kappa||^2dy.
}
\]

Thus no formal logarithmic coordinate is introduced at `kappa=0`; the integral is explicitly truncated away from zero.

## 3. Lower bound from zero-level thickening

For `|k|<=delta`,

\[
A_{\kappa\kappa}(k,\theta)
\ge
 e^{-C_{tube}\delta}
A_{\kappa\kappa}(0,\theta).
\]

Therefore

\[
\begin{aligned}
D_{\log\kappa}^{\varepsilon,\delta}(\theta)
&\ge
 e^{-C_{tube}\delta}
A_{\kappa\kappa}(0,\theta)
\int_{\varepsilon\le|k|\le\delta}k^{-2}dk\\
&=
2e^{-C_{tube}\delta}
A_{\kappa\kappa}(0,\theta)
\left(
\frac1\varepsilon-rac1\delta
\right).
\end{aligned}
\]

Hence

\[
\boxed{
D_{\log\kappa}^{\varepsilon,\delta}(\theta)
\ge
c_\delta
A_{\kappa\kappa}(0,\theta)
\left(
\frac1\varepsilon-rac1\delta
\right).
}
\]

If

\[
A_{\kappa\kappa}(0,\theta)>0,
\]

then

\[
\boxed{
D_{\log\kappa}^{\varepsilon,\delta}(\theta)
\to\infty
\qquad(\varepsilon\downarrow0).
}
\]

The divergence rate is at least `1/epsilon`.

## 4. Recurrent mean consequence

M17-342--343 provide on the retained regular-zero branch a positive recurrent mean

\[
\liminf_{T\to\infty}
\frac1T
\int_0^T
A_{\kappa\kappa}(0,\theta)d\theta
\ge a_0>0.
\]

Therefore

\[
\boxed{
\liminf_{T\to\infty}
\frac1T
\int_0^T
D_{\log\kappa}^{\varepsilon,\delta}(\theta)d\theta
\ge
c_\delta a_0
\left(
\frac1\varepsilon-rac1\delta
\right).
}
\]

Thus the recurrent truncated log-diffusion mean diverges as `epsilon -> 0`.

## 5. Interpretation: this is a coordinate singularity carrying real zero-level diffusion

The divergence is not a Navier--Stokes blow-up contradiction.

The underlying unweighted multiplier-diffusion density

\[
A_{\kappa\kappa}(k)
\]

remains regular and positive through `k=0`.

The divergence is generated by the singular weight

\[
k^{-2}
\]

introduced when passing from `grad kappa` to

\[
\nabla\log|\kappa|.
\]

Therefore one must not use the infinity of the log-coordinate charge itself as a physical contradiction.

Its valid information is that a regular zero corridor cannot be included in a finite global log-`kappa` budget.

## 6. Permanent branch separation

The coefficient-diffusion architecture must therefore be split permanently into

\[
\boxed{
\begin{aligned}
H_{coefficient\ diffusion}
\Longrightarrow{}&
H_{sign\text{-}preserving\ away\text{-}zero\ log\text{-}diffusion}\\
&\lor H_{regular\ zero\ corridor}\n&\lor G_{zero\ criticality/interface}.
\end{aligned}
}
\]

On the zero corridor, use the regular quantities

\[
A_{\kappa\kappa}(0),
\qquad
\mathcal C_{\Phi,-}^{0},
\qquad
\mathcal Q_0,
\]

from M17-323--346 rather than the singular log coordinate.

## 7. Consequence for M17-403/406 ancestry

M17-403's formal `R^{-1}` log-diffusion pullback can only be sought on a sign-preserving genealogy that remains separated from the zero corridor in the relevant normalized representation.

A single finite ancestral quantity of the form

\[
\int\rho^2|\nabla\log|\kappa||^2
\]

across all signs and regular zero crossings is not an appropriate target: M17-408 proves it is generically infinite on a regular active zero tube.

Therefore the missing log-`kappa` theorem is not

\[
\text{global finite log-diffusion total},
\]

but rather

\[
\boxed{
\text{finite/rigid sign-preserving away-zero genealogy}
\quad\text{or}\quad
\text{forced return to zero-level currency}.
}
\]

## 8. DSD audit role

The DSD role is to prevent a singular coordinate descriptor from being mistaken for a physical divergence theorem. The proof is coarea plus the M17-343 regular level-set thickening estimate.

## 9. Audit verdict

**PASS — regular zero crossings make global log-`kappa` diffusion intrinsically divergent, so zero-corridor and away-zero branches must remain separate.**

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
