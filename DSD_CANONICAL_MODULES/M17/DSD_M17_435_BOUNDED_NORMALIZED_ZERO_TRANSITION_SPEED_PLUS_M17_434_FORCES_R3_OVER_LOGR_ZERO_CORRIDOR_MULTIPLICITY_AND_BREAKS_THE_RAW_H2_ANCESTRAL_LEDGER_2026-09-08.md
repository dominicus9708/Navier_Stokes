# DSD M17-435 — Bounded normalized zero-transition speed plus M17-434 forces `R^3/log R` zero-corridor multiplicity and breaks the raw-H2 ancestral ledger

Date: 2026-09-08  
Canonical ID: **M17-435**

Status: **CONDITIONAL ZERO-TRANSITION CLOSURE / SIGN-FRAGMENTATION TO RAW-H2 HARMONIC DIVERGENCE**

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]

## 1. Setup

Work in one parent-normalized record with descendant own scale

\[
\boxed{r=R^{-1},\qquad R\gg1.}
\]

Assume a same-material positive-flux exact CE-H closed loop/tube occupies a parent-time set of fixed positive measure

\[
|J_R|\ge T_*>0.
\]

Away from coefficient-transition corridors, assume the retained branch has

\[
\boxed{c_\kappa r^{-2}\le|\kappa|\le C_\kappa r^{-2}.}
\]

Also retain the M17-413 parent-length loop, own-scale tube geometry, amplitude floor, positive flux floor, and representation-safe record genealogy.

If any of these fail, keep the corresponding scale/amplitude/flux/geometry/genealogy exit instead of using this module.

## 2. M17-434 bounds every sign-preserving phase

M17-434 proves that a sign-preserving own-scale coefficient phase with positive flux cannot last longer than logarithmically many own-time units across an infinite record sequence.

Thus, up to the exact series threshold, every surviving sign-preserving phase has duration

\[
\boxed{
\tau_{phase}
\lesssim
C_{sign}r^2\log R.
}
\]

A parent interval of order-one duration therefore requires at least

\[
\boxed{
N_{phase}
\gtrsim
c\frac{R^2}{\log R}
}
\]

separate own-scale sign phases unless a positive fraction of the parent time leaves the retained own-scale branch.

## 3. What separates two consecutive sign phases

Two consecutive retained phases cannot change sign while maintaining

\[
|\kappa|\ge c_\kappa r^{-2}
\]

continuously.

Therefore between them at least one of the following occurs:

\[
G_{zero\ corridor},
\]

\[
G_{coefficient\ scale\ exit},
\]

\[
G_{positive\ flux/amplitude\ loss},
\]

or a geometry/genealogy/interface/CE-H/domain exit.

The present module studies the branch where the transitions are genuine regular zero-corridor passages rather than one of the explicit exits.

## 4. Bounded normalized transition speed gives own-time corridor thickness

Assume on every retained zero transition

\[
\boxed{
r^4|D_t\kappa|\le S_*<\infty.}
\]

This is the natural own-scale normalization because `kappa ~ r^-2` and own time is `r^2`.

Fix a corridor threshold

\[
|\kappa|\le\delta r^{-2}
\]

with fixed `delta>0`.

A genuine sign change must traverse a coefficient distance at least

\[
2\delta r^{-2}.
\]

Since

\[
|D_t\kappa|\le S_*r^{-4},
\]

the time spent in the zero corridor is at least

\[
\boxed{
\Delta t_{zero}
\ge
\frac{2\delta}{S_*}r^2
=:c_0r^2.
}
\]

Thus every regular sign transition carries a fixed positive amount of own-time zero-corridor residence.

## 5. Record-matched regular transverse zero geometry

Assume during each corridor the zero level remains uniformly regular at the descendant scale:

\[
\boxed{
c_g r^{-3}
\le
|\nabla\kappa|
\le
C_g r^{-3},}
\]

with retained amplitude lower bound and tubular geometry on a parent-length loop arc.

M17-417 then gives

\[
N_x^{zero}\gtrsim cR
\]

bounded-overlap own-scale zero-corridor segments along the loop.

M17-409/417 give on each such segment a fixed normalized raw-`H2` payment over one unit own-time.

Because Section 4 supplies a fixed fraction of one own-time, each sign crossing pays

\[
\boxed{
h_{cross}^{norm}
\gtrsim
cR.
}
\]

The factor `R` is the spatial multiplicity along the parent-length zero loop.

## 6. Number of crossings

If the retained own-scale branch covers parent time `T_*` up to the zero corridors themselves and no scale/flux/geometry exit absorbs a positive fraction of the interval, the phase bound from Section 2 forces

\[
\boxed{
N_{cross}
\gtrsim
c\frac{R^2}{\log R}.
}
\]

Therefore the total normalized raw-`H2` zero-corridor charge in the record satisfies

\[
\boxed{
H_{R,zero}^{norm}
\gtrsim
c
\frac{R^3}{\log R}.
}
\]

## 7. Apply the cubic ancestry weight

M17-405 applies the record weight `R^-3`.

Hence

\[
\boxed{
R^{-3}H_{R,zero}^{norm}
\gtrsim
\frac{c}{\log R}.
}
\]

For geometric record scales

\[
R_m\asymp q^m,
\qquad q>1,
\]

we have

\[
\log R_m\asymp m.
\]

Therefore

\[
\boxed{
\sum_m\frac1{\log R_m}
=\infty.
}
\]

Thus infinitely many representation-safe records satisfying the hypotheses of M17-435 contradict the finite M17-405 ancestral raw-`H2` ledger.

## 8. Conditional closure statement

Under:

1. fixed positive parent-time occupancy;
2. own-scale `|kappa| ~ r^-2` away from crossings;
3. positive flux floor;
4. parent-length tubular geometry;
5. bounded normalized transition speed `r^4|D_t kappa|`;
6. record-matched regular transverse zero gradient `|grad kappa| ~ r^-3`;
7. amplitude retention;
8. representation-safe parent-to-record genealogy;

we obtain

\[
\boxed{\text{contradiction with M17-405}.}
\]

This is a genuine late-CE-H conditional closure theorem.

## 9. Exact surviving transition exits

A surviving sequence must therefore force at least one of:

\[
\boxed{G_{normalized\ transition\ speed\ decompactification}},
\]

\[
\boxed{G_{zero\ gradient/finite\ jet\ scale\ degeneration}},
\]

\[
\boxed{G_{coefficient\ own\text{-}scale\ exit}},
\]

\[
\boxed{G_{positive\ flux/amplitude\ thinning}},
\]

\[
\boxed{G_{tubular\ reach/chart\ loss}},
\]

or parent-to-record genealogy/interface/CE-H/domain loss.

Thus rapid sign fragmentation is no longer an untyped escape: if it remains regular in normalized space-time, the harmonic `1/log R` accumulation closes it.

## 10. Higher-order zero transitions

M17-435 uses a first-order record-matched transverse zero.

If `grad kappa` degenerates but a finite higher transverse/mixed jet remains record-matched, M17-418 provides the corresponding intrinsic scale. Extending the bounded-transition-speed counting argument to those higher finite jets is the next natural calculation.

Infinite spatial flatness remains governed by M17-419 only on the retained analytic compact state branch.

## 11. DSD role

DSD is used only to combine the independently certified currencies without double counting: sign-residence, material flux, zero-corridor geometry, spatial loop multiplicity, and ancestry weight.

The proof is ordinary ODE residence counting, continuity, M17-409/417 local PDE estimates, and the harmonic divergence of `1/log R_m` on geometric records.

## 12. Audit verdict

**PASS as a conditional regular-zero sign-fragmentation closure.**

The remaining hard transition branch is now normalized transition-speed or zero-jet/scale decompactification, not ordinary bounded-speed regular zero crossing.

\[
\boxed{\text{GLOBAL 3D NAVIER--STOKES REGULARITY REMAINS UNPROVED.}}
\]
