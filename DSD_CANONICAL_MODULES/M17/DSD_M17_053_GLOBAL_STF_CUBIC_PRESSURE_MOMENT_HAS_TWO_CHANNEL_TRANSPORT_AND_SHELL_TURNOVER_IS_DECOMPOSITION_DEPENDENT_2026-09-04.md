# DSD M17-053 — The global STF cubic pressure moment has a two-channel transport law; shell turnover is decomposition-dependent

Date: 2026-09-04
Canonical ID: **M17-053**

Status: **INTERNAL l=3 MOMENT-LOCKING DYNAMICS / THE HARMONIC CUBIC TENSOR OF M17-051--052 IS EXACTLY THE SYMMETRIC TRACE-FREE PART `H=STF_3(nabla^3P)`. BECAUSE `-Delta P=S_P`, THE TRACE OF `nabla^3P` IS `-grad S_P`, AND THE STF PROJECTION ADDS BACK THE UNIQUE `(1/5) sym(delta tensor grad S_P)` TRACE CORRECTION. DISTRIBUTIONALLY, `H` IS THE PRESSURE SOURCE PAIRED WITH THE PURE STF THIRD-DERIVATIVE NEWTONIAN KERNEL, WHICH HAS NO LOCAL TRACE/CONTACT TERM. ALONG A MATERIAL CORE CENTER `Y'=B(Y)`, THE GLOBAL l=3 MOMENT SATISFIES A TWO-CHANNEL LAW: SOURCE PRODUCTION `D_BS_P+(3/2)S_P` PLUS RELATIVE SOURCE TRANSPORT AGAINST THE STF KERNEL. THE MOVING-SHELL TERM OF M17-046 APPEARS ONLY AFTER AN ARTIFICIAL NEAR/FAR CUTOFF AND CANCELS WHEN THE PARTITION IS RECOMBINED. THUS RECURRENT l=3 LOCKING IS NOT A NEW INDEPENDENT TURNOVER LEDGER; IT IS A PARTICULAR ANGULAR MOMENT OF THE SAME GLOBAL PRESSURE-SOURCE PRODUCTION/TRANSPORT ARCHITECTURE. GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. The harmonic cubic as the STF pressure third jet

Let

\[
T_{ijk}:=\partial_{ijk}P.
\]

Because

\[
-\Delta P=S_P,
\]

its trace is

\[
T_{iik}
=\partial_k\Delta P
=-\partial_kS_P.
\]

For a symmetric rank-three tensor in three dimensions, the STF projection is

\[
\boxed{
(STF_3T)_{ijk}
=T_{ijk}
-\frac15\left(
\delta_{ij}T_{llk}
+\delta_{ik}T_{llj}
+\delta_{jk}T_{lli}
\right).
}
\]

Therefore

\[
\boxed{
\mathcal H_{ijk}
:=STF_3(\nabla^3P)_{ijk}
=\partial_{ijk}P
+\frac15\left(
\delta_{ij}\partial_kS_P
+\delta_{ik}\partial_jS_P
+\delta_{jk}\partial_iS_P
\right).
}
\]

By construction,

\[
\boxed{\mathcal H_{iik}=0.}
\]

---

## 2. Agreement with M17-051

M17-051 decomposes the local cubic pressure as

\[
P_3
=H_3
-\frac1{10}|x|^2(s\cdot x),
\qquad
s=\nabla S_P(0).
\]

Its particular third derivative is

\[
\partial_{ijk}P_3^{part}
=-\frac15(
\delta_{ij}s_k
+\delta_{ik}s_j
+\delta_{jk}s_i).
\]

Hence

\[
\partial_{ijk}P_3
+\frac15(
\delta_{ij}s_k
+\delta_{ik}s_j
+\delta_{jk}s_i)
=\partial_{ijk}H_3.
\]

Thus at the nodal core

\[
\boxed{
\mathcal H_{ijk}
=\partial_{ijk}H_3.
}
\]

The STF pressure third jet and the harmonic cubic tensor are exactly the same descriptor.

---

## 3. STF Newtonian kernel

Let

\[
G(z)=\frac1{4\pi|z|}.
\]

Form the rank-three STF kernel

\[
\boxed{
\mathcal K_{ijk}(z)
:=STF_3[\partial_{ijk}G(z)].
}
\]

Away from the origin,

\[
|\mathcal K(z)|\lesssim|z|^{-4}.
\]

The trace/contact distribution carried by raw derivatives of the Newtonian kernel is removed by the STF projection.
Therefore the harmonic cubic can be represented distributionally/principal-value-wise as

\[
\boxed{
\mathcal H_{ijk}(Y)
=\left\langle
S_P(y),\mathcal K_{ijk}(Y-y)
\right\rangle.
}
\]

This is the tensor form of the seven radial moment formulas in M17-052.

---

## 4. Material-center derivative

Let the marked nodal core move by

\[
\boxed{Y'=B(Y,\theta).}
\]

Define

\[
\mathcal D_Y
:=\partial_\theta+B(Y)\cdot\nabla_Y.
\]

Differentiate the global moment distributionally.
Use

\[
\partial_\theta S_P
=D_BS_P-B(y)\cdot\nabla_yS_P
\]

and integrate the transport term by parts.
Since

\[
\nabla\cdot B=\frac32,
\]

and

\[
\nabla_y\mathcal K(Y-y)
=-\nabla_Y\mathcal K(Y-y),
\]

we obtain

\[
\boxed{
\begin{aligned}
\mathcal D_Y\mathcal H_{ijk}
={}&\left\langle
D_BS_P+\frac32S_P,
\mathcal K_{ijk}(Y-\cdot)
\right\rangle\\
&+\left\langle
S_P,
(B(y)-B(Y))\cdot\nabla_y
\mathcal K_{ijk}(Y-y)
\right\rangle.
\end{aligned}
}
\]

Call the two terms

\[
\boxed{
\mathscr P^{(3)}_{ijk}
+\mathscr R^{(3)}_{ijk}.
}
\]

Thus

\[
\boxed{
\mathcal D_Y\mathcal H
=\mathscr P^{(3)}+\mathscr R^{(3)}.
}
\]

---

## 5. Source-production channel

The first term is

\[
\boxed{
\mathscr P^{(3)}
=\left\langle
D_BS_P+\frac32S_P,\mathcal K
\right\rangle.
}
\]

M17-046 already gives

\[
\begin{aligned}
D_BS_P+\frac32S_P
={}&2\Sigma:\Delta\Sigma
-\frac12|\Sigma|^2
-2\operatorname{tr}(\Sigma^3)
-2\Sigma:\Omega^2\\
&-2\Sigma:\nabla^2P
-\left(\sigma+\kappa-\frac14\right)\rho^2.
\end{aligned}
\]

Thus the cubic harmonic orientation is regenerated by the actual strain/vorticity/pressure source dynamics, not by an arbitrary harmonic coefficient.

---

## 6. Relative-transport channel

The second term is

\[
\boxed{
\mathscr R^{(3)}
=\left\langle
S_P,
(B(y)-B(Y))\cdot\nabla_y\mathcal K(Y-y)
\right\rangle.
}
\]

Since

\[
|\nabla\mathcal K(z)|\lesssim|z|^{-5}
\]

away from the core, this measures how the global pressure source moves relative to the marked nodal center.

It includes both deformation of the surrounding source architecture and translation/shear relative to the core.

---

## 7. Why the M17-046 shell term disappears globally

M17-046 introduced a moving cutoff `w_R` and obtained three terms:

\[
\mathcal P_3^{far}
+\mathcal T_3^{shell}
+\mathcal R_3^{rel}.
\]

If the complementary near contribution `1-w_R` is differentiated as well, it carries the opposite cutoff derivative.
Therefore

\[
\boxed{
\mathcal T_3^{shell,near}
+\mathcal T_3^{shell,far}=0.
}
\]

After recombination, the full moment has only the two physical channels of Sections 5--6.

Thus shell turnover is a valid bookkeeping term for a near/far decomposition, but not an independent conserved or dissipative mechanism of the global `l=3` moment.

---

## 8. DSAIG contraction

The harmonic contribution to DSAIG is

\[
\boxed{
N_{harm}
=TF_h[p_\ell\mathcal H_{\alpha\beta\ell}].
}
\]

The slant vector obeys

\[
D_Bp=3\lambda p,
\]

so its direction is materially frozen.
The normalized direction of `Q_0` is also materially frozen.

Hence

\[
\boxed{
D_BN_{harm}
=3\lambda N_{harm}
+TF_h[p_\ell(\mathcal D_Y\mathcal H)_{\alpha\beta\ell}]
}
\]

in the fixed great-circle frame.

Using the two-channel moment law,

\[
\boxed{
D_BN_{harm}
=3\lambda N_{harm}
+TF_h[p\lrcorner\mathscr P^{(3)}]
+TF_h[p\lrcorner\mathscr R^{(3)}].
}
\]

Thus recurrent harmonic screening requires source-production and relative-transport tensors to keep the contraction locked to the fixed nodal anisotropy line.

---

## 9. Connection with the radial l=3 ledger

M17-052 gives seven scalar moments

\[
A_{3m}(0)
=\frac17\int_0^\infty r^{-2}s_{3m}(r)dr.
\]

These seven coefficients are simply the coordinates of `mathcal H` in a basis of the seven-dimensional space

\[
STF^3(\mathbb R^3).
\]

Therefore the Newtonian tensor law and the radial spherical-harmonic law are equivalent descriptions of one state variable.

This is a DSD descriptor equivalence, not two independent constraints.

---

## 10. Far-tail estimate remains quantitative

M17-050 gives

\[
\|S_P\|_1\le Z_*.
\]

Consequently the portion of `mathcal H` generated outside radius `R` obeys

\[
\boxed{
|\mathcal H^{tail}(R)|
\le CZ_*R^{-4}.
}
\]

The corresponding higher transport kernel has one additional inverse power.
For source regions uniformly separated from the core, relative-transport tails can therefore be estimated with `R^-5` kernel decay times the relative velocity factor.

No global uniform velocity-moment bound is silently assumed here; only the tensor itself has the unconditional `R^-4` tail bound from M17-050.

---

## 11. DSD audit

### Audit A — treating near/far shell turnover as a new invariant cost
Rejected. It cancels between complementary partitions in the full moment.

### Audit B — counting radial l=3 moments and STF tensor as separate information
Rejected. They are coordinate representations of the same seven-dimensional tensor.

### Audit C — treating local Poisson trace data as part of the harmonic tensor
Rejected by the STF projection.

### Audit D — claiming the two-channel law fixes a sign
Rejected. Source production and relative transport are signed tensor channels.

### Audit E — proof status
The pressure-moment dynamics is structurally closed but no orientation contradiction is yet obtained.

---

## 12. Updated l=3 locking frontier

Persistent Rank-1 slanted alignment now requires the combined local equation

\[
\boxed{
P_{Q_0}^{\perp}
\left[
V_p-N_{part}-N_{harm}-\text{higher terms}
\right]=0,
}
\]

where

\[
N_{part}
=\frac{6\lambda}{5}
TF_h[p\otimes\nabla_h\Sigma_{33}
+\nabla_h\Sigma_{33}\otimes p],
\]

and the harmonic tensor evolves by

\[
\boxed{
\mathcal D_Y\mathcal H
=\mathscr P^{(3)}+\mathscr R^{(3)}.
}
\]

Thus `l=3` locking is not an independent mysterious pressure degree of freedom. It is one angular moment of the same global source-production and relative-transport architecture already underlying the pressure field.

---

## 13. Next target — perpendicular moment-production gate

Let `E_Q` be the unit direction in the two-dimensional horizontal trace-free tensor space orthogonal to `Q_0`.
Define the scalar DSAIG harmonic mismatch

\[
\boxed{
M_3
:=E_Q:N_{harm}.
}
\]

Because `E_Q` and the direction of `p` are material invariants on the regular slanted branch, the next calculation can write an exact scalar evolution for `M_3` from Sections 8--10.

The useful question is whether recurrent total alignment forces zero mean or sign conditions on the projected source-production/relative-transport terms that can be connected to the M5 payer/hysteresis ledger.

---

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
