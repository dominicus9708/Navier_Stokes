# DSD M5-508 — Enstrophy tightness upgrades the all-order bounded hull to global smooth compactness

Date: 2026-09-01

Status: **GLOBAL COMPACTNESS UPGRADE / M5-507 GIVES UNIFORM GLOBAL `H^m` BOUNDS FOR EVERY FIXED `m` ON THE BOUNDED-P BRANCH / IF THE SAME HULL IS UNIFORMLY TIGHT ONLY AT THE ENSTROPHY `L2` LEVEL, A SPATIAL CUTOFF AND SOBOLEV INTERPOLATION FORCE TIGHTNESS IN EVERY LOWER `H^s` NORM / LOCAL RELLICH COMPACTNESS PLUS THESE UNIFORM TAIL ESTIMATES THEN GIVE GLOBAL STRONG `H^s` PRECOMPACTNESS FOR EVERY FINITE `s`, HENCE GLOBAL `C^k` PRECOMPACTNESS FOR EVERY FIXED `k` / THEREFORE THE BOUNDED-P SURVIVOR SPLITS CLEANLY INTO REMOTE ENSTROPHY ESCAPE OR A GLOBALLY SMOOTH COMPACT SIMILARITY HULL / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Input from M5-507

On the bounded-palinstrophy corridor,

\[
\boxed{
\forall m<\infty,
\quad
\sup_{\mathbf Y\in\widehat{\mathfrak H}}
\|W_{\mathbf Y}\|_{H^m(\mathbb R^3)}
\le M_m<\infty.
}
\]

The constants may depend on `m`.

M5-496 already split the hull by enstrophy tightness.

The tight branch is

\[
\boxed{
\lim_{R\to\infty}
\sup_{\mathbf Y\in\widehat{\mathfrak H}}
\int_{|y|>R}|W_{\mathbf Y}(y)|^2dy
=0.
}
\]

M5-508 asks how much compactness follows from this one `L2` tail condition once all derivative orders are globally bounded.

---

## 2. Exterior cutoff

Choose a smooth radial cutoff `chi_R` such that

\[
\chi_R(y)=0
\quad\text{for }|y|\le R,
\]

\[
\chi_R(y)=1
\quad\text{for }|y|\ge2R,
\]

and for every derivative order `j`,

\[
|\nabla^j\chi_R|
\le C_jR^{-j}.
\]

For `R>=1`, all cutoff derivative factors are uniformly bounded.

Set

\[
F_R
:=
\chi_RW.
\]

Then

\[
F_R=W
\quad\text{on }|y|\ge2R.
\]

---

## 3. The cutoff `L2` norm tends to zero uniformly

Because `chi_R` is supported in the exterior of `B_R`,

\[
\|F_R\|_2^2
\le
\int_{|y|>R}|W|^2dy.
\]

Hence uniform enstrophy tightness gives

\[
\boxed{
\lim_{R\to\infty}
\sup_{\widehat{\mathfrak H}}
\|F_R\|_2
=0.
}
\]

---

## 4. Uniform high-Sobolev bound for the cutoff

Fix an integer `m>=1`.

By the Leibniz rule,

\[
\|\chi_RW\|_{H^m}
\le
C_m
\sum_{j=0}^m
\|\nabla^j\chi_R\,\nabla^{m-j}W\|_2.
\]

For `R>=1`, the cutoff derivatives are uniformly bounded, while M5-507 gives uniform global derivative bounds.

Therefore

\[
\boxed{
\sup_{R\ge1}
\sup_{\widehat{\mathfrak H}}
\|F_R\|_{H^m}
\le C_m'<\infty.
}
\]

The bound need not tend to zero; boundedness is enough for interpolation.

---

## 5. Interpolation upgrades `L2` tail tightness

Fix a real `s` with

\[
0<s<m.
\]

Sobolev interpolation gives

\[
\|F_R\|_{H^s}
\le
C
\|F_R\|_2^{1-s/m}
\|F_R\|_{H^m}^{s/m}.
\]

The high norm is uniformly bounded and the `L2` norm tends uniformly to zero.

Therefore

\[
\boxed{
\lim_{R\to\infty}
\sup_{\widehat{\mathfrak H}}
\|F_R\|_{H^s}
=0.
}
\]

Since `F_R=W` outside `B_(2R)`, this proves uniform tail smallness in every finite lower Sobolev order.

Because `m` can be chosen arbitrarily large, we obtain

\[
\boxed{
\forall s<\infty,
\quad
\text{the hull is uniformly }H^s\text{-tight}.
}
\]

---

## 6. Global strong Sobolev precompactness

Take any sequence of hull states

\[
W_n.
\]

Fix `s<infinity` and choose an integer

\[
m>s.
\]

On every fixed ball `B_(2R)`, the uniform `H^m` bound and Rellich compactness give a subsequence converging strongly in

\[
H^s(B_(2R)).
\]

The exterior `H^s` tail can be made uniformly arbitrarily small by first choosing `R` large.

Therefore the local strong convergence patches with the small tail to give

\[
\boxed{
W_{n_j}
\to W_*
\quad\text{strongly in }H^s(\mathbb R^3).
}
\]

Thus the hull is globally strongly precompact in every finite Sobolev order.

---

## 7. Diagonal all-order compactness

Apply the preceding argument successively for

\[
s=0,1,2,3,\dots
\]

and take a diagonal subsequence.

Then one obtains a subsequence converging strongly in every finite Sobolev norm.

Hence

\[
\boxed{
\widehat{\mathfrak H}
\text{ is precompact in the Fr\'echet topology generated by }
H^s,
\ s<\infty.
}
\]

This is a global, not merely local, compactness statement.

---

## 8. Global `C^k` compactness

Fix any integer `k>=0`.

Choose

\[
s>k+\frac32.
\]

Strong global `H^s` convergence implies, by Sobolev embedding,

\[
\boxed{
W_{n_j}
\to W_*
\quad\text{strongly in }C^k_b(\mathbb R^3).
}
\]

Therefore for every fixed `k`, the tight all-order hull is globally precompact in bounded `C^k` topology.

No uniform analytic/Gevrey radius is claimed.

---

## 9. Exact spatial dichotomy on the bounded-P branch

If uniform enstrophy tightness fails, M5-496 gives

\[
H_{tail}^{remote-E}.
\]

If uniform enstrophy tightness holds, M5-507--508 give global smooth compactness.

Thus

\[
\boxed{
\mathcal C_{Sob}^{all}
\Longrightarrow
H_{tail}^{remote-E}
\lor
\mathcal C_{smooth}^{global}.
}
\]

Here

\[
\boxed{
\mathcal C_{smooth}^{global}
:
\text{global strong precompactness in every fixed }H^s
\text{ and }C^k.
}
\]

---

## 10. Combined derivative/spatial frontier

M5-504 gave the unbounded-palinstrophy endpoint

\[
H_{tail}^{remote-Sob}.
\]

M5-507--508 give the bounded-palinstrophy alternatives.

Therefore the projected-diffusion component now satisfies

\[
\boxed{
\mathcal C_{ax+projdiff}
\Longrightarrow
H_{tail}^{remote-Sob}
\lor
H_{tail}^{remote-E}
\lor
\mathcal C_{smooth}^{global}.
}
\]

The first two branches are explicit spatial tail/escape defects.

The third branch has no remaining derivative or spatial compactness defect at any fixed finite Sobolev order.

---

## 11. Relation to the M5-485 invariant hull

M5-485 built a shift-invariant marked dilation hull with positive ratchet frequency.

On `C_smooth^global`, the observables built from finitely many derivatives and spatial integrals with controlled tails become continuous on a genuinely compact global phase space.

This is stronger than the earlier local compactness setting.

Therefore the strict-cocycle strategy from M5-485 can now be retried without the previous hidden possibility that the observable loses mass at spatial infinity.

This does not itself construct the cocycle.

It removes one major compactness obstruction to constructing it.

---

## 12. DSD audit

The compactness upgrade uses three distinct ingredients and must not conflate them:

1. **all-order global boundedness** from M5-507;
2. **only `L2` spatial tightness** from the non-remote-E branch of M5-496;
3. **local Rellich compactness** plus cutoff interpolation.

Without item 1, `L2` tightness would not control derivative tails.

Without item 2, all Sobolev norms can remain bounded while a packet translates to infinity.

Without item 3, boundedness alone is not compactness.

---

## 13. Highest-value next target

The globally smooth compact branch is now the cleanest place to revisit the M5-485 strict-cocycle obstruction.

A useful candidate should be

- globally continuous on `C_smooth^global`;
- invariantly bounded;
- sensitive to the positive-density projective-ratchet mark;
- and corrected by the positive axial-production ledger so that loops in orientation do not cancel the drift.

If no such scalar cocycle exists, the failure should itself be classifiable as a recurrent geometric cycle rather than another compactness defect.

The next calculation should therefore return to the dual axial/projective geometry on this now globally compact phase space.

---

## 14. Status

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
