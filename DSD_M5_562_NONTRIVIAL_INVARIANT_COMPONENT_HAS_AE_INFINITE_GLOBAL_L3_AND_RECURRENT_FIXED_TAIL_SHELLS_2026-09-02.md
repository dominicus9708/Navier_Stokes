# DSD M5-562 — Nontrivial invariant component has a.e. infinite global L3 and recurrent fixed tail shells

Date: 2026-09-02

Status: **MEASURE-THEORETIC TAIL RIGIDITY / ON ANY NONTRIVIAL ERGODIC COMPONENT OF THE TWO-SIDED COMPACT SIMILARITY HULL, THE SET OF STATES WITH FINITE GLOBAL `L3` HAS ZERO INVARIANT MEASURE / OTHERWISE INVERTIBLE POINCARE RECURRENCE PRODUCES A BACKWARD SIMILARITY-TIME SEQUENCE WITH UNIFORMLY BOUNDED GLOBAL `L3`, WHICH BECOMES A PHYSICAL ANCIENT SEQUENCE `t_k->-infinity` AND IS KILLED BY THE ALBRITTON--BARKER LIOUVILLE THEOREM / COMBINED WITH THE ANNULAR-MEAN TAIL REDUCTION, ALMOST EVERY NONTRIVIAL RECURRENT STATE THEREFORE HAS A DIVERGENT WEIGHTED DIRICHLET SHELL STACK / FOR EVERY REMOTE SHELL INDEX CUTOFF, SOME FIXED SHELL BEYOND THAT CUTOFF HAS A POSITIVE-MEASURE POSITIVE-AMPLITUDE EVENT SET AND HENCE RECURS IN BOTH TIME DIRECTIONS / THE OLD `RECURRENT CORE + COMPLETELY NONRECURRENT ESCAPING TAIL` LOOPHOLE IS NOT AVAILABLE ON A TYPICAL FULL-STATE RECURRENT ORBIT / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Compact two-sided similarity hull

Work on the compact complete similarity hull generated by the audited Type-I ancient family.

Denote it by

\[
\mathfrak H,
\]

with time-shift flow

\[
\sigma_t:\mathfrak H\to\mathfrak H,
\qquad t\in\mathbb R.
\]

A point

\[
Y\in\mathfrak H
\]

represents a complete smooth similarity solution

\[
U_Y(y,\theta),
\qquad
W_Y(y,\theta),
\]

with

\[
U_{\sigma_tY}(y,\theta)
=U_Y(y,\theta+t).
\]

Let `mu` be a shift-invariant ergodic probability measure supported on a nontrivial recurrent component carrying the retained active/core mark.

The flow is invertible because the hull consists of complete trajectories.

---

## 2. Global `L3` observable

Define the extended nonnegative observable

\[
\boxed{
F(Y)
:=
\|U_Y(\cdot,0)\|_{L^3(\mathbb R^3)}
\in[0,\infty].
}
\]

For fixed `R`,

\[
F_R(Y)^3
:=
\int_{B_R}|U_Y(y,0)|^3dy
\]

is continuous in the local smooth topology.

Moreover

\[
F(Y)^3
=
\sup_{R>0}F_R(Y)^3.
\]

Hence `F` is Borel measurable and lower semicontinuous as an extended-valued observable.

For each integer `M>=1`, define

\[
E_M
:=
\{Y\in\mathfrak H:F(Y)\le M\}.
\]

These are measurable sublevel sets.

---

## 3. Suppose a bounded-global-L3 level set has positive measure

Assume for contradiction that

\[
\mu(E_M)>0
\]

for some finite `M`.

Because the flow is measure preserving and invertible, Poincare recurrence applies to both `sigma_t` and its inverse.

Therefore for `mu`-almost every recurrent point

\[
Y\in E_M
\]

there exists a sequence

\[
t_j\to-\infty
\]

such that

\[
\boxed{
\sigma_{t_j}Y\in E_M.
}
\]

Equivalently,

\[
\boxed{
\|U_Y(\cdot,t_j)\|_3\le M.
}
\]

---

## 4. Convert negative similarity time to ancient physical time

For the complete similarity solution represented by `Y`, use the inverse transform

\[
s=-e^{-\theta},
\qquad
x=\sqrt{-s}\,y,
\]

\[
u_Y(x,s)
=(-s)^{-1/2}
U_Y\left(\frac{x}{\sqrt{-s}},-\log(-s)\right).
\]

At similarity time `theta=t_j`, the physical time is

\[
s_j=-e^{-t_j}.
\]

Since

\[
t_j\to-\infty,
\]

we have

\[
\boxed{s_j\to-\infty.}
\]

The `L3` norm is exactly Navier--Stokes scale invariant:

\[
\boxed{
\|u_Y(\cdot,s_j)\|_3
=
\|U_Y(\cdot,t_j)\|_3
\le M.
}
\]

Thus the physical mild ancient solution has a backward sequence with uniformly bounded global `L3` norm.

---

## 5. Apply ancient global-L3 Liouville rigidity

The Albritton--Barker ancient Liouville theorem states that a mild ancient Navier--Stokes solution with

\[
\sup_j\|u(\cdot,s_j)\|_3<\infty,
\qquad
s_j\downarrow-\infty,
\]

must be identically zero.

Therefore every recurrent point of `E_M` must represent

\[
\boxed{u_Y\equiv0}
\]

and hence

\[
U_Y\equiv0,
\qquad
W_Y\equiv0.
\]

---

## 6. Nontrivial ergodic component excludes the zero orbit

The zero solution is a fixed point of the similarity shift.

Therefore its singleton

\[
\{0\}
\]

is invariant.

On an ergodic component,

\[
\mu(\{0\})
\in\{0,1\}.
\]

The selected component carries the nontrivial recurrent core/record mark, so

\[
\mu(\{0\})\ne1.
\]

Hence

\[
\boxed{
\mu(\{0\})=0.
}
\]

But almost every point of `E_M` was forced to be zero.

Therefore

\[
\boxed{
\mu(E_M)=0
\qquad\forall M<\infty.
}
\]

Taking the countable union,

\[
\{F<\infty\}
=
\bigcup_{M=1}^\infty E_M,
\]

we obtain

\[
\boxed{
F(Y)=\infty
\quad\text{for }\mu\text{-a.e. }Y.
}
\]

In words:

\[
\boxed{
\|U_Y(0)\|_{L^3(\mathbb R^3)}=\infty
\quad\mu\text{-a.e. on every nontrivial ergodic component.}
}
\]

---

## 7. Use the full-tail annular reduction

The previous `ANCIENT_ANNULAR_MEAN_TAIL_REDUCTION` audit proved that annular means do not create an independent low-frequency escape.

For dyadic enlarged annuli `C_k` with radii

\[
R_k=2^kR_0,
\]

define

\[
e_k(Y)
:=
\int_{C_k}|\nabla U_Y(y,0)|^2dy
\]

and

\[
\boxed{
J_k(Y):=R_ke_k(Y).
}
\]

If `U_Y` is not globally `L3` at spatial infinity, then

\[
\boxed{
\sum_{k=1}^\infty J_k(Y)^{3/2}
=\infty.
}
\]

Therefore

\[
\boxed{
\sum_kJ_k(Y)^{3/2}=\infty
\quad\mu\text{-a.e.}
}
\]

on the nontrivial ergodic component.

---

## 8. Every remote tail contains a positive-measure fixed-shell event

Fix any shell cutoff `K`.

For almost every `Y`,

\[
\sum_{k\ge K}J_k(Y)^{3/2}=\infty.
\]

In particular, for almost every `Y`, there exists at least one

\[
k\ge K
\]

with

\[
J_k(Y)>0.
\]

For positive numbers,

\[
J_k(Y)>0
\Longrightarrow
\exists m\in\mathbb N:
\quad
J_k(Y)\ge\frac1m.
\]

Hence, modulo a null set,

\[
\mathfrak H
=
\bigcup_{k\ge K}
\bigcup_{m=1}^\infty
\left\{Y:J_k(Y)\ge\frac1m\right\}.
\]

Because this is a countable union of measurable sets with total measure one, at least one pair

\[
(k_*,m_*)
\]

with

\[
k_*\ge K
\]

satisfies

\[
\boxed{
\mu(A_{k_*,m_*})>0,
}
\]

where

\[
A_{k,m}
:=
\left\{Y:J_k(Y)\ge\frac1m\right\}.
\]

Thus **for every arbitrarily remote cutoff**, there exists a fixed finite shell beyond it whose positive weighted-Dirichlet activity has positive invariant measure.

---

## 9. Fixed remote shell recurrence

Apply Poincare recurrence to the positive-measure set

\[
A_{k_*,m_*}.
\]

For `mu`-almost every recurrent point `Y` in this set, there exist sequences

\[
t_j^+\to+\infty,
\qquad
t_j^-\to-\infty
\]

such that

\[
\sigma_{t_j^\pm}Y\in A_{k_*,m_*}.
\]

Equivalently,

\[
\boxed{
J_{k_*}(\sigma_{t_j^\pm}Y)
\ge
\frac1{m_*}
}
\]

at arbitrarily large positive and negative similarity times.

Therefore the same **fixed finite radius shell** is repeatedly active on a typical full-state recurrent orbit.

This is stronger than recurrence of the core alone.

---

## 10. What this removes

The old tail loophole was phrased as

\[
\text{locally recurrent active core}
+
\text{nonrecurrent critical tail escaping to infinity}.
\]

Such a description can still occur for a specially chosen nonrecurrent trajectory or subsequence.

But on the nontrivial invariant component used in the marked-hull argument, a typical recurrent full state cannot have **all** remote finite shells visited only once.

For every remote cutoff, at least one fixed shell beyond the cutoff returns infinitely often with a fixed positive weighted-Dirichlet threshold.

Thus the measure-theoretic hard core is instead

\[
\boxed{
\text{recurrent active core}
+
\text{recurrent fixed-radius critical-shell activity at arbitrarily remote scales}.
}
\]

---

## 11. Why this is not yet a contradiction

The passive critical similarity equation transports a shell outward in log radius.

Therefore the same material/spectral packet cannot simply remain forever at one fixed similarity radius.

However a fixed shell may recur because later activity is supplied from smaller radii.

That is a **historical replenishment** mechanism.

M5-562 has not yet proved that such replenishment necessarily incurs an already-typed nonrecyclable `H/T` cost.

It has only removed the possibility that a typical nontrivial invariant state avoids replenishment by allowing every finite tail shell to pass once and never return.

---

## 12. Relation to the amplitude-sensitive historical gate

The previous amplitude-sensitive historical audit proved

\[
\text{non-}L3\text{ tail}
\Longrightarrow
H
\lor T_{forget/rebuild}
\lor\text{pressure/residual}
\lor\text{persistent passive high-ratio tail}.
\]

Its final survivor was a passive persistent genealogy whose Hardy cost could be paid by ordinary viscosity.

M5-562 adds that, on a typical full-state recurrent component, this passive genealogy cannot be only a one-way set of shells disappearing from every fixed similarity radius.

It must repeatedly repopulate fixed finite shells.

Thus the surviving tail is sharpened to

\[
\boxed{
\text{persistent passive high-ratio genealogy}
+
\text{historical finite-radius replenishment}.
}
\]

---

## 13. Highest-value next target

The next task is now exact and local in log radius:

> Follow one critical shell under the exact dilation-characteristic Duhamel formula. Show that on the passive spectator branch a packet initially supported near radius `R` is transported to radius `R exp(Delta theta/2)` up to an integrable `O(R^-2)` scaled-profile error. Therefore recurrent activity of the same fixed shell after arbitrarily long times must be supplied from progressively smaller historical radii. Once the backward characteristic reaches the active core, quantify the required core-to-tail replenishment flux/derivative action and compare it with the existing finite-memory/turnover ledger.

This is the direct bridge between full-state recurrence and historical genealogy.

---

## 14. Firewall

The measure argument does **not** say every point of the topological hull is recurrent.

It applies to `mu`-almost every point of the selected nontrivial invariant component.

That is sufficient for an invariant-average contradiction strategy, but it must not be promoted to a pointwise statement about arbitrary hull elements.

Likewise, recurrence of a fixed shell does not yet identify which material labels repopulate it.

That is the next genealogy audit.

---

## 15. Status

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
