# DSD M5-137 — Integer-z Pressure-Multipole Sector in the Fuchsian Extension

Date: 2026-08-27

Status: **SCOPE-CORRECTED / THIS NOTE CLASSIFIES ONLY THE INTEGER-POWER `z=r^-2` PRESSURE SECTOR GENERATED BY THE PURE PARABOLIC TAIL-CORRECTION HIERARCHY / IT DOES NOT CLASSIFY THE FULL SAME-TAIL STRONG FIBER, WHICH MAY CONTAIN HALF-INTEGER POWERS `z^{m/2}` AND HENCE EVEN SPHERICAL MULTIPOLES / THE FULL CORRECTION IS M5-138 / GLOBAL REGULARITY UNPROVED.**

---

## 1. Correct scope

Assume only for this note that the leading pressure correction is expanded in integer powers

\[
\Pi(z,\eta,\theta)
\sim
\sum_{n\ge0}z^n\Psi_n(\eta,\theta),
\qquad z=r^{-2}.
\]

Then

\[
P_n=r^{-(2+2n)}\Psi_n.
\]

This integer-`z` sector is the sector naturally generated by the tail self-interaction / parabolic correction hierarchy of M5-135.

It is **not** the most general strong Fuchsian expansion.

---

## 2. Poisson operator in the integer sector

For `alpha=-(2+2n)`, the pressure Poisson coefficient equation has homogeneous part

\[
-\left[
\partial_\eta^2
-(4n+3)\partial_\eta
+(2n+2)(2n+1)
+\Delta_{S^2}
\right]\Psi_n=0.
\]

On a spherical harmonic of degree `ell`, the characteristic exponents are

\[
\lambda_+=2n+\ell+2,
\qquad
\lambda_-=2n+1-\ell.
\]

Two-sided boundedness in `eta` leaves a constant mode only when

\[
\boxed{\ell=2n+1.}
\]

Thus within the integer-`z` sector the bounded resonances are

\[
\boxed{\ell=1,3,5,7,\ldots.}
\]

---

## 3. Physical form

The resonant integer-sector term is

\[
P_n^{harm}
=r^{-(2n+2)}Y_{2n+1}(\theta),
\]

which is harmonic on the punctured space.

For `n=0` this is the critical `ell=1` pressure dipole of M5-133.

M5-134 shows that this leading dipole is fixed by the canonical tail factor.

---

## 4. Correction to the original interpretation

The original version of M5-137 incorrectly promoted the integer-`z` result to a classification of the entire same-tail strong fiber.

That promotion is invalid.

A strong quotient may contain terms with half-integer powers such as

\[
z^{1/2},\ z^{3/2},\ldots,
\]

because an ordinary compact/core pressure field can generate, for example,

\[
r^{-3}=r^{-2}z^{1/2}.
\]

These half-integer sectors carry even spherical harmonic multipoles and are compatible with strong pressure differences.

Therefore

\[
\boxed{
\text{odd multipoles only}
}
\]

is RED as a claim about the full fiber.

---

## 5. DSD correction audit

### Formation

The error arose by treating one generated asymptotic subspace as the whole strong correction space.

### Axis

Integer parabolic order and general radial homogeneity are now separated.

### Static aggregation

The integer-sector resonance list is retained as a valid subset, not discarded.

### Dynamics

No statement about full fiber uniqueness or nonuniqueness is inferred from this restricted list.

### Cross-audit

M5-138 provides the complete harmonic multipole indexing.

---

## 6. Retained valid result

Within the pure integer-`z` tail-generated hierarchy,

\[
\boxed{
\ker_{bounded,n}=\mathcal H_{2n+1}(S^2).
}
\]

This remains correct and useful, but only with the scope above.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]