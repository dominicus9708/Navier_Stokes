# Type-I Pressure Bound from the Fixed-Center Velocity Tower — 2026-08-20

Overall status: **PRESSURE SIDE OF THE COMPACTNESS BRIDGE REDUCED — GLOBAL REGULARITY NOT PROVED.**

This note shows that, in the fixed limiting-center frame, the standard CKN pressure quantity is controlled by the Type-I velocity tower. The acceleration-induced affine pressure was already removed by using a fixed center for compactness. The remaining genuine whole-space pressure can be estimated by a near Calderon--Zygmund part plus dyadic remote harmonic tails.

---

## 1. Canonical pressure and local split

In the fixed-center rescaled variables use the canonical whole-space pressure, modulo an arbitrary function of time,

\[
P=\mathcal R_i\mathcal R_j(U_iU_j),
\]

so

\[
-\Delta P=\partial_i\partial_j(U_iU_j).
\]

Fix a unit observation ball. Split

\[
P=P_{near}+\sum_{k\ge2}P_k,
\]

where `P_near` is generated by `U \otimes U` in a fixed multiple of the unit ball and `P_k` is generated by the annulus

\[
A_k=\{2^k<|z|<2^{k+1}\}.
\]

Only pressure oscillation enters the CKN quantity, so for every remote shell subtract the spatial constant `P_k(0,t)`.

---

## 2. Near pressure

Calderon--Zygmund gives, on a fixed enlarged ball,

\[
\|P_{near}\|_{L^{3/2}_x}
\lesssim
\|U\|_{L^3_x}^2.
\]

After integrating over a unit parabolic interval,

\[
\boxed{
D_{near}(1)\lesssim C(c)
}
\]

where `C(c)` denotes the scale-critical cubic velocity quantity on a fixed parent cylinder. Thus bounded Type-I cubic velocity controls the near pressure.

---

## 3. Remote pressure oscillation

The pressure kernel has homogeneity `-3`, so its gradient has homogeneity `-4`. For `|y|<=1` and `z in A_k`,

\[
|K(y-z)-K(-z)|
\lesssim
|y|\,|z|^{-4}
\lesssim
2^{-4k}.
\]

Therefore

\[
|P_k(y,t)-P_k(0,t)|
\lesssim
2^{-4k}
\int_{A_k}|U(z,t)|^2dz.
\]

If the parent-ball Type-I energy is uniformly bounded,

\[
A(R)=R^{-1}\sup_t\int_{B_R}|U|^2\le A_*,
\]

then

\[
\int_{A_k}|U|^2dz
\le
\int_{B_{2^{k+1}}}|U|^2dz
\lesssim
A_*2^k.
\]

Hence

\[
\boxed{
|P_k(y,t)-P_k(0,t)|
\lesssim
A_*2^{-3k}.
}
\]

The dyadic series is absolutely summable.

---

## 4. Remote contribution to D

Summing the shell oscillations gives on the unit ball

\[
|P_{far}(y,t)-P_{far}(0,t)|
\lesssim A_*.
\]

Thus over a unit parabolic cylinder,

\[
\boxed{
D_{far}(1)\lesssim A_*^{3/2}.
}
\]

By Navier--Stokes scaling, at arbitrary radius `R`,

\[
\boxed{
D(R)
\lesssim
C(cR)+A_*^{3/2},
}
\]

with a fixed enlargement factor `c>1` in the near term. Equivalently, a standard harmonic-pressure recursion may be used; the dyadic shell proof makes the remote Type-I summability explicit.

---

## 5. Consequence

If the fixed-center first-hitting tower has

\[
\sup_R A(R)<\infty,
\qquad
\sup_R C(R)<\infty,
\]

then

\[
\boxed{
\sup_R D(R)<\infty.
}
\]

Thus the pressure quantity is not an independent obstruction once the fixed-center velocity tower is Type-I controlled.

Combined with the local interpolation

\[
C\lesssim A^{3/4}E^{3/4}+A^{3/2},
\]

the compactness burden is reduced primarily to `A` and `E`; `C` follows from `A,E`, and `D` follows from `A,C`.

---

## 6. Remaining caveat: off-center subcylinders

The Albritton--Barker Type-I quantity takes a supremum over all parabolic subcylinders in a neighborhood, not only the centered tower. The estimates above control the nested cylinders centered at the limiting singular point. To obtain the full local Type-I quantity, one must additionally rule out secondary off-center bad cylinders. In the present proof tree, repeated secondary concentration/multicore activity is naturally classified as turnover `T`; this needs a quantitative definition rather than being assumed away.

Status: **FIXED-CENTER PRESSURE D IS CONTROLLED BY TYPE-I VELOCITY A,C. THE REMAINING COMPACTNESS ISSUE IS UNIFORM CONTROL OF A,E ON ALL RELEVANT SUBCYLINDERS, WITH OFF-CENTER SECONDARY BADNESS TO BE ROUTED INTO T/H.**