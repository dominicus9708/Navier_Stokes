# DSD Finite-Memory Replacement -> Positive-Frequency Costed Exit

Date: 2026-08-25

Status: **FINITE MULTIFLUX MEMORY CONVERTED INTO POSITIVE EVENT-FREQUENCY OF COSTED EXITS / FINITE PIGEONHOLE GIVES ONE REPEATED EXIT TYPE / LOCAL QUIET MULTIFLUX NO LONGER A LONG-TIME SURVIVOR / EXPORT/ESCAPING-TAIL AND CONSTANT COMPARISONS REMAIN / GLOBAL REGULARITY UNPROVED.**

## 1. Purpose

`DSD_COHERENT_MULTIFLUX_ENSTROPHY_MULTIPLICITY_CAP_2026-08-25.md` proves that a bounded normalized core can store at most

\[
N_{max}<\infty
\]

coherent same-sign fixed-flux material populations.

Therefore repeated fixed-fraction replacement cannot remain forever in the local `T_multi-flux` storage mode.

This note combines that finite memory with the already proved positive-density finite partition of recurrent fixed-age witness times.

The result is a combinatorial but quantitative conclusion: if replacement survives on a positive-density recurrent set, then one of the genuinely costed exits must occur with positive event frequency.

---

## 2. Fixed-age recurrent replacement set

Fix the finite shell age `k` selected by the recurrent shell extraction.

Let `T_rep` denote the recurrent-time subset on which the coherent low-contact/replacement branch occurs.

Assume

\[
\boxed{
\underline d(T_{rep})=d_{rep}>0.
}
\]

Every such event is associated with a fixed-age backward comparison window whose Leray length is bounded by

\[
\boxed{
S_{fix}(k)<\infty.
}
\]

Choose a maximal set of replacement event times separated by more than `S_fix(k)`.

The standard maximal-packing argument gives, over a long Leray interval of length `S`, a selected event count

\[
\boxed{
N_{rep}(S)
\ge
\frac{d_{rep}}{2S_{fix}(k)}S-o(S).
}
\]

Thus the selected replacement events have positive asymptotic event rate

\[
\boxed{
\rho_{rep}
\ge
\frac{d_{rep}}{2S_{fix}(k)}>0.
}
\]

Status: **PROVED by the same separated-event packing used in the diffusion-erosion ledger.**

---

## 3. Finite-memory rule

On the coherent no-export/no-destruction/no-projective corridor, each uncompensated replacement adds a distinguishable stored material flux population.

The multiplicity theorem gives the finite storage capacity

\[
N_{max}.
\]

Therefore any chronological block containing

\[
\boxed{N_{max}+1}
\]

selected replacement events must contain at least one exit from quiet local multiflux storage.

The possible typed exits are

\[
\boxed{
\begin{aligned}
X_1&=\text{robust viscous flux change},\\
X_2&=\text{projective/directional reorganization},\\
X_3&=\text{material flux export},\\
X_4&=\text{noncoherent derivative/Betchov/H exit},\\
X_5&=\text{loss of bounded-Z/thick corridor}.
\end{aligned}
}
\]

If one works inside the corridor where `X_5` is excluded by hypothesis, there are only four costed/geometric exit types.

Status: **PROVED from the finite multiplicity cap.**

---

## 4. Exit-event rate lower bound

Partition the first `N_rep(S)` selected replacement events into consecutive blocks of size at most

\[
N_{max}+1.
\]

Every complete block contains at least one typed exit.

Hence the number `N_exit(S)` of costed exits obeys

\[
N_{exit}(S)
\ge
\frac{N_{rep}(S)}{N_{max}+1}-1.
\]

Divide by `S` and let `S -> infinity`.

Then

\[
\boxed{
\underline\rho_{exit}
\ge
\frac{\rho_{rep}}{N_{max}+1}
\ge
\frac{d_{rep}}
{2S_{fix}(k)(N_{max}+1)}
>0.
}
\]

Thus positive-density replacement forces a positive **event frequency** of genuine exits.

Status: **PROVED.**

---

## 5. Finite pigeonhole over exit type

There are finitely many exit types.

Let `m_X` be the number retained in the corridor under consideration; normally `m_X<=4` after excluding loss of the bounded/thick hypotheses as a separate complement branch.

At least one exit type `X_*` occurs with asymptotic event rate

\[
\boxed{
\underline\rho_{X_*}
\ge
\frac1{m_X}
\underline\rho_{exit}
\ge
\frac{d_{rep}}
{2m_XS_{fix}(k)(N_{max}+1)}
>0.
}
\]

Therefore the proof cannot be evaded by cycling among viscous, projective, export, and noncoherent exits so that every individual mechanism occurs only finitely or at zero event frequency.

At least one typed mechanism recurs quantitatively.

Status: **PROVED.**

---

## 6. Consequence for viscous-flux exits

Suppose `X_*` is robust viscous flux change.

The smooth thick-core flux/enstrophy gate supplies a fixed normalized palinstrophy lower bound on every such stage/window under the bounded-deformation/tightness hypotheses.

Because the selected replacement events were separated by more than the common fixed-age window length, one can pass to a disjoint subfamily of the corresponding flux-change windows with only another fixed multiplicative loss in event rate.

Therefore a positive-frequency viscous-flux survivor produces a positive recurrent mean palinstrophy floor.

This floor can be compared with the existing recurrent mean-`Q` ceiling.

The comparison is now a finite constant test, not a qualitative recurrence gap.

Status: **REDUCED TO CONSTANT COMPARISON; no universal inequality asserted here.**

---

## 7. Consequence for projective exits

Suppose `X_*` is projective/directional reorganization.

The transverse-axis action gate and smooth projective-action closure give, on every pure projective event,

\[
\frac{L_I}{2}+\mathscr A_{shape}
\ge
\frac\pi2
\]

and an explicit lower bound on

\[
\int_I\lambda ds.
\]

A positive event rate therefore yields a positive recurrent projective/frequency action rate.

On the bounded pure corridor this enters the telescoping `H1` ledger already used to S-close sufficiently tight projective corridors.

Thus the new finite-memory argument supplies precisely the recurrence frequency that the projective multistage ledger requires.

Status: **BRIDGE PROVED / final numerical corridor comparison inherited from the projective closure.**

---

## 8. Consequence for noncoherent/H exits

Suppose `X_*` is loss of coherent common-axis geometry through derivative/eigenaxis bending/Betchov residual activity.

Then the event leaves the quiet geometric turnover lane and re-enters the already typed `H`, buffer-strain, palinstrophy, or Betchov residual ledgers.

Positive event frequency prevents this from being treated as a sparse exceptional correction.

Its quantitative closure depends on which already-defined `H` subbranch receives the events.

Status: **ROUTED, not newly closed.**

---

## 9. Consequence for export exits

Suppose `X_*` is material flux export.

Then old fixed-flux populations leave the bounded recurrent core at positive event frequency.

There are two possibilities:

1. exported flux repeatedly re-enters/replenishes similarity scales, giving historical recycling and the existing `H_remote/T` routing;
2. exported flux does not return and drifts to similarity infinity, giving the nonrecurrent escaping passive critical-tail topology.

Thus positive-frequency export collapses the local turnover problem directly onto the already isolated global tail frontier.

Status: **PROVED ROUTING.**

---

## 10. Local T_multi is no longer a long-time terminal branch

The sequence

\[
\text{replacement}
\to
\text{store another old/new flux population}
\to
\text{replacement}
\to\cdots
\]

cannot continue indefinitely in a bounded coherent core.

After finitely many uncompensated replacements, a costed exit is mandatory.

With positive-density recurrent replacement, mandatory exits themselves recur at positive event frequency.

Therefore

\[
\boxed{
T_{multi-flux}
\text{ is removed as an indefinitely quiet recurrent terminal branch.}
}
\]

It remains only as finite memory between costed events.

---

## 11. Updated long-time turnover frontier

On the bounded coherent recurrent core, the positive-density replacement branch reduces to

\[
\boxed{
T_{rep,+dens}
\Longrightarrow
X_{visc,+freq}
\lor
X_{proj,+freq}
\lor
X_{export,+freq}
\lor
X_{H,+freq}.
}
\]

At least one of these four must survive with a positive event rate bounded below explicitly in terms of

\[
d_{rep},
\quad S_{fix}(k),
\quad N_{max}.
\]

This is substantially sharper than the previous statement that replacement merely had to "pay something eventually."

---

## 12. Relation to the full D/R/T tree

The current recurrent endgame is now organized as follows:

### Mean diffusion D

\[
D_{mean}
\Longrightarrow
\text{hyperpalinstrophy charge }H_{mean}(k)\asymp q^{-k/2}.
\]

### Material return R

Global recurrent critical-halo return routes to historical replenishment; nonrecurrent return merges with escaping critical tail.

### Replacement T

Positive-density replacement cannot remain local multiflux storage. It forces a positive-frequency viscous/projective/export/H exit.

Thus the main unresolved global topology is increasingly concentrated in

\[
\boxed{
\text{positive-frequency export}
\to
\text{escaping/nonrecurrent critical tail}
}
\]

plus finite constant comparisons for the already costed local exits.

---

## 13. Audit verdict

### PROVED

- positive-density fixed-age replacement yields a positive separated-event rate;
- finite multiflux memory forces at least one genuine exit per `N_max+1` replacement events;
- genuine exits inherit a positive event frequency;
- finite exit-type pigeonhole forces one typed mechanism to recur at positive event frequency;
- local coherent multiflux storage is not an indefinitely quiet recurrent survivor.

### REDUCED TO EXISTING LEDGERS / CONSTANT TESTS

- repeated viscous-flux exit;
- repeated projective exit;
- repeated noncoherent/H exit.

### STILL OPEN

- positive-frequency export when flux escapes permanently to similarity infinity;
- universal numerical closure of all costed local exit comparisons;
- residual all-age tightness for the mean-diffusion branch;
- global regularity.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
