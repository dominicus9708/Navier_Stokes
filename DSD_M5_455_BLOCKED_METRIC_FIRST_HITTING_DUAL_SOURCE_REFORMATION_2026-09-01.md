# DSD M5-455 — Blocked metric first-hitting dual-source reformation

Date: 2026-09-01

Status: **UNIFORM METRIC DISTORTION ONLY CHANGES THE GENERATION BLOCK SIZE / AFTER A FIXED NUMBER OF ORIGINAL FIRST-HITTING STAGES THE PULLED-BACK VORTICITY MUST GROW BY A FIXED FACTOR, HENCE PAY FIXED LONGITUDINAL LOCAL-STRAIN ACTION / THE M5-454 ANGULAR DEPLETION THEN FORCES TRANSVERSE VORTICITY CONTENT; OUTSIDE A REMOTE/DERIVATIVE-THROUGHPUT EXIT THIS CONTENT THICKENS INTO A COHERENT MISALIGNED NATURAL COMPANION PACKET / THUS THE DUAL-SOURCE GEOMETRY REFORMS INSIDE EVERY QUIET UNIFORMLY ELLIPTIC BLOCK / GLOBAL REGULARITY REMAINS UNPROVED.**

Assume

\[
\|F\|+\|F^{-1}\|\le K_F
\]

on the uniformly elliptic branch. Choose a fixed integer `L_kappa` such that

\[
\boxed{q^{L_\kappa}>2K_F^2.}
\]

Since `Omega=F eta`, across every block of `L_kappa` original first-hitting generations,

\[
\frac{\|\eta_{end}\|_\infty}{\|\eta_{start}\|_\infty}
\ge K_F^{-2}q^{L_\kappa}>2.
\]

M5-453 therefore gives a fixed block action

\[
\boxed{
\int_{block}
\sup_x\lambda_{max}(S_w)dt
\ge \log 2.
}
\]

The block duration in normalized variables is bounded above by a constant depending only on `L_kappa` and the first-hitting stage-length bound. Hence at some block time `t_*` there is a vorticity maximum point `x_*` and its direction `e_*` for which the longitudinal stretching has a fixed positive size, unless the block has already entered the strong time/strain interface branch.

On the inherited M5-392 analytic corridor, bounded linear distortion by `F` preserves fixed-order normalized spatial derivative bounds for `eta`. The corresponding strain thickness audit has the same two outcomes as M5-377:

1. the local strain/source continuity scale remains bounded below; or
2. continuity is destroyed by derivative/remote harmonic throughput, already typed as strong `H`.

On outcome 1, a fixed natural ball around the productive point carries a fixed `L2` amount of longitudinal strain. Apply M5-454 with `e=e_*`:

\[
\|e_*^TS_we_*\|_2
\le C_\kappa
\|(I-e_*\otimes e_*)\eta\|_2.
\]

Therefore

\[
\boxed{
\|(I-e_*\otimes e_*)\eta\|_{L^2(B_R)}
\ge c_\kappa>0
}
\]

for some fixed normalized window `B_R`, unless a fixed part of the transverse mass lies outside every such window, which is the remote/delocalized enstrophy branch.

Inside a fixed window, the first-hitting amplitude cap and analytic derivative bound turn this `L2` floor into a point with fixed transverse amplitude and then a fixed-radius coherent ball. Its vorticity direction differs from `e_*` by a fixed angle depending only on the ellipticity and first-hitting constants.

Because `det F=1`, the corresponding directed vorticity flux remains scale critical. Returning to physical units gives a companion flux of order

\[
\boxed{\Phi_{comp}\asymp\nu}
\]

up to fixed metric-comparability constants.

Thus the quiet bounded-metric block satisfies

\[
\boxed{
N_{metric}^{elliptic}
\Longrightarrow
G_{dual\ flux}^{metric}
\lor
H_{remote/derivative}^{strong}.
}
\]

Firewall: this note reforms the dual-source geometry but does not yet import the isotropic ancient Liouville theorem into the time-dependent metric system. The genealogy and eventual rigidity of the metric dual pair still require a separate audit.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]