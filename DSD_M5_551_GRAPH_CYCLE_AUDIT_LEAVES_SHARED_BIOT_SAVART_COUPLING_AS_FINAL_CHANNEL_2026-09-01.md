# DSD M5-551 — Graph-cycle audit: finite lineage topology alone cannot close; shared Biot--Savart coupling is the missing channel

Date: 2026-09-01

Status: **GRAPH-LEVEL NO-GO / ATTACHING ALL CURRENTLY AUDITED COSTS TO THE M5-498 FINITE LINEAGE GRAPH DOES NOT FORCE A POSITIVE-EXCESS EDGE ON EVERY RECURRENT CYCLE / AN ABSTRACT TWO-LINEAGE RECURRENT CONFIGURATION CAN SATISFY FIXED NONCOLLINEAR DUAL GEOMETRY, ZERO SIGNED FLUX DRIFT, NONDEGENERATE AMPLITUDE COBoundaries, ANCHORED TRANSVERSE STRAIN-DIFFUSION BALANCE, AND THE REQUIRED AXIAL STRAIN THRESHOLDS WITHOUT ANY GRAPH-STORAGE CONTRADICTION / THEREFORE THE FINITE GRAPH HAS DISCARDED THE LAST ESSENTIAL PDE INFORMATION: ALL LINEAGE STRAINS AND DIFFUSIVE TERMS ARE GENERATED BY ONE COMMON GLOBAL VORTICITY FIELD THROUGH THE NAVIER--STOKES/BIOT--SAVART OPERATORS / THE NEXT RIGIDITY LEMMA MUST USE THIS SHARED NONLOCAL COUPLING, NOT GRAPH TOPOLOGY ALONE / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Input finite graph

M5-498 reduced the quiet tight genealogy to a finite persistent lineage network

\[
\mathcal L
=
\{L_1,\dots,L_N\},
\qquad
N\le N_{max}.
\]

Mean transfer flow decomposes into recurrent directed cycles.

M5-543 localizes all positive active marks to one finite core.

M5-547--550 attach the following audited event costs:

- axial first-hitting strain floor;
- pair-motion/projective cost;
- anchored near-recycling tube;
- nondegenerate amplitude coboundary;
- migration surface-current cost;
- zero signed material-flux drift.

The question is whether these labels alone force one nonrecyclable edge on every directed cycle.

---

## 2. Abstract zero-excess two-lineage recurrence

Consider two persistent lineage labels

\[
L_a,
\qquad
L_b,
\]

with nonzero fixed fluxes

\[
|\Phi_a|,|\Phi_b|\ge\phi_0
\]

and fixed noncollinear directions

\[
\xi_a\cdot\xi_b=c_*,
\qquad
|c_*|<1.
\]

Suppose each lineage is persistent without net transfer to the other:

\[
\bar F_{ab}=\bar F_{ba}=0.
\]

This is allowed by M5-498: a recurrent component may contain self-contained persistent vertices with no positive transfer edge.

The dual relation is an interaction mark, not necessarily a material-transfer edge.

---

## 3. Anchored directional balances can be assigned consistently

For each lineage impose the anchored marker relation

\[
\boxed{
\tau_i=-\mathcal D_i,
\qquad i=a,b.
}
\]

M5-548 limits exact equality to the representative lineage but allows positive-volume near-recycling tubes around active markers.

There is no graph-level rule preventing both lineages from carrying such anchored states recurrently.

No lineage-count increase occurs.

No directed transfer is required.

---

## 4. Parallel amplitude balances can also close independently

On the nondegenerate marker branch, M5-549 gives

\[
\boxed{
\left\langle
\sigma_i+
\frac{\xi_i\cdot\Delta W_i}{\rho_i}
\right\rangle
=1.
}
\]

Thus choose bounded recurrent amplitude functions `rho_i(theta)` whose logarithmic derivatives have zero mean.

At the ledger level, positive axial strain can be canceled by signed parallel diffusion while the amplitude returns to its previous range.

Again there is no graph-storage contradiction.

---

## 5. Flux can remain recurrent with zero signed drift

M5-489 gives

\[
\Phi_i(\theta_{j+1})-
\Phi_i(\theta_j)
=D_i(j),
\]

and recurrence allows

\[
\langle D_i\rangle=0
\]

with either frozen flux or oscillatory zero-mean diffusion.

Thus the two labels can preserve their fixed-flux identities indefinitely without creating a one-sign graph potential.

---

## 6. First-hitting strain floors only impose thresholds

M5-550 gives at first-hitting events

\[
\sigma_i\ge1
\]

and therefore a fixed strain-energy floor.

But this only requires

\[
Z_*\ge Z_{strain}^{min}.
\]

A recurrent abstract cycle can simply live above that critical threshold.

Sequential events do not require the strain budget to grow without bound.

---

## 7. Migration is not graph-forced

The material-surface migration branch is activated only if a chosen marker amplitude degenerates.

The abstract recurrent configuration may remain on the uniformly nondegenerate marker lane instead.

Therefore the graph cannot force a migration edge merely from recurrence.

Conversely, if migration occurs, its surface-current cost can currently be paid by the derivative-production ledger.

Hence migration does not create a mandatory nonrecyclable graph edge either.

---

## 8. Explicit abstract ledger consistency

At the level of the currently proved scalar/vector constraints, one can therefore impose simultaneously

\[
\boxed{
\begin{aligned}
&\Phi_a,\Phi_b\neq0,\\
&\xi_a,\xi_b\text{ fixed and noncollinear},\\
&\tau_i=-\mathcal D_i,\\
&\langle D_B\log\rho_i\rangle=0,\\
&\left\langle
\sigma_i+\frac{\xi_i\cdot\Delta W_i}{\rho_i}
\right\rangle=1,\\
&\langle D_{flux,i}\rangle=0,\\
&Z_*\ge Z_{strain}^{min}.
\end{aligned}
}
\]

Nothing in the finite directed-graph bookkeeping alone makes this list inconsistent.

This is an **abstract ledger consistency model**, not a claim that an actual Navier--Stokes solution realizing it exists.

---

## 9. What the graph abstraction has forgotten

The apparent independence of the two lineage balances is artificial.

In an actual Navier--Stokes field,

\[
\boxed{
\Sigma
=
\mathcal R_{strain}[W]
}
\]

is a nonlocal singular-integral transform of the **same global vorticity field** `W`.

Likewise

\[
\Delta W
\]

comes from the same Eulerian vorticity field and is linked to the same transport equation.

Thus the objects

\[
\Sigma_a,
\Sigma_b,
\Delta W_a,
\Delta W_b
\]

cannot actually be assigned independently to different graph vertices.

The finite graph records lineage identity and transfer, but it forgets the common PDE source.

---

## 10. Shared Biot--Savart coupling

For two active positions

\[
x_a,
\qquad
x_b,
\]

we have

\[
\boxed{
\Sigma(x_i)
=
\operatorname{p.v.}
\int K_S(x_i-z)W(z)dz,
\qquad i=a,b,
}
\]

with one and the same vorticity `W`.

Therefore

\[
\boxed{
\Sigma(x_a)-\Sigma(x_b)
=
\operatorname{p.v.}
\int
\left[
K_S(x_a-z)-K_S(x_b-z)
\right]W(z)dz.
}
\]

Similarly the two projected diffusion vectors are evaluations of one common `Delta W` field.

This shared-source relation is absent from the abstract graph ledger.

---

## 11. Why the earlier same-tensor shortcut is still forbidden

M5-491 correctly prohibited replacing

\[
\Sigma_a,
\Sigma_b
\]

by one identical tensor.

M5-551 does not undo that firewall.

The new target is subtler:

\[
\boxed{
\Sigma_a\neq\Sigma_b
\quad\text{but they are correlated through one Biot--Savart source.}
}
\]

A useful rigidity estimate must exploit the kernel difference rather than assume equality.

---

## 12. Candidate common-source estimate

If the persistent pair separation stays within a fixed finite range,

\[
|x_a-x_b|\le L_{pair},
\]

split the common Biot--Savart source into

1. neighborhoods of the two active carriers;
2. the bounded intermediate core;
3. the remote endpoint tail.

M5-542 makes contribution 3 arbitrarily small per generation.

The finite core then determines both tensors through explicit kernel weights.

The target is a bound of the schematic form

\[
\boxed{
\mathcal C_{pair}
:=
\text{simultaneous anchored balance on }a,b
\le
\text{finite-core interaction functional},
}
\]

where the right side has a sign/coercivity property unavailable to the independent-vertex model.

---

## 13. Updated final rigidity problem

The proof obstruction is no longer

- a tail problem;
- a compactness problem;
- a component-selection problem;
- a finite-memory problem;
- or a graph-cycle problem.

It is now the following PDE coupling problem:

> **Shared-source rigidity.** Can one common smooth finite-enstrophy vorticity field sustain a recurrent finite set of noncollinear persistent material lineages, each satisfying the audited anchored/migration balances, while the Biot--Savart strain and viscous Laplacian simultaneously close every lineage ledger with zero invariant excess?

This is the narrowest formulation reached so far.

---

## 14. Highest-value next target

Analyze the kernel-difference identity

\[
\Sigma(x_a)-\Sigma(x_b)
=
\int
\bigl[K_S(x_a-z)-K_S(x_b-z)\bigr]W(z)dz
\]

on the persistent dual pair.

The remote part is already negligible.

For the finite core, split the integral into the two coherent carrier balls and the residual core.

The immediate questions are:

1. does fixed noncollinearity plus fixed flux force a nonzero lower bound on one cross-interaction block;
2. can both anchored cancellation relations be satisfied if that cross block is nonzero;
3. if cancellation is possible, does it require a third coherent source/reformation event, returning to finite-memory pressure?

This is the next concrete analytic calculation.

---

## 15. Status

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]