# DSD W1 Periodic Physical Terminal Trace Interface

Date: 2026-08-26

Status: **AUDIT-CORRECTED / THE PERIODIC OMEGA-LIMIT DSS ORBIT HAS A STATIC PHYSICAL `1/r` TERMINAL TRACE, BUT THIS TRACE IS NOT AUTOMATICALLY INHERITED BY THE ORIGINAL FINITE-ENERGY PARENT ON FIXED PHYSICAL ANNULI / A DIAGONAL RATE GATE IS IDENTIFIED / GLOBAL REGULARITY UNPROVED.**

## 1. Canonical periodic Leray tail

For the periodic W1 omega-limit orbit `U_per`, the canonical far tail has the form

\[
T(Y,s)
=
\frac1{|Y|}
\Phi\left(\widehat Y,\log|Y|-\frac s2\right),
\]

with logarithmic period `L=S/2`.

On same-phase remote cells of the periodic orbit,

\[
U_{per}-T\in L^2\cap L^3
\]

with the previously derived cell decay.

This statement concerns the periodic omega-limit orbit itself.

---

## 2. Exact inverse transform of the periodic orbit

Let

\[
\tau=T_*-t,
\qquad
Y=\frac{x-X_*}{\sqrt\tau},
\qquad
s=-\log\tau.
\]

For the tail term,

\[
\log|Y|-\frac s2
=
\log|x-X_*|.
\]

Hence the physical field generated by the periodic DSS orbit has the static leading trace

\[
\boxed{
u_*(x)
=
\frac1{|x-X_*|}
\Phi\left(
\widehat{x-X_*},
\log|x-X_*|
\right).
}
\]

It obeys

\[
\boxed{
u_*(X_*+\lambda x)=\lambda^{-1}u_*(X_*+x),
\qquad \lambda=e^L.}
\]

For the *periodic DSS solution obtained by inverse-transforming `U_per`*, the canonical tail therefore yields the expected static physical `1/r` trace.

---

## 3. Exact local integrability class of that trace

For nonzero bounded periodic `Phi`, near `X_*`,

\[
|u_*|\sim r^{-1}.
\]

Thus

\[
\boxed{
u_*\in L^q_{loc}\quad(q<3),}
\]

while

\[
\boxed{
u_*\notin L^3_{loc},
\qquad
u_*\in L^{3,\infty}_{loc}.}
\]

Also

\[
\int_{B_\rho}|u_*|^2dx=O(\rho),
\]

so

\[
\boxed{
u_*\in L^2_{loc}.}
\]

But

\[
|\nabla u_*|\sim r^{-2},
\]

hence

\[
\boxed{
u_*\notin H^1_{loc}.}
\]

Therefore the periodic DSS model trace belongs to

\[
\boxed{
L^2_{loc}\cap L^{3,\infty}_{loc}
\setminus
(L^3_{loc}\cup H^1_{loc}).
}
\]

This local integrability calculation remains valid.

---

## 4. Audit correction: the original parent is not the periodic orbit

Let `U_orig(s)` denote the actual rescaled finite-energy prelimit orbit and let `U_per` be a periodic omega-limit orbit.

Compactness/recurrent extraction gives, along some sequence `s_n -> infinity`, convergence of `U_orig(s_n)` to `U_per(0)` in the topology supplied by W1.

That convergence is a fixed-Leray-scale statement.

However a fixed physical annulus

\[
0<r_1<|x-X_*|<r_2
\]

corresponds at time `s_n` to the expanding Leray annulus

\[
e^{s_n/2}r_1<|Y|<e^{s_n/2}r_2.
\]

Therefore the earlier inference

\[
U_{orig}(s_n)\to U_{per}
\quad\Longrightarrow\quad
u_{orig}(t_n)\to u_*
\text{ on fixed physical annuli}
\]

is **not automatic**.

It exchanges the omega-limit `n -> infinity` with the far-field `|Y| -> infinity` along the diagonal

\[
|Y|\asymp e^{s_n/2}.
\]

That exchange was not proved.

Accordingly, all earlier claims that the finite-energy parent strongly converges on every fixed punctured annulus to the periodic trace are withdrawn.

---

## 5. Quantitative diagonal-rate gate

Assume for some `p>3`

\[
\varepsilon_n
:=
\|U_{orig}(s_n)-U_{per}(0)\|_{L^p(\mathbb R^3)}.
\]

Let

\[
t_n=T_*-e^{-s_n}.
\]

For the corresponding physical fields,

\[
u_{orig}(x,t_n)-u_{per}(x,t_n)
=
e^{s_n/2}
\left[
U_{orig}(Y,s_n)-U_{per}(Y,0)
\right],
\]

with `dx=e^{-3s_n/2}dY`.

Hence on any fixed physical annulus `A_x`,

\[
\boxed{
\|u_{orig}(t_n)-u_{per}(t_n)\|_{L^p(A_x)}
\le
 e^{\frac{p-3}{2p}s_n}\varepsilon_n.
}
\]

Since `A_x` has finite volume, the same upper bound controls every physical `L^q(A_x)` norm with `1<=q<=p`, up to a fixed annulus constant.

Therefore a sufficient rate for diagonal inheritance is

\[
\boxed{
\varepsilon_n
=o\left(
e^{-\frac{p-3}{2p}s_n}
\right).
}
\]

Equivalently, omega-limit attraction must beat the expansion rate of the diagonal physical annulus.

Minimality and compact recurrence alone provide no such exponential attraction rate.

---

## 6. Relation to the all-age co-moving transport audit

The previous all-age calculation gives another warning.

A fixed physical radius `r` at late Leray time `s_n` corresponds to

\[
R_n=e^{s_n/2}r.
\]

Transporting that shell backward to a fixed Leray radius `R_0` requires

\[
h_n=2\log(R_n/R_0)
=s_n+2\log(r/R_0).
\]

Hence the base time is

\[
\boxed{
s_n-h_n
=2\log(R_0/r),
}
\]

which is independent of `n`.

Thus the fixed-physical-radius diagonal remembers a fixed earlier part of the actual parent history rather than automatically converging to the late periodic omega-limit phase.

This is consistent with the diagonal-rate obstruction above.

---

## 7. What remains valid about the periodic DSS model

The following statements remain valid for the physical solution generated by the periodic orbit itself:

1. the leading terminal trace is static and DSS;
2. it has `1/r` amplitude;
3. it is locally finite-energy;
4. it is critical weak-`L3` and non-`H1`;
5. the nonresonant `r^{-3}` correction can carry a nonvanishing time derivative, so the static trace need not solve stationary Navier--Stokes.

These facts describe the *model backward DSS survivor*.

They do not by themselves identify the terminal trace of the original finite-energy parent.

---

## 8. Corrected parent--trace compatibility target

The periodic branch now has the logically correct form

\[
\boxed{
\begin{array}{c}
U_{orig}(s_n)\to U_{per}(0)
\text{ on the W1 omega-limit topology}
\\[1mm]
+
\\[1mm]
U_{per}\text{ has a canonical critical }1/r\text{ far tail}
\\[1mm]
\not\Rightarrow
\\[1mm]
\text{the finite-energy parent inherits that tail on }
|Y|\sim e^{s_n/2}.
\end{array}
}
\]

A true parent--trace theorem must supply one of the following:

- a quantitative omega-limit attraction rate strong enough to beat `e^{(p-3)s/(2p)}`;
- an independent diagonal inheritance estimate;
- or a different global-anchor argument that avoids identifying the parent's fixed-annulus terminal trace with the periodic orbit trace.

Thus the precise remaining gate is

\[
\boxed{
\text{OMEGA-LIMIT TAIL}
\longrightarrow
\text{PRELIMIT DIAGONAL TAIL}
}
\]

and this implication is currently **UNPROVED**.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
