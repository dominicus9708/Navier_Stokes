# DSD M5-138 — Full Half-Integer Pressure-Multipole Spectrum

Date: 2026-08-27

Status: **CORRECTED FULL FIBER SPECTRUM / THE NATURAL FUCHSIAN VARIABLE `z=r^-2` ADMITS PUiseux HALF-INTEGER STRONG CORRECTIONS / A DECAYING HARMONIC PRESSURE MULTIPOLE OF EVERY DEGREE `ell>=1` APPEARS AT RELATIVE FUCHSIAN POWER `z^{(ell-1)/2}` / `ell=1` IS THE UNIQUE CRITICAL TAIL MODE AND IS FACTOR-FIXED, WHILE `ell>=2` ARE STRONG SUBLEADING CANDIDATE FIBER CHANNELS / GLOBAL REGULARITY UNPROVED.**

---

## 1. Why integer powers are insufficient

The M5-135 tail self-interaction hierarchy naturally produces

\[
r^{-2},r^{-4},r^{-6},\ldots
\]

pressure orders, i.e. integer powers of

\[
z=r^{-2}.
\]

However a general strong same-tail correction need not be generated solely by that hierarchy.

For example, a localized/core stress can produce a far pressure contribution of order

\[
r^{-3}.
\]

Relative to the critical factor `r^-2`,

\[
r^{-3}=r^{-2}z^{1/2}.
\]

Thus the full Fuchsian pressure expansion must allow Puiseux powers

\[
\boxed{z^{m/2},\qquad m=0,1,2,\ldots.}
\]

---

## 2. General decaying harmonic pressure multipoles

Every decaying harmonic scalar multipole on the punctured three-space has the form

\[
\boxed{
P_{\ell}^{harm}(Y)
=
r^{-(\ell+1)}Y_\ell(\theta),
\qquad \ell=0,1,2,\ldots.
}
\]

Indeed

\[
\Delta\left(r^{-(\ell+1)}Y_\ell\right)=0
\qquad(Y\ne0).
\]

The `ell=0` mode is `r^-1`, which is slower than the audited critical pressure scale `r^-2` and is excluded from the W1 canonical pressure class.

Hence the admissible harmonic critical/subcritical multipoles start at

\[
\boxed{\ell=1.}
\]

---

## 3. Fuchsian power of each multipole

Write

\[
P=r^{-2}\Pi(z,\eta,\theta).
\]

For an `ell`th harmonic multipole,

\[
r^{-(\ell+1)}
=
r^{-2}r^{-(\ell-1)}
=
r^{-2}z^{(\ell-1)/2}.
\]

Therefore

\[
\boxed{
\Pi_{\ell}^{harm}
=
z^{(\ell-1)/2}Y_\ell(\theta).
}
\]

The full harmonic spectrum is consequently

\[
\begin{array}{c|c|c}
\ell & P^{harm} & \text{relative Fuchsian power}\\
\hline
1 & r^{-2}Y_1 & z^0\\
2 & r^{-3}Y_2 & z^{1/2}\\
3 & r^{-4}Y_3 & z^1\\
4 & r^{-5}Y_4 & z^{3/2}\\
5 & r^{-6}Y_5 & z^2\\
\vdots & \vdots & \vdots
\end{array}
\]

Thus the integer-`z` sector sees odd `ell`, while the half-integer sector sees even `ell`.

---

## 4. General coefficient check

Take a pressure term

\[
P=r^{-\gamma}\psi(\eta,\theta),
\qquad\gamma>0.
\]

For `alpha=-gamma`, the homogeneous pressure operator is

\[
-\left[
\partial_\eta^2
+(1-2\gamma)\partial_\eta
+\gamma(\gamma-1)
+\Delta_{S^2}
\right]\psi=0.
\]

For a constant-in-`eta` spherical harmonic of degree `ell`, bounded resonance requires

\[
\gamma(\gamma-1)=\ell(\ell+1).
\]

The decaying solution is

\[
\boxed{\gamma=\ell+1.}
\]

Hence every admissible decaying spherical harmonic degree `ell>=1` has exactly the physical radial power listed above.

---

## 5. Critical versus strong channels

### `ell=1`

\[
P_1^{harm}=r^{-2}Y_1.
\]

This is exactly critical:

\[
P_1^{harm}\in L^{3/2,\infty}
\]

but not in finite-index borderline Lorentz spaces over an arbitrarily deep critical range.

M5-134 therefore fixes its coefficient on each canonical velocity-tail fiber.

### `ell>=2`

\[
P_\ell^{harm}=r^{-(\ell+1)}Y_\ell
\]

decays strictly faster than `r^-2`.

These modes belong to stronger far-pressure classes and can occur inside pressure differences generated by a strong

\[
Z\in L^2\cap L^3
\]

fiber.

Thus they remain candidate P1 variables.

---

## 6. Physical terminal interpretation

Under inverse Leray scaling,

\[
p_{phys}(x,t)
=\tau^{-1}P\left(x/\sqrt\tau,s\right),
\qquad \tau=T_*-t.
\]

An `ell`th harmonic multipole gives

\[
\boxed{
p_\ell^{harm}(x,t)
=
\tau^{(\ell-1)/2}
|x|^{-(\ell+1)}Y_\ell(x/|x|).
}
\]

Thus:

- odd `ell` give integer powers of terminal time;
- even `ell` give half-integer powers in this similarity/Fuchsian representation.

The existence of half-integer similarity powers does not by itself contradict smooth terminal behavior on a fixed punctured annulus, because these terms describe scale-coupled center multipoles rather than an ordinary local Taylor expansion at fixed `x` without additional cancellation.

That interface must be audited separately.

---

## 7. Relation to whole-space core pressure

A localized stress tensor produces far pressure through the double derivative of the Newtonian kernel, whose leading decay is typically

\[
r^{-3}.
\]

This is precisely the `ell=2`, `z^{1/2}` strong sector.

Therefore the even/half-integer modes are not artificial algebraic possibilities; they are the natural far multipoles of strong interior pressure sources.

---

## 8. DSD four-chain audit

### Formation — GREEN

The full spectrum is generated from the actual harmonic solutions of the pressure Poisson equation, without assuming an integer parabolic series.

### Axis — GREEN

Radial homogeneity, spherical rank, and Fuchsian power are kept distinct.

### Static aggregation — GREEN

Only `ell=1` is counted as part of the critical pressure tail. All `ell>=2` modes are strong subleading channels.

### Dynamics — YELLOW

This note classifies allowable harmonic modes but does not prove which coefficients are dynamically realizable by one W1 orbit.

### Cross-audit — GREEN

M5-137 remains valid as the integer-`z` subset. M5-138 corrects the earlier overextension to the full fiber.

---

## 9. Revised P1 interpretation

The same-tail fiber is not merely an odd multipole tower.

Its pressure-side candidate data are

\[
\boxed{
\{\text{harmonic multipoles of every degree }\ell\ge2\}
}
\]

plus nonharmonic strong pressure corrections determined by the strong velocity quotient.

The tail-factor injectivity problem is therefore equivalent to proving that the whole-space unforced NSE/Fuchsian extension uniquely selects or eliminates all of these strong subleading channels.

---

## 10. RED firewall

Do not restrict the full P1 fiber to integer powers of `z`.

Do not infer ordinary terminal Taylor analyticity directly from the similarity/Fuchsian Puiseux expansion; fixed-`x` terminal regularity and scale-coupled center multipoles are different limiting procedures.

---

## 11. Next gate

The next useful audit is not another finite-order resonance list.

It is to determine which strong multipole coefficients are compatible with the **same unforced finite-energy prelimit** and whether compact recurrence can preserve a nontrivial sequence of such coefficients without producing an additional critical tail.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]