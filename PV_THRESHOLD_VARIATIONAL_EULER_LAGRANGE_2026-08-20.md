# Variational Threshold Core for the P_V H1 Functional — 2026-08-20

Overall status: **LOCAL VARIATIONAL REDUCTION — GLOBAL REGULARITY NOT PROVED.**

This note continues the non-H/non-T `P_V` branch after threshold localization and the curvature/precompactness bootstrap.

---

## 1. Local cubic H1 production functional

The exact H1 residual contraction can be written without explicit `u` or `omega` as

\[
N(S):=-\langle \mathcal R_{VI},-\Delta S\rangle
\]

with

\[
\boxed{
N(S)
=-\int_{\mathbb R^3}
\left[
S_{k\ell}\,\partial_k S:\partial_\ell S
+2\sum_k\operatorname{tr}\bigl(S(\partial_kS)^2\bigr)
\right]dx.
}
\]

The hyperdissipation is

\[
H(S)=\|\Delta S\|_2^2.
\]

Hence the local H1 efficiency quotient is

\[
\eta(S)=\frac{N(S)}{H(S)}.
\]

The nonlocality of the Navier--Stokes representation now remains only in the constraint that `S` belongs to the strain constraint space `L^2_st`.

---

## 2. Why both amplitude and spatial scale must be fixed

Pure amplitude scaling `S -> cS` gives

\[
N(cS)=c^3N(S),\qquad H(cS)=c^2H(S),
\]

so

\[
\eta(cS)=c\eta(S).
\]

Thus the first-hitting amplitude cap is essential.

Likewise pure coordinate dilation

\[
S_b(x)=S(bx)
\]

preserves the pointwise strain/vorticity amplitude but yields

\[
N(S_b)=b^{-1}N(S),\qquad H(S_b)=bH(S),
\]

hence

\[
\boxed{\eta(S_b)=b^{-2}\eta(S).}
\]

Therefore an unconstrained variational problem is not meaningful: a broadening sequence can make the quotient arbitrarily large. The non-T tightness/first-hitting scale fixing is mathematically essential, not cosmetic.

---

## 3. Smooth fixed-energy/fixed-radius slice

For the Euler--Lagrange calculation, consider a smooth variational slice

\[
E(S)=\|S\|_2^2=E_0,
\]

\[
M(S)=\int |x|^2|S(x)|^2dx=M_0,
\]

with the center fixed separately. This is a smooth proxy for one level set inside the precompact first-hitting threshold class.

Maximize

\[
\eta(S)=N(S)/H(S)
\]

subject to `S in L^2_st`, `E=E_0`, and `M=M_0`.

---

## 4. First variation of N

Set

\[
(M_{sp})_{k\ell}=\partial_kS:\partial_\ell S,
\qquad
M_{rg}=\sum_k(\partial_kS)^2.
\]

A direct variation and integration by parts give

\[
\delta N
=\int \mathcal E_N(S):\delta S\,dx,
\]

where

\[
\boxed{
\begin{aligned}
\mathcal E_N(S)
={}&-M_{sp}-2M_{rg}
+2\partial_k\left(S_{k\ell}\partial_\ell S\right)\\
&+2\sum_k\partial_k\left[(\partial_kS)S+S(\partial_kS)\right].
\end{aligned}
}
\]

Here the first two terms are interpreted as symmetric matrix fields in the same matrix index space as `S`.

Also

\[
\delta H=2\int \Delta^2S:\delta S,
\]

\[
\delta E=2\int S:\delta S,
\]

\[
\delta M=2\int |x|^2S:\delta S.
\]

---

## 5. Euler--Lagrange equation in the strain constraint space

Let the maximizing quotient be

\[
\Lambda=\eta(S)=N/H.
\]

Then there are Lagrange multipliers `alpha,beta` such that

\[
\boxed{
P_{st}\left[
\mathcal E_N(S)
-2\Lambda\Delta^2S
-2\alpha S
-2\beta |x|^2S
\right]=0.
}
\]

This is a fourth-order nonlinear eigenvalue equation with a confining moment term. It is local before the final strain-space projection.

The true first-hitting problem has an `L^infty` vorticity cap rather than an `L^2` amplitude constraint. The present equation is therefore a smooth slice equation, not yet the full KKT system for the exact first-hitting cap.

---

## 6. Two homogeneity identities

Use the amplitude variation `delta S = S`. Since

\[
\delta N=3N,\qquad
\delta H=2H,\qquad
\delta E=2E,\qquad
\delta M=2M,
\]

stationarity gives

\[
3N-2\Lambda H-2\alpha E-2\beta M=0.
\]

Because `Lambda H=N`,

\[
\boxed{
N=2\alpha E+2\beta M.
}
\]

Now use the coordinate-dilation variation generated by `x dot grad S`. The scaling laws are

\[
N(S_b)=b^{-1}N,
\quad
H(S_b)=bH,
\quad
E(S_b)=b^{-3}E,
\quad
M(S_b)=b^{-5}M.
\]

Stationarity at `b=1` gives

\[
-N-\Lambda H+3\alpha E+5\beta M=0,
\]

hence

\[
\boxed{
2N=3\alpha E+5\beta M.
}
\]

Solving the two linear identities yields the exact Pohozaev balance

\[
\boxed{
\alpha E=\beta M=\frac14N.
}
\]

Thus any positive threshold maximizer on a fixed-energy/fixed-radius slice must distribute its confinement multipliers equally between amplitude and spatial moment in the weighted sense above.

---

## 7. Reduced nonlinear eigenproblem

Substituting the Pohozaev multipliers gives

\[
\boxed{
P_{st}\left[
\mathcal E_N(S)
-2\Lambda\Delta^2S
-\frac{N}{2E}S
-\frac{N}{2M}|x|^2S
\right]=0.
}
\]

Therefore a hypothetical threshold maximizer is not an arbitrary recurrent core. It must solve a very rigid fourth-order strain-compatible nonlinear eigenproblem with a self-consistent harmonic confinement.

---

## 8. Consequences and remaining issues

1. A maximizing sequence that broadens spatially is excluded by fixing `M/E`, matching the non-T tightness requirement.
2. A pure amplitude runaway is excluded by fixing an amplitude slice; the exact first-hitting problem should ultimately replace this with the vorticity `L^infty` KKT constraint.
3. Exact maximal H1 production has already been shown to force a one-dimensional max-mid profile incompatible with nonzero whole-space `L^2` energy. Hence any true maximizer of the compact threshold class must lie strictly below the algebraic saturation state.
4. The remaining quantitative question is whether the variational maximum `Lambda_K` of the actual first-hitting compact class is strictly below viscosity `nu`.

Status: **THE P_V THRESHOLD PROBLEM HAS BEEN REDUCED TO A CONSTRAINED FOURTH-ORDER STRAIN-COMPATIBLE NONLINEAR EIGENPROBLEM. A POSITIVE MAXIMIZER ON A FIXED ENERGY/RADIUS SLICE SATISFIES THE EXACT POHOZAEV BALANCE alpha E = beta M = N/4. GLOBAL REGULARITY REMAINS UNPROVED.**