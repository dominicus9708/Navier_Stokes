# DSD M5-606 — Recurrent negative-kappa sink packets saturate into the persistent network or force viscous flux oscillation

Date: 2026-09-03

Status: **FINITE-MEMORY CE-H SINK SATURATION / M5-605 TURNS EVERY FIXED RESIDUAL NEGATIVE-KAPPA SHARE INTO A COHERENT FIXED-FLUX SINK PACKET / M5-488 FINITE MEMORY THEN FORCES RECURRENT SUCH PACKETS EITHER TO BE ABSORBED INTO A FINITE PERSISTENT MATERIAL-FLUX NETWORK OR TO PAY POSITIVE-DENSITY COMPENSATING EXITS / ON GLOBAL CE-H, PROJECTIVE REORGANIZATION IS ABSENT (`D_B xi=0`) AND REMOTE EXPORT IS EXCLUDED BY M5-604, SO THE ONLY GENUINE COMPENSATING EXIT IS VISCOUS MATERIAL-FLUX CHANGE / USING M5-603, POSITIVE-DENSITY VISCOUS TURNOVER IS EXACTLY POSITIVE ABSOLUTE ACTIVITY OF THE FLUX-WEIGHTED KAPPA DRIFT WITH ZERO SIGNED MEAN / GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Input sink packet

M5-605 gives, whenever a fixed fraction of the finite-core negative-kappa budget lies outside the represented persistent carriers, a coherent packet with

\[
|W|\ge w_0>0,
\qquad
\kappa\le-\kappa_0<0,
\qquad
|\Phi_{sink}|\ge\phi_0>0.
\]

The packet has fixed normalized spatial scale and direction coherence.

---

## 2. Finite-memory genealogy

Treat the sink packet as a fixed-flux material population in the M5-488 storage ledger.

Every recurrent residual sink event has the genealogy split

\[
\boxed{
\text{sink packet}
\Longrightarrow
\text{existing-lineage absorption}
\lor
\text{new/replacement label}
\lor
\text{compensating exit}.
}
\]

Only finitely many fixed-flux populations can be stored quietly:

\[
N\le N_{max}.
\]

Therefore indefinite recurrent creation of genuinely new sink labels is impossible without positive-density compensating exits.

---

## 3. CE-H removes two compensating channels

On the global CE-H branch,

\[
D_B\xi=0,
\]

so the projective reorganization channel used in the general M5-488 ledger is absent.

M5-604 removes a fixed remote/export sink budget from the compact branch.

Hence the recurrent compensating exit reduces to

\[
\boxed{X=X_{visc\ flux}.}
\]

The finite-memory cocycle becomes

\[
\boxed{
\langle R_{sink}\rangle
\le
N_{max}\langle X_{visc\ flux}\rangle.
}
\]

---

## 4. Saturated quiet branch

If

\[
\langle X_{visc\ flux}\rangle=0,
\]

then recurrent residual sink packets cannot keep creating new labels.

After finitely many absorptions/additions the compact branch reaches a saturated finite family

\[
\mathcal L_{\kappa}
=
\{L_1,\ldots,L_{N_\kappa}\},
\qquad N_\kappa\le N_{max},
\]

which represents all recurrent fixed negative-kappa sink packets up to arbitrarily small residual budget.

Thus

\[
\boxed{
\text{quiet CE-H sink branch}
\Longrightarrow
\text{finite persistent kappa-sink network}.
}
\]

---

## 5. Viscous-turnover branch

Suppose instead

\[
\langle X_{visc\ flux}\rangle>0.
\]

For each affected persistent material surface patch, M5-603 gives

\[
\frac{d}{d\theta}\log|\Phi|
=\bar\kappa_\Phi.
\]

A fixed viscous flux-change event thickens in similarity time by compact smoothness, hence a positive-density turnover branch gives

\[
\boxed{
\langle|\bar\kappa_\Phi|\rangle>0
}
\]

on at least one persistent/recurring lineage component.

But bounded recurrent flux gives

\[
\boxed{
\langle\bar\kappa_\Phi\rangle=0.
}
\]

Therefore

\[
\boxed{
\langle(\bar\kappa_\Phi)_+\rangle
=
\langle(\bar\kappa_\Phi)_-\rangle
=
\frac12\langle|\bar\kappa_\Phi|\rangle
>0.
}
\]

The CE-H viscous-turnover branch is thus an actual sign-changing flux oscillation, not a one-sided flux loss.

---

## 6. Updated CE-H split

The negative-kappa measure bridge has now reduced to

\[
\boxed{
\text{CE-H}
\Longrightarrow
\begin{cases}
\text{finite persistent kappa-sink network},\\
\text{or positive-density sign-changing viscous-flux oscillation}.
\end{cases}
}
\]

The second branch is compatible with bounded flux because positive and negative `kappa` episodes can cancel in signed mean.

It is therefore not yet a contradiction.

---

## 7. Next target

A new structural identity is needed to constrain the spatial organization of `kappa` itself.

The global CE-H eigenvalue equation

\[
\Delta W=\kappa W
\]

admits a Pohozaev/dilation multiplier. Because the similarity proof is fundamentally scale-critical, the next audit should pair this eigenvalue equation with

\[
y\cdot\nabla W
\]

to determine whether the negative mean `kappa` budget necessarily forces a signed radial `kappa` gradient.

This may connect the zero-mean material-flux cocycle to a spatial monotonicity constraint.

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
