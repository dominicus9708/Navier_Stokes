# DSD M18-046 — Upstream remote-root compression

Date: 2026-09-11

Status: **ROOT-CERT GLOBAL BRANCH AUDIT. SEVERAL APPARENTLY DISTINCT UPSTREAM COMPLEMENTS—CENTER TURNOVER, NORMALIZED-ENSTROPHY ESCALATION, AMBIENT-STRAIN ESCALATION, DETACHED-ANCIENT RESTART FAILURE, AND UPPER STAGE-CLOCK FAILURE—ARE NOT INDEPENDENT ROOTS. EXISTING M5 MODULES ROUTE THEM INTO A MUCH SMALLER REMOTE/TYPE-II FRONTIER. THE GENUINE REMOTE HARD CORE IS AN ITERATED REMOTE/SHELL-H CASCADE, SOURCE-SCALE TYPE-II COMPACTNESS FAILURE OR NONTRIVIAL ANCIENT EULER LIMIT, AND DRIFT/REALIZATION DEFECTS. GLOBAL REGULARITY REMAINS UNPROVED.**

---

## 1. Purpose

M18-041--045 separated the late CE-H analysis from the upstream branch-completeness problem.

The present audit asks whether the upstream complement menu is being overcounted.

The answer is yes.

Several historical labels describe different representations of the same remote/noncompact mechanism.

---

## 2. Center turnover is not independent

M5-399 proves the exhaustive split

\[
\boxed{
\text{adjacent center genealogy}
\Longrightarrow
\text{nested center}
\lor
S_{remote}^{formed}.
}
\]

Thus unbounded center motion is itself a formed remote active satellite.

No separate `center-turnover root` should remain in ROOT-CERT.

---

## 3. Normalized-enstrophy escalation is not independent

M5-401 proves, under the first-hitting amplitude cap,

\[
\boxed{
Z_j:=\|\Omega_j\|_2^2\to\infty
\Longrightarrow
S_{remote}.
}
\]

The mechanism is the dyadic critical parameter

\[
\Lambda_R=R^2\sup_{A_R}|\Omega|.
\]

Uniformly bounded `Lambda_R` would force an `R^{-1}` shell-enstrophy tail and hence a summable global tail. Therefore unbounded normalized enstrophy necessarily creates remote scale separation.

Thus normalized-enstrophy escalation is a router, not a terminal root.

---

## 4. Ambient-strain escalation recursively creates another satellite

M5-402 sharpens

\[
S_{remote}
\Longrightarrow
T_{dynamic}\lor H_{ambient}\lor A_{detached}
\]

to

\[
\boxed{
S_{remote}^{(m)}
\Longrightarrow
T_{dynamic}^{(m)}
\lor A_{detached}^{(m)}
\lor S_{remote}^{(m+1)}.
}
\]

The key point is that unbounded ambient/harmonic strain cannot remain source-free at every scale once local rotational derivatives are controlled.

It forces satellite-frame enstrophy escalation and therefore another remote active object.

Thus `H_ambient` is not independent.

---

## 5. Detached ancient profiles mostly return to the same frontier

M5-405 gives the detached dichotomy

\[
\boxed{
A_{detached}
\Longrightarrow
H_{shell/remote}
\lor
T_{drift/realization}
\lor
\bot.
}
\]

On the quiet uniformly shell-critical branch,

\[
\sup_{t,R}R\int_{A_R}|\nabla u|^2<\infty
\]

implies

\[
\nabla u\in L^{3/2,\infty},
\qquad
u-c\in L^{3,\infty},
\]

and the retained mild ancient profile enters the imported weak-critical Liouville corridor, contradicting the nonzero satellite mark.

Therefore a nontrivial detached survivor must evade uniform shell-critical control or coherent Galilean realization.

Again the escape routes are remote/H or realization turnover, not a new detached root.

---

## 6. Upper stage-clock failure is Type-II

M18-045 proves

\[
\sup_jL_j<\infty
\iff
\sup_j\Theta_j<\infty,
\qquad
\Theta_j=W_j(T^*-t_j),
\]

up to fixed `q` constants.

Hence

\[
\boxed{
L_j\to\infty
\Longrightarrow
\Theta_j\to\infty,
}
\]

which is precisely vorticity Type-II along the first-hitting sequence.

Thus upper clock failure also collapses into the Type-II root.

---

## 7. Strong remote source reaches Euler scaling

M5-443 gives a canonical source-scale Euler normalization for a strong remote payer.

With remote distance ratio `K_j->infinity`, the effective viscosity is

\[
\boxed{
\varepsilon_j=K_j^{-2}\to0,
}
\]

while the Euler-scaled vorticity remains uniformly bounded and the local velocity oscillation remains nontrivial.

Therefore

\[
\boxed{
H_{remote}^{strong}
\Longrightarrow
E_{ancient}^{Euler,compact}
\lor
H_{Euler\text{-}scale\ noncompact}^{strong}.
}
\]

This is the true Type-II source-scale junction.

Neither branch is presently closed in full generality.

---

## 8. Compressed upstream frontier

After removing representation duplicates, the major upstream noncompact root can be written schematically as

\[
\boxed{
\mathcal R_{remote/II}
=
\mathcal R_{cascade}
\lor
\mathcal R_{Euler}
\lor
\mathcal R_{realization},
}
\]

where

### 8.1 Iterated cascade

\[
\boxed{
\mathcal R_{cascade}:
\text{remote/shell-H event}
\to
\text{new remote scale}
\to\cdots
}
\]

with no presently proved additive common-frame ancestry contradiction.

### 8.2 Euler-scale root

\[
\boxed{
\mathcal R_{Euler}:
E_{ancient}^{Euler,compact}
\lor
H_{Euler\text{-}scale\ noncompact}^{strong}.
}
\]

A compact nontrivial ancient Euler profile requires an applicable rigidity theorem; the noncompact branch requires quantitative pricing/routing.

### 8.3 Realization/drift root

\[
\boxed{
\mathcal R_{realization}:
\text{incoherent Galilean frame, projective/export, or restart realization failure}.
}
\]

These defects must not be silently removed by gauge choices.

---

## 9. What has been removed from the primitive root menu

The following should no longer be counted separately:

\[
\boxed{
\begin{aligned}
&\text{center turnover},\\
&\text{normalized-enstrophy escalation},\\
&\text{ambient-strain escalation},\\
&\text{quiet detached ancient profile},\\
&\text{upper stage-clock failure}.
\end{aligned}
}
\]

Each either closes conditionally or routes into `R_remote/II`.

This is a substantial branch-count reduction.

---

## 10. Relation to W1/ratchet survivor

The global singularity audit should now be organized at a higher level as

\[
\boxed{
\text{hypothetical singularity}
\Longrightarrow
\mathcal R_{remote/II}
\lor
\mathcal R_{local-critical}
\lor
\mathcal R_{realization}
\lor
\mathcal W_{compact},
}
\]

where `W_compact` denotes the Type-I, bounded-Z, nested, recurrent W1/ratchet corridor on which the later CE-H and non-CE-H payer analyses operate.

This does not yet prove that the displayed list is complete. It is the currently audited compressed target for the next ROOT-CERT pass.

---

## 11. Highest-value next target

The next audit should attack `R_cascade` before returning to high-order CE-H derivatives.

The critical question is whether an iterated remote-satellite chain can be represented in one common physical ancestry sufficiently to force one of:

1. additive or bounded-overlap standard-energy/palinstrophy cost;
2. geometric scale accumulation incompatible with finite-time first-hitting structure;
3. eventual Euler-scale compactness;
4. explicit realization/export failure.

A naive sum across recentered frames is forbidden.

The representation map must be proved first.

---

## 12. Status

\[
\boxed{
\text{UPSTREAM REMOTE ROOTS HAVE BEEN COMPRESSED, NOT CLOSED.}
}
\]

\[
\boxed{
\text{MAIN REMOTE HARD CORE: ITERATED CASCADE / EULER-SCALE ROOT / REALIZATION DEFECT.}
}
\]

\[
\boxed{\text{GLOBAL REGULARITY REMAINS UNPROVED.}}
\]
